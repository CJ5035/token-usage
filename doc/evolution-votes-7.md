# EVOLUTION-7 门禁1 投票记录（诊断评审）

## 第 1 轮（2026-09-06）

### 架构师：通过

建议：
1. 影响面遗漏同类位置 `chartOvTrend`（app.js:1636-1641，总览 7 日趋势）——候选清单已列但诊断 R1/R2/修复方向未覆盖，建议纳入 R2 与方向 1（空时卡内占位，与统计页两图统一），证据索引补 app.js:1641。
2. R4 修复需协调 db 层样本守卫：server 重算 pct 时须尊重 `insufficient` 语义（db.py:2395 same_y.requests<5 / 凌晨<1点）；spike（db.py:2400）双方均 db 口径内部自洽，pct 若改口径需一并明确 spike 选择，避免新口径混用。
3. R5 伴生「今日暂无用量」需定义与 `renderWindows` insufficient_sample 文案的优先级（app.js:2269）。
4. （正面确认）复用占位模式有两处已验证先例：chartZcodeTrend（app.js:906-915）、chartClaudecodeTrend（app.js:1119-1123），emptyEl 显隐经 rerenderCharts/refreshIcons noAnim 链路验证无回归；空守卫放图表函数内部与 EVOLUTION-4 缓存键控/seq 守卫天然兼容。

### 产品经理：通过

建议：
1. R3 修复未覆盖跨档位口径差：server 只把 DSH 并入 today，7d/30d 窗口不含 DSH——「今天 11.31M」与「近7天 1.19B」间 DSH 贡献不可见；hint 文案设计时统筹，或与备选池「Records 共 0 会话 vs DSH 415」合并评估。
2. 修复方向 3 备选方案「并入后 today 与 db 口径 same_y 重算 pct」有分母口径缺陷（分子含 DSH 分母不含，单边放大）；建议优先评估「dsh 今日>0 时 pct 标注『含今日 DSH』」而非直接重算。
3. 修复后仍保留两种策略（档位不适→藏卡；适用无数据→占位）——建议把产品判据写进验收标准：「语境下卡片本不适用→藏卡；适用但无数据→卡内占位」；统计页 two-col 不藏卡的技术与产品理由一致，可固化。
4. 常驻口径 hint 对 opencode 主力用户是噪音：建议评估条件显隐（主区全 0 且下方本地渠道区有数据时才显示）。
5. 验收场景显式化四组：①新用户零数据全页 ②纯本地渠道用户 ③清晨零用量时段 ④DSH 就绪前后瞬态；R5 修复后清晨时段实拍回归。

### 用户体验官：驳回

阻塞：
1. `chartOvTrend`（app.js:1640-1642 `if (!dated.length) return;` 留白）与 R2 判定的损伤完全同型，候选清单 #7 已列但诊断 R1/R2/修复方向草案 1 均未纳入——期望：R2 补证据行号，方向 1 增加「总览 7 日费用对比卡」（卡内占位，沿用 .zcode-chart-empty 先例）。否则空态策略统一后总览页仍残留空白卡，核心命题未闭环。

建议：
1. 占位文案区分两语义：「今日暂无用量」vs「该范围暂无数据」，同一 i18n key 族取词，不制造第五种文案风格。
2. records 页同类口径矛盾（共 0 会话 vs DSH 415）是否纳入应显式说明。
3. DSH 就绪前后「今天」0→11.31M 瞬态无用户可见提示，与 R3 同属状态不可见，建议在方向 2 一并评估。
4. 验收标准加入可测断言：「today.tokens=0 时任何代码路径不得渲染百分比箭头」，不仅依赖 R4 间接消除。

### 判定

streak = 0（存在驳回票）。修订诊断报告后进入第 2 轮。

## 修订对照表（第 1 轮意见 → 落实情况）

| 上轮意见 | 落实情况 |
|---|---|
| 体验官-阻塞1：chartOvTrend 纳入 | ✅ 已落实：R2 补证据（app.js:1636-1642），修复方向 1 增加「总览 7 日费用对比卡」，证据索引补行号 |
| 架构师-建议1：同上 | ✅ 同上（与阻塞项同一处落实） |
| 架构师-建议2：spike/insufficient 协调 | ✅ 已落实：修复方向 3 增加口径协调说明（insufficient 语义尊重 + spike 口径选择留计划阶段定但点名） |
| 架构师-建议3：insufficient 与「今日暂无用量」优先级 | ✅ 已落实：修复方向 4 点名该优先级问题留计划阶段 |
| 架构师-建议4：先例确认 | ✅ 已吸收：修复方向 1 引用两处先例行号 |
| PM-建议1：7d/30d 口径差 | ✅ 已落实：R3 补充该口径差事实（server.py:1079-1081 仅 today 并入），修复方向 2 的 hint 设计要求统筹说明 |
| PM-建议2：pct 修复优先「标注」方向 | ✅ 已落实：修复方向 3 改为优先评估标注方案，重算方案降为备选并注明分母缺陷 |
| PM-建议3：产品判据固化 | ✅ 已落实：修复方向 1 增加产品判据（语境不适用→藏卡；适用无数据→占位） |
| PM-建议4：hint 条件显隐 | ✅ 已落实：修复方向 2 增加条件显隐评估要求 |
| PM-建议5：四组验收场景 | ✅ 已落实：新增「验收场景」节列出四组 + 可测断言 |
| 体验官-建议1：文案两语义 | ✅ 已落实：修复方向 1/4 明确两语义 i18n key 族 |
| 体验官-建议2：records 页显式说明 | ✅ 已落实：新增「范围边界」节：records 页口径矛盾不并入本问题（备选池跟踪），理由：records 空态文案已存在，矛盾源于另一数据链路，修复应独立评估避免本问题范围膨胀 |
| 体验官-建议3：DSH 瞬态提示 | ✅ 已落实：修复方向 2 增加瞬态提示评估项 |
| 体验官-建议4：可测断言 | ✅ 已落实：验收场景节加入「today.tokens=0 不得渲染百分比箭头」断言 |

## 第 2 轮（2026-09-06）

### 架构师：通过

建议：
1. 证据索引「server.py:1079-1081」行号间接：并入动作实际在 1074-1078（重写 payload["today"]），1079-1081 是 `if channel == "dsh"` 单渠道分支（该分支反而用 dsh_win 覆盖 7d/30d，仅单渠道请求生效）；建议后续引用以 1074-1078 + 1079 分支条件为准。
2. R1/R2 末尾「对照组」段落整段重复，修订时合并到 R2 一处。
3. （正面确认）emptyEl 先例回归判断复核成立（rerenderCharts:2423-2424 noAnim 重渲两先例图、2413-2416 既有 chartToday 空守卫注释先例、seq 守卫无交互冲突）；同类位置遍历（首页三图/chartToday/统计页两图/总览趋势/sparklineSvg:1625 自守卫/各表格 noData）无遗漏，影响面闭环。

对照表核对：14/14 条全部「已落实」（明细见架构师第 2 轮输出）。

### 产品经理：通过

建议：
1. 同上行号偏差（事实陈述无误，下轮顺手校正）。
2. 方向 3 标注文案需精确为「涨跌百分比未含今日 DSH」——same_y（db.py:2379 主表单源）与 same_7（三源 merge）渠道构成不同源，不能把全部口径差笼统归结为 DSH。
3. 验收场景补第五组「有数据用户回归」：opencode 主力用户有数据时空态守卫零触发、hint 隐藏、涨跌正常显示（条件显隐的逆命题）。
4. R5 处理恰当；计划阶段清晨实拍时顺带抓取 /api/report/windows 原始响应快照，锁定精确数值路径作为断言测试输入。

对照表核对：14/14 条全部「已落实」。

### 用户体验官：通过

建议：
1. donut 空态「centerText 跳过 + .zcode-chart-empty 占位」组合需明确占位文案视觉位置（卡心居中），避免"中央空无一物+占位挤角落"或双文案并存中间态；计划中给 donut 空态一张实拍验收图（对应验收场景 1）。
2. 条件显隐 hint 位置应紧贴主区首个全 0 卡片上方或 Token 构成卡内，而非页面底部——同屏矛盾的解释必须与矛盾源头同视野（截图 07 首屏即见 4 张 0 卡）。

对照表核对：5/5 条全部「已落实」（含阻塞项四处闭环：诊断证据/修复方向/验收场景/证据索引）；本轮自查 grep 全部 9 处 new Chart 调用点，7 处损伤点均已被 R1/R2 覆盖，无第 3 处漏网。

### 判定

streak = 1（第 2 轮 3/3 全票）。按门禁状态机需连续两轮全票（streak==2），吸收本轮 9 条非阻塞建议出 v3 后进入第 3 轮。

## 第 3 轮（2026-09-06）—— 门禁1 通过轮

> 流程备注：本轮曾误以单一子代理合并三席复核（违反上下文隔离约束），该结果仅作预检；正式评审由三席独立子代理完成如下。

### 架构师：通过

建议（非阻塞，处置：①②已落实，③留计划阶段）：
1. v3 方向 3「same_y 为主表单源、same_7 为三源 merge」与代码不符——same_y（db.py:2379 经 window()→_win_merge）同为三源 merge，差异仅在时间窗与渠道贡献随时间变化；理由陈述已修正（v3 已改），结论不变更精确。
2. v3 头部「对照表见 evolution-votes-7.md」悬空——第 2 轮对照表散于各席输出，已在本文件第 2 轮小节补摘要说明。
3. （正面确认）关键行号 16 处逐一复核吻合；`.zcode-chart-empty` 本身即 absolute inset 0 + 居中，"卡心居中"与复用先例天然一致；空守卫在 rerenderCharts 三条调用路径下行为一致，方案可行。

对照表核对：7/7 全部已落实。

### 产品经理：通过

建议（非阻塞，留计划阶段）：验收场景 1 补「切至无数据档位显示『该范围暂无数据』占位」锚点——两种文案语义各有一个验收落点（场景 3 已覆盖「今日暂无用量」）。

对照表核对：7/7 全部已落实（含源码逐行复核 server.py:1074-1084 与行号校正的一致性）。

### 用户体验官：通过

建议（非阻塞，留计划阶段）：donut 空态实拍验收图时核对——`.zcode-chart-empty` 是 inset:0 全卡居中而 donut 空数据下图例条目为空、高度坍缩，确认占位呈现为视觉卡心且与标题/图例区无碰撞（无需改 v3，实拍时核对）。

对照表核对：7/7 全部已落实；抽查行号零偏差。

### 判定

streak = 2（第 2、3 轮连续全票）→ **门禁1 通过**，诊断报告 v3 定稿，进入阶段3（实施计划）。

### 第 2 轮对照表落盘说明

第 2 轮三席对照表核对明细载于各席第 2 轮评审输出（主 agent 会话留档），本文件第 2 轮小节的"对照表核对：14/14（或 5/5）全部已落实"为核对结论摘要；第 3 轮对照表（7 条）已在上方逐条列出。
