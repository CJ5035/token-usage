# 投票记录：EVOLUTION-1 门禁1（诊断评审）

## 第 1 轮（2026-09-05）

### 架构师：通过
核实记录：根因成立（单例连接 + 隐式事务模型逐项复核）；并发写清单完整（补充核查 ThreadPoolExecutor 工作线程与 WindowApi 均不触 DB）；server.py 6 处直连 execute 均为 SELECT；诊断聚焦根因。

意见（均为非阻塞建议）：
- [建议] 故障模式 1"永久中间态"需精确化：成为永久态还需 delete_account 剩余路径中途异常；另有等价变体——同步 rollback 使 delete_account 前段 DELETE 回滚、后段随自身 commit 固化，产生"账号行已删、usage_records 残留"孤儿数据。阶段 3 测试用例按两种精确时序设计。
- [建议] db.py:884 `upsert_charts_buckets` 同为显式 BEGIN 批量写，报告未点名；db.py 有 30+ 处 conn.commit()，阶段 3 需列完整写函数清单作验收对照。
- [建议] §7"脏读窗口缩到毫秒级"与 §4"毫秒~秒级"口径不一致，统一为后者。

### 产品经理：通过
意见（均为非阻塞建议）：
- [建议] 事务边界盘点补全：`upsert_charts_buckets`（db.py:884）与 insert_usage_records 同型显式 BEGIN；`delete_account` 内部 `clear_cc_summary`（db.py:1242）与 `_persist_active`（db.py:368）自带独立 commit，竞争窗口更大。阶段 3 应覆盖这些隐藏 commit 点，级联删除包进单一事务。
- [建议] 阶段 3 补并发回归验证手段（模拟长同步批次期间执行写操作，断言设置不丢失、账号状态一致）作为修复完成判据。
核对：用户价值可信、P1 定级合理（与 #2/#3 对比论证充分）、方向 A 不改变用户预期行为、触发场景覆盖真实路径。

### 用户体验官：通过
意见（均为非阻塞建议）：
- [建议] 前端两处设置开关 PUT 失败完全吞错且不回读（app.js:1971 auto_sync、app.js:1977 show_accounts_panel），与存储脱节后用户无从发现——独立前端问题，建议声明不在本问题范围，避免"修完 #1 设置保存就可靠"的误期许。
- [建议] 备选池"同步失败零反馈"与本诊断模式 3 同源，§6 修复收益处交叉注明（写路径串行化后该备选项触发面缩小）。
核对：行号全部准确；"database is locked"降级诚实正确；后果分级与前端实际行为一致。

### 汇总
3/3 通过 → streak=1（需连续两轮）。全部意见为非阻塞建议，按修订原则一次性吸收后进入第 2 轮。

## 第 2 轮（2026-09-05）

对照表核对：三席一致确认上轮 7 条意见全部"已落实"（详见各席核对记录，含 db.py 34 处 commit、db.py:884 显式 BEGIN、隐藏 commit 点、口径统一的逐行复核）。

### 架构师：通过
- [建议] 阶段 3 测试设计：时序甲的同步侧触发者应锚定到**不带显式 BEGIN 的写点**（update_sync_state db.py:1208、save_cc_summary db.py:1229——每轮同步必经、裸 commit 结尾）；insert_usage_records 的显式 BEGIN 在他线程事务未决时会抛故障模式 3 异常而非拦腰提交，照它写用例复现不出时序甲。
- [建议] §7 边界澄清：RLock 持锁后时序甲/乙即消失，剩余窗口仅为进程崩溃一致性（非并发问题、非本问题回归判据）。

### 产品经理：通过
- [建议] 并发回归断言追加："同步批次全程无 cannot start a transaction within a transaction 异常"（模式 3 的消除证据）。
- [建议] 行号微漂移记录：前端吞错实际约 app.js:1970/1976；_DEFAULT_SETTINGS 实为 db.py:1036-1041。阶段 3 引用时以实际复核为准。

### 用户体验官：通过
- [建议] 范围声明措辞扩为"设置读写链路"：补 app.js:1626-1640 renderSettings 整体 catch ignore（读取失败静默保留旧 DOM），避免阶段 3 修复后被设置页旧值误判未修。
- [建议] §6 可注明前端仅渲染 last_sync_status、last_sync_error 详情未展示（app.js:1629-1631），为备选池"同步失败零反馈"的用户可感知证据。

### 汇总
3/3 通过 → streak=2 → **门禁1 通过**。4 条新建议（均为非阻塞）带入阶段 3 实施计划落实。

---

# 门禁2（计划评审）投票记录

## 第 1 轮（2026-09-05）：0/3，三席全部驳回 → streak=0，已一次性修订计划 v2

### 架构师：驳回
- [阻塞] 写函数盘点漏网：`get_active_account_id`（db.py:371-400）三个让位分支（390/394/399）调用 `_persist_active`（UPDATE+commit），经 `_resolve_account_id` 被几乎所有读函数触达，是无锁写路径；同步显式 BEGIN 未决期间命中即复活故障模式 1/2。建议仅锁三处写分支（方案 a）。清单 20 项→21 项。
- [建议] get_db 锁内须复查 `_DB is None`（双重检查）。
- [建议] 测试补并发读线程 + 确定性 in_transaction 回归。
- [建议] 回滚措辞改"整文件恢复派发前快照"；close_db 竞态窗口可忽略（已核实仅退出/测试调用）。
- 核对：20 项清单行号全部准确；db.py 为唯一含 SQL 写模块；锁序单向无环；RLock 嵌套链无死锁。

### 产品经理：驳回
- [阻塞] 同上 get_active_account_id 漏网（§1.3"`_persist_active` 仅被持锁函数调用"声明与代码事实不符；§1.4"消除全部三类故障模式"承诺不成立）。
- [阻塞] 测试未覆盖诊断 §7 要求的"同步进行中切换账号/删除账号"用户场景（现测试仅技术场景）。
- [建议] 验收补用户可感知口径（如"删除账号后重启不复活"）。
- [建议] 注明白盒导入 `_DB_LOCK` 属有意选择。
- 认可：P1 定级、方向A改动量收益比、不改变用户语义、范围声明诚实。

### 用户体验官：驳回
- [阻塞] 锁持有时长未评估；`import_zcode_usage`/`import_claudecode_usage` 函数体前半段为 CPU 密集 payload 循环（estimate_cost_raw 每行重建定价表，zcode 首次导入为全历史单批），整体进锁会给用户写操作引入数秒级无反馈等待（前端 fetch 无超时、账号操作无 in-flight 态，重复点击会堆积请求）——本计划新引入的可用性问题。建议只锁事务段（COUNT→executemany→commit→COUNT），payload 循环留锁外，并写明各写路径持锁上限口径。
- [建议] 范围声明补 app.js:1954-1965 间隔/范围 pills 吞错（同类静默失败残留）。
- [建议] 测试 3 记录 save_settings 持锁期最大等待耗时作回归锚点。

### 汇总
三席驳回意见高度收敛（1 个代码事实阻塞 + 1 个测试覆盖阻塞 + 1 个锁粒度阻塞），全部逐条落实为计划 v2（对照表见计划文末）。进入第 2 轮。

## 门禁2 第 2 轮（2026-09-05）：3/3 通过 → streak=2 → 门禁2 通过

对照表核对：三席一致确认 10 条意见全部"已落实"。独立复核：架构师确认 21 项清单无漏网、行号全部准确、锁序无环；PM 通读 db.py 全文确认无第 22 处写函数、server.py 6 处直连均为 SELECT；UX官确认 payload 循环出锁后最坏等待低于感知阈值。

非阻塞建议（带入执行阶段备注）：
- 架构师：测试 4 等待耗时断言消息附带实测值；#21 with 块只包 `_persist_active` 调用、return 留锁外。
- PM：§1.4 第四条口径改为"统计数字错乱窗口缩至毫秒级、刷新自愈"；测试 4 补"设置不丢失"最终一致性断言（get_settings 读回一致）；可选补 clear_account/rename_account 混合调用。
- UX官：测试 4 批量构造贴近真实首导入量级（约 1 万行）；pills 为未处理 rejection（非主动吞错），.catch 实际行号 1971/1977。
