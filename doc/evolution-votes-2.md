# 投票记录：EVOLUTION-2 门禁1（诊断评审）

## 第 1 轮（2026-09-05）：架构师通过 / PM 驳回 / UX官通过 → streak=0

### 架构师：通过（4 条非阻塞建议）
- [建议] "增量游标延续"表述错误：deepest_page_fetched 只写不读（server.py 零消费）；无重复实靠增量"连续 inserted==0 停止"（opencode）/ "命中已有 usg_id 停止翻页"（bai/cc）+ usg_id 主键 upsert。应改为"幂等重拉"。
- [建议] prune_old_records 联动是方向A最大缺口：三条同步路径均按 window_days 裁剪，重登首次同步会再次删除窗口外记录——不做联动处置则数据丢失只是被延迟。
- [建议] account_id 复用边界：workspace_hint 为空时去重失败新建行（main.py:540-543 注释自认），保留数据成孤儿。
- [建议] reloginConfirm 同为死键且含旧删除语义。
- 核实：诊断 §1 行号全部吻合；测试联动定位准确；total_records 由 _refresh_sync_totals 实时 COUNT 派生；P1 定级合理。

### 产品经理：驳回（1 阻塞 + 3 建议）
- [阻塞] 方向A语义分工在现有 UI 无法落地：renderUsersList（app.js:1728）与用户菜单（1686）按 has_token 过滤，登出账号从界面消失、保留数据成不可见孤儿；「删除」按钮仅存在于非活跃行——活跃账号今天唯一的清数据路径就是"退出登录"，方向A把它改掉后活跃账号无任何 UI 清数据路径。要求：放开过滤 + 未登录行提供登录/删除入口 + 验证共用电脑路径。
- [建议] deleteUserConfirm 已含"无法恢复"/"removed permanently"，无需改写。
- [建议] logoutDesc 也是死键，§3"用户读过该文案"表述失实。
- [建议] 方向B补充产品权衡（默认不勾零成本，可与 A 共存）。

### 用户体验官：通过（3 条非阻塞建议）
- [建议] btn-danger/danger:true 与新语义矛盾，plan 须显式决策并记录取舍。
- [建议] 退出后账号行从列表消失与"数据保留"文案认知冲突，建议列表保留未登录行（标注状态）或 toast 提示。
- [建议] 删除账号文案已达标，plan 直接确认即可。
- 核实：三按钮并排/弹窗措辞/死键/重复键/total_records 展示位置/CC 24h 注释全部属实；触发链分析与语义归位符合防错与可恢复直觉。

### 汇总
1 条阻塞（UI 联动缺失）+ 高质量技术修正（幂等重拉/prune 联动/孤儿边界），全部逐条落实为诊断 v2（对照表见诊断文末 Review 记录）。进入第 2 轮。

## 第 2 轮（2026-09-05）：架构师通过 / PM 通过 / UX官驳回（2 阻塞）→ streak=0

### 架构师：通过（3 建议：死键补 reloginConfirmNew、「登录」定向落库边界、遮罩可达性——后两条与 UX 官阻塞同源）
对照表 11 条全部"已落实"，逐条附代码复核（app.js:1686/1728 过滤、幂等重拉三证据、prune 守卫、main.py:539-543 边界）。

### 产品经理：通过（4 建议：活跃账号两步删除路径写明、共用电脑路径升格验收场景、notLoggedIn 现成键复用、prune 时序措辞）
对照表 11 条全部"已落实"；确认 v2 诚实性（主动修正 v1 失实表述）与语义分工在放开过滤后成立。

### 用户体验官：驳回
- [阻塞] 非活跃未登录行「登录」凭证串号：save_token 固定写活跃账号（db.py:483-499 docstring 自认）、/api/relogin 与 open_login 无账号参数、switch 对无 token 行 400"该账号未登录"；clear_account 不移动 active 指针使非活跃未登录行真实可达。
- [阻塞] 最后一个已登录账号退出触发全不透明欢迎遮罩（app.js:1789、style.css:375、index.html:270-281 仅两入口），单账号形态下管理路径不可达；另要求记录 app.js:1788 卡片即时移除与 server.py:1071 overview 过滤口径的去留。
- [建议] ur-ws 硬编码后缀、i18n 收尾清单（goLogin 复用、后端硬编码中文）。
- 对照表：第 1 轮 3 条意见两条✅一条⚠️（方向落实但边界未闭环，即本轮阻塞）。

### 汇总
2 条阻塞均为 UI 联动设计的代码事实缺口，诊断 v3 逐条修订（定向落库 by account_id / 遮罩加「管理本地数据」入口 / 口径记录）。进入第 3 轮。

## 第 3 轮（2026-09-05）：3/3 通过 → streak=1

### 架构师：通过（3 建议 → 固化为交接清单 #1/#2/#6）
定向落库方案三支点核实成立（pending_mode 闭包 dict、add 分支定向落库先例、save_resolved_workspace 的 _resolve_account_id 可选参数先例）；遮罩方案交互闭环核实（/api/accounts 无登录门槛、checkState 仅启动一次不会弹回）。建议：去重边界理由澄清（resolved_workspace_id 与去重无关）、id 传递覆盖两条前端路径与回调契约、行号勘误。

### 产品经理：通过（4 建议 → 交接清单 #3/#4/#5/#6）
定向落库与遮罩入口两项产品评估成立；建议：活跃指针归属决策（推荐切到目标行）、遮罩条件用代码口径、prune 边界集成断言、行号微偏。

### 用户体验官：通过（3 建议 → 交接清单 #4）
两条阻塞均判定"已妥善解决"（代码级核实 pending_mode/save_token 先例、遮罩闭环、边界披露诚实）；建议：遮罩条件口径锁定、管理入口滚动定位 users-list、欢迎页三按钮视觉层级。

### 汇总
全部建议为计划阶段事项，已固化为诊断 §8 计划交接清单（7 项）。进入第 4 轮确认。

## 第 4 轮（2026-09-05）：3/3 通过 → streak=2 → **门禁1 通过**

### 架构师：通过
§8 七项逐一代码核实（含 pending_mode 先于单飞守卫、users-list 锚点存在、三按钮层级与现状吻合）；建议：#1 措辞精度（add_account 亦为 opencode add 路径，定向落库不经由它）、server.py:1138/1139 微偏。

### 产品经理：通过
四条建议完整固化核实；"点谁登谁切活跃"决策产品视角确认成立（switch=True 既有先例、操作对象即意图对象）；建议：备选路径（不切活跃）的 toast 须带账号名。

### 用户体验官：通过
§8 七项独立复核全部吻合；建议：§6 残留旧口径括号加注"以 §8 #4 为准"、遮罩入口键与行级键同文案复用。

### 汇总
**门禁1 终止（4 轮，streak=2）。** 5 条收尾建议带入阶段3 计划。

---

# 门禁2（计划评审）投票记录

## 第 1 轮（2026-09-05）：0/3 三席驳回 → streak=0，已修订计划 v2

### 架构师：驳回（3 阻塞 + 4 建议）
- [阻塞] WindowApi.open_login（main.py:340-348，js_api 主入口）不在改动面——定向落库主路径断链；链路实为 app.js:1767 → main.py:340 → main.py:629。
- [阻塞] 单账号/全部登出形态两步删除不成立（get_active_account_id 全部未登录时维持原选择，db.py:403；按钮组按 isActive 单一维度渲染）。
- [阻塞] 用户菜单放开过滤与"400 不可达"口径自相矛盾。
- [建议] Callable 注解（server.py:765）+ 既有调用点兼容说明；§2.4 测试先例勘误（test_commandcode_sync.py:267-271）；goLogin 残留死键；切活跃时机约束。
- 核实：21 项中 clear_account/save_token/pending_mode/ur-ws/遮罩/欢迎页/锚点/样式等全部定位准确。

### 产品经理：驳回（2 阻塞 + 4 建议）
- [阻塞] 单账号形态退出→删除不可达（与架构师同源）；期望按钮组矩阵（未登录行无论 active 一律 登录+删除）。
- [阻塞] Task 4 把场景验收降格为服务端可达性断言——UI 层断裂无法被验收发现；要求人工 UI 检查单。
- [建议] app.js:1788 表述不可执行（实为 overview 刷新，列表刷新在 1785-1786）；goLogin 一并决策；loginRow 英译风格统一；让位 MIN(id) 时序与串号断言注记。

### 用户体验官：驳回（2 阻塞 + 4 建议）
- [阻塞] 两步删除前提不成立（clear_account 不移动活跃指针 + isActive 单一维度渲染）；采纳 (b) 未登录行渲染删除（删除后 remaining==0 走既有回遮罩链路闭环）。
- [阻塞] 菜单放开过滤制造 400 中文报错新路径（推荐菜单保留过滤）。
- [建议] ur-ws 双重标注歧义（徽标承载状态）；logoutUserConfirm 疑问句式；死键清单补 setLogout/goLogin 且"引用点随改"失实；欢迎页按钮 data-i18n 必须性/.btn 样式/主题变量高亮。
- 核实：save_token/遮罩链路/renderSettings 路径/样式/键位全部与代码吻合。

### 汇总
三席阻塞收敛为 4 个独立缺陷（主入口断链 / 按钮组矩阵 / 菜单过滤口径 / 验收降格），全部逐条落实为计划 v2（对照表见计划文末，19 条全落实）。进入第 2 轮。

## 门禁2 第 2 轮（2026-09-05）：3/3 通过 → streak=2 → **门禁2 通过**

对照表 19 条全部落实（架构师逐条代码核实"21 项引用行号全部吻合"）。

### 架构师：通过（5 条实施精度建议）
1. 透传断言落为 WindowApi 契约级（stub _on_open_login 捕获 (mode, id)）；闭包行为由串号回归端到端覆盖。
2. /api/relogin body 读取必须容忍空 body（欢迎页兜底 POST 无 body 会 _read_json_body 抛 ValueError）→ 失败/缺失按 {"id": None}。
3. pending_mode["account_id"] 无条件覆盖（含 None），防定向 id 残留串号。
4. 目标行登录完成前被删除的静默边界：rowcount==0/set_active False → _mlog 记录。
5. server.py:769 set_login_callback 形参注解一并更新。

### 产品经理：通过（2 条建议）
1. 「登录」按钮先调 startLoginWatch()（短轮询兜底刷新）；检查单⑥加"授权后列表即时刷新"观察点。
2. 删除按钮补注 remaining==0 回遮罩沿用既有链路（app.js:1832）。

### 用户体验官：通过（3 条建议）
1. 未登录行按钮顺序 登录/重命名/删除（危险置末惯例）。
2. 全部登出形态顶栏菜单空态文案并存口径记录（不变可接受）。
3. 检查单①补"顶栏菜单不出现未登录项"。

### 汇总
**门禁2 终止（2 轮）。** 10 条实施建议写入计划 §5 执行备注。

