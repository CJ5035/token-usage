# EVOLUTION-10 投票记录

## 门禁1（诊断评审）

### 第 1 轮（2026-09-06）

- **架构师：通过**。证据逐项核实全部准确（含主动 grep 验证 style.css 全文无 .dsh-stats 溢出规则、test_narrow_layout.py:89 反向断言）。建议 3 条：
  1. [建议] test_narrow_layout.py:89 `count("tbl-scroll") == 1` 反向强断言——容器级方案必须同步修订为结构化断言，否则 CI 阻塞；"顺手补齐契约测试"应升格为"修订+增补"。
  2. [建议] td 级方案对模型列省略效果受限：模型名包在 `.model-cell`（flex）内，td 的 text-overflow 不生效，需同步处理或接受硬裁。
  3. [建议] 行号微偏差：.tbl-scroll 规则实为 style.css:292（非 291）；#dsh-tables 开标签实为 index.html:139。
- **产品经理：驳回**。核实后确认 min_size 归因错误。阻塞意见 1 条 + 建议 5 条：
  1. [阻塞] "拉宽自救失效"归因于 min_size 是事实错误（min_size 只限向小，WINDOW_SIZE=(1280,840) 初始即大于 min_size，pywebview 支持拉大）；默认窗宽实为 min(WINDOW_SIZE, 工作区-60) 逻辑像素；第 1/5/6/7 节口径矛盾（"永远看不到" vs "切宽窗口可见"）。要求修正自救论证、修正触发数值、统一口径；定级可维持 P2 6/10（与 EVOLUTION-8 同族口径），但不得再以"自救失效"为加重项；建议将窗口宽度取值认知修正回写认知库。
  2. [建议] 补临界实验（默认窗宽 + 60 字符中间点）。
  3. [建议] 模型表第一列 m.provider 也是自定义长字符串，触发面补全。
  4. [建议] test_narrow_layout.py:89 断言冲突应点明（与架构师建议 1 同点）。
  5. [建议] 两防线形态的产品交互差异（td 截断 vs 横滚对"查数字"任务的代价）应写入阶段3裁决材料。
  6. [建议] 行号勘误（#dsh-tables 起于 index.html:139）。
- **用户体验官：通过**。独立盘点统计页全部 6 张表防线形态（ZCode/CC=td ellipsis、DSH=裸奔、report=.tbl-scroll、records=table-layout:fixed），与报告吻合。建议 3 条：
  1. [建议] td ellipsis 形态现状是纯静默截断（ZCode/CC 表无 title，对照先例 app.js:1378 session_id 带 title）；若统一 DSH 而不补 title，"看不全且无法恢复全名"缺口从两卡扩散到三卡。
  2. [建议] test_narrow_layout.py:87-91 不仅有 report 断言还有 count==1 防扩大断言，是 EVOLUTION-8"滚动仅限 report 表"的历史决策证据，应纳入形态裁决材料。
  3. [建议] DSH 两表上下相邻共置，各自独立横滚会造成上下表列错位滚动的割裂感（report 单表先例无此问题），建议共用滚动容器或约束一致。

**streak：0（存在驳回票）**

### 第 1 轮修订（主 agent 执行）

意见落实对照表：

| # | 来源 | 意见 | 落实情况 |
|---|---|---|---|
| 1 | PM[阻塞] | min_size 归因错误、默认窗宽数值、口径矛盾 | **已落实**：v2 §5 重写触发条件（min(WINDOW_SIZE,工作区-60) 逻辑像素推算 + 实测校准）；§3.3/§6 自救口径改为"横滚可达+拉大窗口可达，但可发现性极低+全页位移代价"；全文"永远看不到/裁死"表述清除；认知修正（窗口宽度取值范围）已在 §5 数值中体现 |
| 2 | PM[建议] | 补临界实验 | **已落实**：补 40/60/102 字符 × 813~1220px 扫描 + scrollLeft 可达性实验（§3.2/§3.3）；阈值修正为 60~102 字符之间 |
| 3 | PM[建议] | m.provider 补全 | **已落实**：§2.5 补 app.js:1041 模型表第一列 |
| 4 | 架构师[建议]+PM[建议]+UX官[建议] | count==1 断言冲突 | **已落实**：§2.6 契约测试固化现状 + §9 修复方向"必须同步改写"升格 |
| 5 | PM[建议] | 两形态产品交互差异入裁决材料 | **已落实**：§9 形态裁决材料段 |
| 6 | UX官[建议] | title 截断补偿 | **已落实**：§9 td 级注意事项② |
| 7 | UX官[建议] | 两表相邻滚动割裂 | **已落实**：§9 形态裁决材料段 |
| 8 | UX官[建议] | count==1 是历史决策证据 | **已落实**：§2.6/§9 |
| 9 | 架构师[建议] | .model-cell flex ellipsis 失效 | **已落实**：§9 td 级注意事项① |
| 10 | 架构师+PM[建议] | 行号勘误 | **已落实**：v2 全文改用 style.css:292 / index.html:139 |
| 11 | 主 agent 自查 | 修订实验中发现 v1"裁死不可达"定性错误 | **已落实**：#page-stats 可横滚（scrollLeft 0→456 实测）；v2 §3.3 如实修正 |
| 12 | 主 agent 新发现 | 溢出源头不止 DSH 长名：ZCode/CC 表固有列宽窄窗溢出（当前数据即触发，800px 实测 ZCode 末列不可见、页面溢出 299px） | **已落实**：v2 §1/§3.1/§6 问题范围扩展为"三卡六表无表格内滚动容器"，定级论据替换 |

### 第 2 轮（2026-09-06）

- **架构师：通过**。对照表 12 条逐条核实全部落实；两项新实验声明（ZCode 固有列宽溢出、横滚可达但不可发现）均可信。建议 3 条（非阻塞）：
  1. [建议] CC 表"当前数据即触发"为结构推定缺逐表实测（CC 仅 6 列 app.js:1119-1133，窄于 ZCode 8/9 列），建议标注"ZCode 实测实锤、CC 同构推定"。
  2. [建议] 两表相邻滚动割裂提示未随范围扩展同步——应覆盖三卡六表（ZCode 卡 index.html:125-128、CC 卡 150-153 同构）。
  3. [建议] §2.4 应补 `overflow-y: overlay` 废弃值交叉引用（候选清单备选池 #2），防阶段3 动 overflow 链时遗漏依赖。
- **产品经理：通过**。CC 列构成核实（app.js:1074-1080 仅 6 列）为本轮最关键发现。建议 3 条（非阻塞）：
  1. [建议] CC 表触发断言属证据外推（同架构师第 1 条），建议补 CC 实测或降级为"同族推断、待实测"；范围叙事保留"六表均无滚动容器"结构事实，"当前真实数据即触发"限定 ZCode。
  2. [建议] 认知库回写未完成：evolution-log.md:186 旧认知"min_size 是窄窗问题总根因"未修正，是 v1 归因错误的源头，建议增补/改写。
  3. [建议] §3.1 证据标注精度：① right=885 是渠道表末列、969/1073 是模型表列，应标注表名；② 800px 行备注"150% DPI 最小窗"与 §5 口径矛盾（800=125% DPI min_size 下限）。
- **用户体验官：通过**。第 1 轮 3 条建议全部落实且措辞准确。建议 2 条（非阻塞）：
  1. [建议] 容器级方案继承 report 先例的滚动条低可发现性弱点（thumb var(--border) 复制六份）——阶段3 需权衡配套增强可见性或溢出提示；若改 thumb 样式须同步改写 test_narrow_layout.py:109-113 `test_webkit_scrollbar_customization_unchanged` 回归锚（同类 CI 阻塞风险，§9 漏列）。
  2. [建议] §3.1 残余标注矛盾：800px 行备注应为"125% DPI 最小窗"（同 PM 第 3 条②）。

**streak：1（3/3 全票通过一轮；需连续两轮全票达 streak=2）**

### 第 2 轮修订（主 agent 执行，吸收三席通过票建议）

| # | 来源 | 意见 | 落实情况 |
|---|---|---|---|
| 1 | 架构师1+PM1 | CC 表触发断言需实测或降级 | **已落实（实测否定）**：补 CC 两表实测——800/911/1033px 末列均可见（right=773/885/1007），不触发数字列丢失；v3 §3.1/§5/§6 触发面收窄为"ZCode 两表（实测）+ DSH 长名（实验）"，CC 改为"结构事实成立（无滚动容器）但不触发数字列丢失" |
| 2 | 架构师2 | 割裂提示扩展三卡 | **已落实**：v3 §9 列出三卡六表位置（含 ZCode/CC 卡 index.html:125-128/150-153） |
| 3 | 架构师3 | §2.4 overlay 交叉引用 | **已落实**：v3 §2.4 补备选池 #2 交叉引用 |
| 4 | PM2 | 认知库回写 | **已落实**：evolution-log.md 第三轮认知 1 已改写（初始窗宽公式 + min_size 仅约束下限 + 自救路径定性修正） |
| 5 | PM3 | §3.1 标注精度（表名区分 + 800px 备注） | **已落实**：v3 §3.1 表格区分渠道表/模型表列；备注改为"125% DPI min_size 下限（1000 物理像素 ÷ 1.25）" |
| 6 | UX官1 | 滚动条可见性权衡 + :108-113 回归锚 | **已落实**：v3 §9 补容器级方案弱点说明 + test_webkit_scrollbar_customization_unchanged 回归锚提示（行号核实为 108-113，函数定义起 :108） |
| 7 | UX官2 | 800px 备注矛盾 | **已落实**：同 #5 |
| 8 | UX官（第1轮遗留 ±1 行号） | app.js:916→915 | **已落实**：v3 §9 ②改为 app.js:904/915（核实模型表 td 起于 :914-915） |

### 第 3 轮（2026-09-06）

- **架构师：通过**。对照表 8 条核实：7 已落实、#1 升格落实（直接补实测且数据自洽）。建议 3 条：
  1. [建议] "CC 末列 800px 超卡片 66px 对页面溢出有小额贡献"不成立——页面滚动宽度由最宽元素决定（299=1099−800，源头唯一），CC 不产生页面滚动宽度。
  2. [建议] §9 "均受 style.css:469 .tbl+.tbl 同构规则作用"对 DSH 不成立（:469 是 .zcode-stats 前缀规则，DSH 间距来自 :293 通用规则）。
  3. [建议] §9 ② 补 CC 表无 title 行号（app.js:1119/1128）。
- **产品经理：通过**。建议 4 条：
  1. [建议] §5"数据条件"行残留 v2 旧口径（ZCode/CC 并列实锤），与同节上一条自相矛盾。
  2. [建议] §7 首句同源残留（ZCode/CC/DSH 并列不可见）。
  3. [建议] 第 2 轮对照表 #1 应改记"部分落实"（核心落实 + 两处叙述残留）。
  4. [建议] 行号微偏差：模型表头延伸至 app.js:864（855-862→854-864）；CC 至 :1081（1074-1080→1073-1081）。
- **用户体验官：驳回**。阻塞 2 条 + 建议 2 条：
  1. [阻塞] §7 第一条与 §3.1 实测直接矛盾（CC 列入不可见清单 + DSH 无条件列入 + 列名混合举例）；期望与"ZCode 两表 + DSH 长名"口径一致，可顺手补 DSH 无长名末列实测。
  2. [阻塞] §5"数据条件"行残留 CC，与同节上一行自相矛盾。
  3. [建议] §6 论据①概括过宽：1033px 仅模型表触发、渠道表 851px 才触发，应精确化防止阶段3 误读验收口径。
  4. [建议] §9 :469 规则作用域对 DSH 不成立（同架构师第 2 条）。

**streak：0（UX官驳回票；驳回点为 v2 残句未同步的文内矛盾，属实）**

### 第 3 轮修订（主 agent 执行，产出 v4）

| # | 来源 | 意见 | 落实情况 |
|---|---|---|---|
| 1 | UX官[阻塞2]+PM[建议1] | §5"数据条件"行残留 CC | **已落实**：v4 §5 重写——"ZCode 两表与 DSH 模型表……当前真实数据即触发（实测实锤）；CC 两表与 DSH 渠道表当前数据不触发（实测）" |
| 2 | UX官[阻塞1]+PM[建议2] | §7 首句残留 | **已落实**：v4 §7 第一条改写为"ZCode 两表与 DSH 模型表末列不可见（实测+列名对应）"；CC 单独次级条目（不触发、仅结构同源）；DSH 长名保留独立条 |
| 3 | UX官[建议] | 补 DSH 无长名末列实测 | **已落实（并发现新触发面）**：DSH 模型表 851px 起末列不可见（right=931）——当前数据即触发；DSH 渠道表不触发。v4 §3.1 表格扩为六表三宽度矩阵，§5/§6/§7/§10 同步 |
| 4 | UX官[建议3] | §6 论据①精确化 | **已落实**：v4 §6 ①"模型表 1033px 起、渠道表与 DSH 模型表 851px 起（均实测），@125% 仅模型表触发、@150% 三表全触发" |
| 5 | 架构师[建议1] | CC 页面溢出贡献表述错误 | **已落实**：v4 §3.1 注与头部修订说明 1 改为"页面滚动宽度由最宽溢出元素决定，CC 不产生页面滚动宽度" |
| 6 | 架构师[建议2]+UX官[建议4] | :469 规则作用域 | **已落实**：v4 §9 改为"ZCode/CC 受 :469 前缀规则作用，DSH 卡间距来自 :293 通用规则" |
| 7 | 架构师[建议3] | 补 CC title 行号 | **已落实**：v4 §9 ② 补 app.js:1119/1128 |
| 8 | PM[建议4] | 表头行号校正 | **已落实**：v4 全文 854-864 / 1073-1081 |
| 9 | PM[建议3] | 第 2 轮对照表 #1 改记 | **已落实**：本表第 3 轮节将第 2 轮 #1 的最终状态如实呈现（第 2 轮记录保持历史原样，残句问题在第 3 轮修订闭环） |

### 第 4 轮（2026-09-06）

- **架构师：通过**。对照表 9 条全部落实；DSH 模型表新实测数据经机制推演自洽（min-content 恒定 931 / 拉伸态 right=视口−26 / 临界视口推算 930 落在 851 与 1033 之间）。建议 4 条（非阻塞）：
  1. [建议] §7 首条未按 DPI 分档（@125% 仅模型表触发、@150% 三表全触发），建议补分档限定。
  2. [建议] §5 窗宽公式行号偏差：计算行实为 app/main.py:488-489（491-493 是 create_window 参数行）。
  3. [建议] §9 ② CC 无 title td 行号可精化（1119/1128 为渲染起点行）。
  4. [建议] §5"数据条件"行长名加剧仅归 DSH 渠道表——模型表第一/二列同为无 td 防线自定义字段，应改"DSH 两表"；§3.2 未标明实验对象表名。
- **产品经理：通过**。对照表 9 条全部落实；实测数值与 CSS 机制精确自洽（min-content 撑破态/width:100% 跟随态两模式互证），"伪数据不可能呈现这种规律"，新增证据判定可信。建议 2 条（非阻塞）：
  1. [建议] §7 DSH 模型表末列名微瑕：实际 UI 文案为"平均 tok/s"（dshColTps，app.js:106），非 ZCode 的"平均输出速度"（zcodeAvgTps，app.js:96）——同语义不同文案，验收对照 UI 时会困惑。
  2. [建议] 阶段3 验收清单应将"DSH 模型表 851px 末列恢复可见"列为必测项，防止按旧口径漏验。
- **用户体验官：驳回**。阻塞 1 条 + 建议 2 条：
  1. [阻塞] §7 DSH 模型表末列错标"平均输出速度"——UI 实际文案"平均 tok/s"（app.js:106/998），按此报告阶段3 在 DSH 表头找不到"平均输出速度"列；建议拆分标注各表列名。
  2. [建议] §3.1 注"卡片内裁切观感"与 §7"仍在视口内可见"不一致（.card 无 overflow 默认不裁切）。
  3. [建议] §7 首句未分 DPI 档（同架构师第 1 条）。
  - 驳回声明："本轮驳回仅因一处列名与实际 UI 文案不符，修正量一句话，修订后我票面无遗留障碍。"

**streak：0（UX官驳回票；驳回点为列名标注精度，其在驳回中明示修订后无遗留障碍）**

### 第 4 轮修订（主 agent 执行，产出 v5）

| # | 来源 | 意见 | 落实情况 |
|---|---|---|---|
| 1 | UX官[阻塞1]+PM[建议1] | DSH 模型表末列名错标 | **已落实**：v5 §7 拆分标注——ZCode 渠道表末列"平均首字延迟"、ZCode 模型表末列"平均输出速度"、DSH 模型表末列"平均 tok/s"（app.js:106 dshColTps 已核实） |
| 2 | UX官[建议3]+架构师[建议1] | §7 未分 DPI 档 | **已落实**：v5 §7 首句补"@125% DPI 仅 ZCode 模型表触发、@150% DPI 三表全触发（见 §6①）" |
| 3 | UX官[建议2] | "卡片内裁切观感"措辞 | **已落实**：v5 §3.1 注改为"溢出卡片边缘但仍在视口内可见——.card 无 overflow 声明默认不裁切" |
| 4 | 架构师[建议2] | main.py 行号 | **已落实**：v5 §5 改 app/main.py:488-489（已核实 win_w/win_h 计算行） |
| 5 | 架构师[建议4] | 长名加剧归"DSH 两表" + §3.2 标实验对象 | **已落实**：v5 §5 改"DSH 两表的长名加剧（模型表第一/二列同为无 td 防线自定义字段）"；§3.2 标注"对象：DSH 渠道表首列" |
| 6 | 架构师[建议3] | CC td 行号精化 | **已落实**：v5 §9 ② 改 app.js:1120/1129（td 实际行，已核实） |
| 7 | PM[建议2] | DSH 模型表列入阶段3 必测项 | **已记录**：转入阶段3 计划的验收清单要求（诊断报告 §9 修复方向已含六表统一包裹，计划阶段将把三张触发表逐一列入验收点） |

### 第 5 轮（2026-09-06）

- **架构师：通过**。对照表 7 条全部落实；v5 修订点逐一对照代码与全文口径，未发现新矛盾。建议 2 条（流程提醒）：① 阶段3 计划勿丢失"三张触发表逐一列为验收点"（对照表 #7 转办项）；② §7 括注与 §6① 分档口径日后同步维护。
- **产品经理：通过**。对照表 7 条全部落实（#7 合理归置认可）。建议 2 条：① 验收清单将三张触发表逐一列验收点、以 §3.1 实测 right 值（885/1073/931）作量化回归基线，防"目测可见"口径漂移与漏验 ZCode 两表；② 阶段3 计划产出时回查对照表 #7 承诺。
- **用户体验官：通过**。阻塞闭环确认（列名拆分经 i18n key + 表头行逐一核实）；对照表 7 条全部落实。建议 1 条：阶段3 计划落地三张触发表验收点，防跨阶段交接遗漏（同 PM/架构师提示）。

**streak：2 → 门禁1 通过（第 4、5 轮连续 3/3 全票）**

**门禁1 总成本**：5 轮 × 3 席 = 15 席次。轮次分布：R1 架构师✓/PM✗/UX官✓（min_size 归因错误）、R2 3/3✓（CC 触发断言质疑）、R3 架构师✓/PM✓/UX官✗（v2 残句未同步）、R4 架构师✓/PM✓/UX官✗（列名精度）、R5 3/3✓。

**对阶段3 的交接要求（三席一致）**：
1. 契约测试双改写：test_narrow_layout.py:88-92 count==1 反向断言 + :108-113 滚动条回归锚（若动 thumb 样式）。
2. 验收清单：三张触发表（ZCode 渠道表 851px / ZCode 模型表 1033px / DSH 模型表 851px）"末列恢复可见"逐一列为验收点，以 §3.1 实测 right 值（885/1073/931）为量化回归基线。
3. 形态裁决材料齐备（诊断报告 §9）：td ellipsis vs .tbl-scroll 交互代价、三卡两表相邻共置（共用滚动容器或行为一致）、滚动条可见性权衡、td 截断的 title 补偿与 .model-cell flex 处理。

---

## 门禁2（计划评审）——EVOLUTION-10

### 第 1 轮（2026-09-06）

- **架构师：驳回**。阻塞 1 条 + 建议 5 条：
  1. [阻塞] 计划遗漏第二条 count==1 契约断言——tests/test_i18n_consistency.py:164-165 `test_tbl_scroll_still_wraps_only_channel_table` 与 test_narrow_layout.py:89 互为副本，改后 count==4 必挂 CI；要求 §3.3 增补同步改写，执行前 `grep -rn "tbl-scroll" tests/` 全量盘点自证。
  2. [建议] §2.1"零 JS 改动"论据失实（app.js:872/1089 引用容器做 hidden 显隐切换）；结论碰巧成立，建议修正表述。
  3. [建议] §4.2 DSH 长名验收"~1099px 量级"张冠李戴（1099 是 ZCode 模型表固有宽；实验对象 DSH 渠道表固有宽 ~750-780px）。
  4. [建议] CC 卡加容器后行为变化未列入验收。
  5. [建议] 测试改写同步 docstring（test_narrow_layout.py:9-11）与函数名（exactly_once）；.probe/serve_ui.py 建议注明路径。
- **产品经理：驳回**。阻塞 1 条 + 建议 2 条：
  1. [阻塞] CC 两表行为回退（零操作可见 → 横滚 ~66px）无显式裁决、验收无 CC 判据——走查阶段无法区分"预期变化"与"回归"；要求 §2 补裁决小节 + §4.2 补 CC 验收行。
  2. [建议] DSH 模型表第一/二列长名注入走查补齐（.model-cell 硬裁路径应以走查证实而非默认成立）。
  3. [建议] title 补偿候补池排期时对"DSH 新增 + ZCode/CC 既有"缺口给明确优先级。
- **用户体验官：驳回**。阻塞 2 条 + 建议 2 条：
  1. [阻塞] CC 行为回退无裁决无验收（同 PM）。
  2. [阻塞] DSH 长名用户全名可达性从"低可发现但存在"恶化为"不存在"，且 model-cell（flex）硬裁无省略号提示；给出两选项：(a) 补 title（推荐，先例 app.js:1378/:738/:1476，可只补 DSH 新增截断）；(b) 坚持零 JS 须补正式裁决记录并修正 §1 措辞。
  3. [建议] 851px 三容器滚动条同屏形态走查实证。
  4. [建议] DSH 模型表注入路径补 scrollWidth 收敛 + 视觉确认（同 PM 建议 2）。
  - 补充说明：方案主体方向无异议，驳回仅针对"修复引入的新可用性问题未裁决、未验收"。

**streak：0（三席均驳回；问题集中于计划完备性：漏一处断言改写 + CC 回退未裁决 + DSH title 缺口）**

### 第 1 轮修订（主 agent 执行，产出计划 v2）

| # | 来源 | 意见 | 落实情况 |
|---|---|---|---|
| 1 | 架构师[阻塞] | 第二处 count==1 断言遗漏 | **已落实**：v2 §3.4 增补 test_i18n_consistency.py:164-165 改写（count==4 + 分工说明）+ :17 docstring 同步 + §3.4/§6 执行前 grep 盘点自证 |
| 2 | PM[阻塞]+UX官[阻塞1] | CC 行为回退未裁决未验收 | **已落实**：v2 新增 §2.4 裁决记录（四条接受理由）+ §4.2 补 CC 两行验收（800/851px 回退预期 + 1280px 宽窗回归） |
| 3 | UX官[阻塞2] | DSH 截断全名不可达 + model-cell 硬裁 | **已落实（采纳方案 a）**：v2 新增 §2.3 辅方案 B——DSH 渲染模板 3 处补 title（app.js:1033/1041/1042，对齐 ：1476 先例）；§3.3 改动清单 + §4.2 title 确认项；放弃"零 JS"表述改"纯模板字符串改动（零逻辑变更）"（§2.1/§5） |
| 4 | 架构师[建议2] | "零 JS"论据失实 | **已落实**：v2 §2.1 修正（app.js:872/1089 hidden 切换引用如实列出，结论改为"零逻辑影响"） |
| 5 | 架构师[建议3] | DSH 长名验收数值张冠李戴 | **已落实**：v2 §4.2 改"DSH 渠道表固有列宽（~770px 量级）" |
| 6 | 架构师[建议5]+PM/UX官 | docstring/函数名同步 | **已落实**：v2 §3.4 明确模块 docstring（:9-11）与函数名（去 exactly_once）同步 |
| 7 | PM[建议2]+UX官[建议4] | DSH 模型表注入走查 | **已落实**：v2 §4.2 新增模型表 provider + model-cell 注入验收行 |
| 8 | UX官[建议3] | 滚动条同屏形态实证 | **已落实**：v2 §4.2 新增 851px 三容器滚动条同屏截图验收行（§2.5 亦引用） |
| 9 | 架构师[建议4] | CC 验收 | **已落实**：同 #2 |
| 10 | PM[建议3] | title 候补池优先级 | **已落实**：v2 §7 候补池范围收敛为"ZCode/CC 既有缺口"（DSH 新增截断本轮闭环） |
| 11 | UX官[阻塞2]附注 | §1 范围表补 dsh-today-note | **已落实**：v2 §1 范围表 DSH 行补注 |
| 12 | 架构师附注 | serve_ui.py 路径 | **已落实**：v2 §4.2 标注 `.probe/serve_ui.py` |

### 第 2 轮（2026-09-06）

- **架构师：通过**。对照表 12 条全部落实；专项核查通过（escapeHtml 转义 `"` 安全、title 放 span 层正确、插入位置逐一对应、对既有 i18n title 断言无副作用、count==4 无子串误匹配、459 基线吻合、门禁1 三条交接要求闭环）。建议 2 条（非阻塞）：① i18n 测试函数名 `test_tbl_scroll_still_wraps_only_channel_table` 与 count==4 语义脱节，改名顺手项；② `test_webkit_scrollbar_customization_unchanged` 实际 def 在 :109，执行按函数名检索勿按行号。
- **产品经理：通过**。对照表 12 条全部落实（本人复核源码二次确认：两处 count==1、hidden 切换、docstring 均属实）。§2.4 是本轮最关键新增——回退从隐性副作用升格为显式产品裁决，代价收益相称。建议 3 条（非阻塞）：① §1"见 §2.2"应为 §2.3（笔误）；② CC 851px 档未实测，走查补记实际横滚量；③ title 空值 `|| ""` 与 td 文案 `|| "—"` 差异执行时注明。
- **用户体验官：通过**。对照表 12 条全部落实；专项核查：title 三处位置准确、model-cell title 放 span 层正确（td text-overflow 对 flex 无效）、:1476 先例对齐、范围收敛不扩散、"未发现新引入的体验问题"。"本轮票面无遗留障碍，同意通过。"建议 2 条（非阻塞）：① 空值行悬浮无 tooltip 属正确行为，走查顺带确认；② span title 与 img title 并存内容一致无冲突，走查确认两区域 tooltip 均可达。

**streak：1（3/3 全票一轮；需连续两轮全票达 streak=2）**

### 第 3 轮（2026-09-06）

- **架构师：通过**。关键行号抽查全部一致，无新引入问题。建议 3 条（非阻塞，转执行阶段）：① i18n 断言消息文本"应仅出现一次"须与 count==4 同步改（防注释断言脱节同款风险）；② i18n 测试函数名改名与 narrow_layout 新命名风格对齐（Task 2 顺手项）；③ PM 建议③与 UX官 2 条走查确认项执行时留意。
- **产品经理：通过**。新发现 2 处同源交叉引用笔误（§2.1"§2.2"应为 §2.3、§1 滚动条处"§2.3"应为 §2.5），均不阻塞；"§2.4 是本轮最关键的新增……计划具备进入执行的条件"（第 2 轮结语维持）。
- **用户体验官：通过**。同发现两处引用残留（与 PM 一致），建议级不阻塞；"本轮票面无遗留障碍"（第 2 轮结语）维持。

**streak：2 → 门禁2 通过（第 2、3 轮连续 3/3 全票）**

**门禁2 总成本**：3 轮 × 3 席 = 9 席次。轮次分布：R1 三席均驳（漏一处断言改写 + CC 回退未裁决 + DSH title 缺口）、R2 3/3✓、R3 3/3✓。

**执行阶段备忘（评委非阻塞建议汇收，Task 2 / 走查时落实）**：
1. test_i18n_consistency.py 改写时：断言消息文本同步（"应仅出现一次"→4 口径）+ 函数名改名（对齐 narrow_layout 新命名风格）。
2. test_webkit_scrollbar_customization_unchanged 按函数名检索（def 实际在 :109）。
3. title 空值 `|| ""` 与 td 文案 `|| "—"` 差异在代码注释或走查记录注明；空值行悬浮无 tooltip 属正确行为。
4. CC 851px 档走查补记实际横滚量（~66px 为估算值，以实测为准回填 §2.4④）。
5. 走查确认：span title 与 img title 两个悬浮区域 tooltip 均可达；851px 亮/暗三容器滚动条同屏截图留档 `.probe/ui-shots-v4/`。
