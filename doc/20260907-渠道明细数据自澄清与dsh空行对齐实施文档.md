# 渠道明细「数据自」文案澄清 与 dsh 空 0 行对齐 实施文档

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除渠道明细「数据自」列的语义误读（列名改为「数据起点」+ tooltip 说明），并把 dsh「已接入但今日零用量仍显示全 0 行」对齐到全表「当前范围无数据不出行」规则，另补表尾无用量渠道提示。

**Architecture:** 纯展示层小改：后端 `server.py` 一处门控条件（dsh 明细行追加改为今日用量 > 0 才追加，渠道 tab 保留）；前端 `index.html` 列头文案 + `app.js` I18N 双语键与 renderChannelTable 表尾提示。不改任何聚合 SQL 与数据口径。

**Tech Stack:** Python 3.12 (sqlite 直查) / 原生 JS (Chart.js 页面) / pytest（源码级静态契约测试）。

**Spec:** `doc/bug-diagnosis-today-trend-20260907.md`（诊断结论：数据正确、文案歧义）+ 本轮讨论决定（不显示空渠道维持现状；dsh 例外对齐；表尾提示补"渠道存在感"）。

## Global Constraints

- 禁止自动签入：任何 `git commit` 必须在用户明确确认后执行（仅 Task 4 有签入步骤）；`doc/` 下文档一律不签入。
- Python 统一用 `python`（不可用时 `D:\.pyenv\pyenv-win\versions\3.12.10\python.exe`）；禁止 `py` / `py -3.12` / 裸 `pip`。
- JS 改动的"编译通过"= `node --check app/web/app.js` + 相关 pytest 通过；Python 改动 = `python -m py_compile` + 相关 pytest 通过。
- I18N 键必须 zh/en 双语成对新增，禁止硬编码 tooltip `title=`（仓库契约：`data-i18n-title` + I18N 键）。
- 外科手术式修改：只动本计划列出的行，不顺手改格式/注释/邻近代码。
- 现有契约测试 `tests/test_i18n_consistency.py` 断言 `data-i18n-title=` 恰 13 处——Task 2 加 1 处后必须同步把两处 `== 13` 改 `== 14`（见 Task 2 步骤），否则回归挂。

---

### Task 1: 后端 — dsh 今日零用量不出明细行

**Files:**
- Modify: `app/server.py:1285-1303`（`_report_channels_response`）
- Test: `tests/test_report_api.py`（文件末尾追加 1 个测试）

**Interfaces:**
- Consumes: `db.report_channels(range_)`、`db.list_channel_summary()`、`dsh_api.get_dsh_usage()`（均不改签名）。
- Produces: `_report_channels_response("today")` 返回 `{"rows": [...], "summary": [...]}`——当 dsh `found` 且今日 `input+output+reasoning == 0` 时，`rows` 不含 `channel=="dsh"` 行，`summary` 仍含 `dsh`（渠道 tab 存在性不变，调用方 `app.js loadReportAll` 无需改动）。

- [ ] **Step 1: 写失败测试**

在 `tests/test_report_api.py` 末尾（`test_server_merge_dsh` 之后）追加：

```python
def test_server_merge_dsh_zero_today_no_row(tmp_report_db, monkeypatch):
    """dsh 已接入但今日零用量: 明细行不出 (与「当前范围无数据不出行」对齐); 渠道 tab 保留."""
    _seed_channels()
    _mock_dsh(monkeypatch, today_tokens=0)
    from app import server
    resp = server._report_channels_response("today")
    assert not any(r["channel"] == "dsh" for r in resp["rows"])
    assert any(s["channel"] == "dsh" for s in resp["summary"])
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_report_api.py::test_server_merge_dsh_zero_today_no_row -v`
Expected: FAIL——`assert not any(...)` 处挂（当前 found 即出行，`rows` 里有全 0 的 dsh 行）。

- [ ] **Step 3: 最小实现**

`app/server.py` `_report_channels_response`（现 1290-1302 行）把 `rows` 追加包进用量门控，`summary` 追加保持在门控外：

```python
    if range_ == "today":
        dsh = dsh_api.get_dsh_usage()
        if dsh.get("found"):
            t = dsh.get("today") or {}
            # 明细行与 db.report_channels「当前范围无数据不出行」对齐; summary 保留 (渠道 tab 接入可见性)
            if (t.get("input") or 0) + (t.get("output") or 0) + (t.get("reasoning") or 0) > 0:
                rows = rows + [{"channel": "dsh", "tokens": (t.get("input") or 0) + (t.get("output") or 0)
                                + (t.get("reasoning") or 0),
                                "input": t.get("input") or 0, "output": t.get("output") or 0,
                                "cache_read": t.get("cache") or 0, "cache_write": 0,
                                "reasoning": t.get("reasoning") or 0, "requests": 0, "cost": 0.0,
                                "cost_available": False, "cost_partial": False,
                                "request_count_exact": False,
                                "data_since": None, "estimated": False}]
            summary = summary + [{"channel": "dsh", "accounts": 0}]
```

（`rows = rows + [...]` 内的 dict 与现文件逐字段一致，只是整体缩进进 `if`；不要动其他字段。）

- [ ] **Step 4: 跑测试确认通过 + 编译**

Run: `python -m py_compile app/server.py && python -m pytest tests/test_report_api.py -v`
Expected: 新测试 PASS；现有 `test_server_merge_dsh`（today_tokens=70）仍 PASS（70>0 门控放行）；本文件其余测试无回归。

---

### Task 2: 前端 — 「数据自」列改名「数据起点」+ tooltip

**Files:**
- Modify: `app/web/index.html:77`（渠道明细表头 th）
- Modify: `app/web/app.js:123`（zh I18N）、`app/web/app.js:242`（en I18N）
- Modify: `tests/test_i18n_consistency.py:72-73`、`:130`（data-i18n-title 总数契约 13→14）、`:1-18`（docstring 计数）
- Test: `tests/test_codex_ui_contract.py`（追加 1 个测试）

**Interfaces:**
- Consumes: 现有 `dataSince` I18N 键（`index.html` th 的 `data-i18n`、`app.js:2574` 汇总条 `t("dataSince")`——改键值后两处自动生效，无需改 JS 逻辑）。
- Produces: 新 I18N 键 `dataSinceTip`（zh/en），被 `index.html` 渠道明细 th 的 `data-i18n-title` 引用；`syncI18nTitles` 现有机制自动随语言切换刷新，无需新增 JS。

- [ ] **Step 1: 写失败测试**

在 `tests/test_codex_ui_contract.py` 末尾追加：

```python
def test_data_since_column_copy_clarified():
    """「数据自」歧义修复: 列头改「数据起点」并带 data-i18n-title tooltip (诊断 doc/bug-diagnosis-today-trend-20260907.md)."""
    html = (ROOT / "app/web/index.html").read_text(encoding="utf-8")
    js = (ROOT / "app/web/app.js").read_text(encoding="utf-8")
    assert 'data-i18n="dataSince"' in html
    assert 'data-i18n-title="dataSinceTip">数据起点</th>' in html
    assert 'dataSince: "数据起点"' in js
    assert 'dataSince: "Data start"' in js
    assert 'dataSinceTip: "该渠道本地最早记录日期（历史覆盖起点），不随上方时间范围变化；「仅今日」表示该渠道无历史数据"' in js
    assert 'dataSinceTip: "Earliest local record date of this channel (history coverage start), unaffected by the range selector; \'Today only\' means the channel has no history yet"' in js
```

（第 2 条断言锚定的是**改造后完整 th 尾部** `data-i18n-title="dataSinceTip">数据起点</th>`——
`data-i18n="dataSince"` 与 `>` 之间隔着新加的 `data-i18n-title`，不能用 `data-i18n="dataSince">数据起点` 作子串。）

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_codex_ui_contract.py::test_data_since_column_copy_clarified -v`
Expected: FAIL——挂在第 2 条断言 `data-i18n-title="dataSinceTip">数据起点</th>`（当前列头是「数据自」且无 tooltip；第 1 条 `data-i18n="dataSince"` 改造前就存在，不挂）。

- [ ] **Step 3: 改 index.html 列头（第 77 行）**

把 渠道明细 表头的：

```html
<th data-i18n="dataSince">数据自</th>
```

改为：

```html
<th data-i18n="dataSince" data-i18n-title="dataSinceTip">数据起点</th>
```

- [ ] **Step 4: 改 app.js I18N 双语键**

zh（第 123 行）：

```javascript
    dataSinceToday: "仅今日", dataSince: "数据起点",
    dataSinceTip: "该渠道本地最早记录日期（历史覆盖起点），不随上方时间范围变化；「仅今日」表示该渠道无历史数据",
```

en（第 242 行）：

```javascript
    dataSinceToday: "Today only", dataSince: "Data start",
    dataSinceTip: "Earliest local record date of this channel (history coverage start), unaffected by the range selector; 'Today only' means the channel has no history yet",
```

（`dataSinceToday` 键名与值都不动——dsh 单元格「仅今日」在新列头下语义自洽。）

- [ ] **Step 5: 同步 i18n-title 总数契约 13→14**

`tests/test_i18n_consistency.py`：
- 第 72-73 行测试改名为 `test_data_i18n_title_count_is_14`，断言 `== 14`；
- 第 130 行 `assert len(referenced) == 13` → `== 14`（提示文案里的"应为 13 个"同步改"应为 14 个"）；
- 文件头 docstring（第 5-15 行）中 `data-i18n-title= 恰 13 处` 与 `实际引用的 13 键` 两处计数描述同步改 14。

- [ ] **Step 6: 跑测试确认通过 + JS 语法检查**

Run: `node --check app/web/app.js && python -m pytest tests/test_codex_ui_contract.py tests/test_i18n_consistency.py -v`
Expected: 全部 PASS（`test_i18n_contract_keys_in_both_langs` 自动把 `dataSinceTip` 纳入双语断言；`test_sync_i18n_titles_mechanism_exists` 等既有锚不变）。

- [ ] **Step 7: 人工抽查（UI 层验证）**

Run: `python entry.py`（或直接跑 GoGauge.exe 开发库副本）
Expected: 渠道明细列头显示「数据起点」，鼠标悬停列头出现双语 tooltip；顶栏汇总条标注行同步显示「数据起点 2026-07-29」样式文案（同一键自动更新）。

---

### Task 3: 前端 — 表尾「本范围无用量渠道」提示

**Files:**
- Modify: `app/web/app.js:2717-2727`（`renderChannelTable`）与 `:2474`（调用点）
- Modify: `app/web/app.js` zh I18N（Task 2 改后的 zh 块内追加）、en I18N（同）
- Test: `tests/test_codex_ui_contract.py`（扩展 Task 2 新增的测试函数）

**Interfaces:**
- Consumes: `/api/report/channels` 响应 `{rows, summary}`（`summary` 来自 `db.list_channel_summary()` + dsh(today found)，已按范围感知，前端已拿到但此前未用）。
- Produces: `renderChannelTable(rows, summary)` 新签名（仅此一处调用）；表尾多一行 `class="empty-cell"` 提示，复用现有样式不加 CSS。

- [ ] **Step 1: 写失败测试**

在 `tests/test_codex_ui_contract.py` 的 `test_data_since_column_copy_clarified` 末尾追加两行断言（同函数内）：

```python
    assert 'unusedChannelsHint: "本范围无用量渠道：{chs}"' in js
    assert 'unusedChannelsHint: "No usage in this range: {chs}"' in js
    assert "renderChannelTable(rows.rows, rows.summary)" in js
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_codex_ui_contract.py::test_data_since_column_copy_clarified -v`
Expected: FAIL（三个断言均未命中）。

- [ ] **Step 3: 改 renderChannelTable（2717-2727 行）与调用点（2474 行）**

调用点（`loadReportAll` 内）：

```javascript
    renderChannelTable(rows.rows, rows.summary);
```

函数体整体替换为（2722-2726 行模板逐字保留，仅加 `summary` 参数、`missing` 计算与表尾拼接）：

```javascript
function renderChannelTable(rows, summary) {
  if (!rows.length) {
    $("report-table").innerHTML = `<tr><td colspan="8" class="empty-cell">${t("reportEmpty")}</td></tr>`;
    return;
  }
  const rowChs = new Set(rows.map((r) => r.channel));
  const missing = (summary || []).map((s) => s.channel).filter((ch) => !rowChs.has(ch));
  const foot = missing.length
    ? `<tr><td colspan="8" class="empty-cell">${t("unusedChannelsHint").replace("{chs}", missing.map((ch) => CH_LABEL[ch] || ch).join(", "))}</td></tr>`
    : "";
  $("report-table").innerHTML = rows.map((r) => `<tr>
    <td style="color:${CH_COLOR[r.channel] || "#4f8ef7"}">${escapeHtml(CH_LABEL[r.channel] || r.channel)}${r.estimated ? ` <span class="est-badge" title="${t("estimateTip")}">${t("estimateBadge")}</span>` : ""}</td>
    <td class="num">${fmtTokens(r.tokens)}</td><td class="num">${fmtTokens(r.input)}</td><td class="num">${fmtTokens(r.output)}</td>
    <td class="num">${fmtTokens(r.cache_read)}</td><td class="num">${fmtInt(r.requests)}</td><td class="num">${fmtOptionalMoney(r.cost)}</td>
    <td>${r.channel === "dsh" ? t("dataSinceToday") : (r.data_since || "—")}</td></tr>`).join("") + foot;   // R6: dsh 仅今日; T5: 渠道名显示名映射+转义, 费用 NULL → —
}
```

- [ ] **Step 4: 加 I18N 双语键**

zh 块（紧挨 Task 2 的 `dataSinceTip` 行后追加即可）：

```javascript
    unusedChannelsHint: "本范围无用量渠道：{chs}",
```

en 块：

```javascript
    unusedChannelsHint: "No usage in this range: {chs}",
```

- [ ] **Step 5: 跑测试确认通过 + JS 语法检查**

Run: `node --check app/web/app.js && python -m pytest tests/test_codex_ui_contract.py tests/test_i18n_consistency.py tests/test_report_api.py -v`
Expected: 全部 PASS；`data-i18n-title` 总数契约不受影响（表尾提示走 `t()`，不新增 `data-i18n-title` 属性）。

- [ ] **Step 6: 人工抽查（UI 层验证）**

Run: `python entry.py`，范围切「今天」，观察某天没用过的渠道（如 Codex 停用时）：
Expected: 明细无 Codex 行；表尾出现「本范围无用量渠道：Codex」灰字提示；全渠道都有用量时表尾无此行。

---

### Task 4: 全量回归 + 签入（人工确认后才执行）

**Files:** 无新增改动。

- [ ] **Step 1: 全量测试回归**

Run: `python -m pytest tests/ -v`
Expected: 全部 PASS（对照 git status 基线：本仓库工作区已有未提交改动，若有与本计划无关的既有失败，先向用户报备再继续，不擅自修）。

- [ ] **Step 2: 汇报待签入清单，等待用户确认**

列出改动文件（共 6 个：`app/server.py`、`app/web/index.html`、`app/web/app.js`、`tests/test_report_api.py`、`tests/test_codex_ui_contract.py`、`tests/test_i18n_consistency.py`），**不执行 git 操作**。

- [ ] **Step 3: 用户确认后分三笔签入（`doc/` 不签入）**

```bash
git add app/server.py tests/test_report_api.py
git commit -m "fix: skip zero-usage dsh row in today channel table"

git add app/web/index.html app/web/app.js tests/test_i18n_consistency.py tests/test_codex_ui_contract.py
git commit -m "feat: clarify data-since column copy with tooltip and unused-channel hint"
```

（若用户希望文案与提示分两笔，拆分方式：Task 2 的 4 个文件一笔 `feat: clarify data-since column copy with tooltip`，Task 3 的 app.js 一笔 `feat: append unused-channel hint footer to channel table`——因两者同文件，默认合为一笔。）

---

## Self-Review 记录

**Review 第 1 轮修正记录（2026-09-08）**：F1 Task 2 断言与实现标记自相矛盾（`data-i18n="dataSince">数据起点` 子串在加 tooltip 后不存在）→ 改锚 `data-i18n-title="dataSinceTip">数据起点</th>`；F2 en tooltip 误引「仅今日」（en 界面实际显示 'Today only'）→ 改英文措辞并与测试断言对齐；F3 Task 3 行模板占位符 → 内联 2722-2726 行完整模板。

**Review 第 2 轮修正记录（2026-09-08）**：F4 Task 2 Step 2 的预期失败描述滞后（F1 修复后首条断言改造前即通过）→ 更正为「挂在第 2 条断言」。

**Review 第 4 轮修正记录（2026-09-08）**：F5 Task 4 Step 2 改动文件计数「预计 5 个」与实际列出的 6 个不符 → 更正为「共 6 个」。

- **Spec 覆盖**：诊断报告建议 ①列名/tooltip（Task 2）②汇总条同措辞（Task 2 经共享键 `dataSince` 自动生效，Step 7 抽查覆盖）③dsh 空 0 行对齐（Task 1）④表尾提示（Task 3）——无遗漏。
- **占位符扫描**：全部步骤含实际代码/断言，无 TBD/占位（Task 3 已内联完整行模板）。
- **类型/命名一致性**：`renderChannelTable(rows, summary)` 定义与唯一调用点同步；`dataSinceTip` / `unusedChannelsHint` 在 HTML/JS/测试三处拼写一致；`CH_LABEL`（app.js:2434 模块级）在表尾 join 中复用与 Codex 列名显示一致；测试断言的 en tip 与 I18N 值逐字符一致（含 `'Today only'` 转义）。
- **契约测试影响面**：`test_i18n_consistency.py` 两处 `== 13` 均已纳入 Task 2 Step 5；`test_server_merge_dsh` 与门控兼容已验证（70>0）；`test_theme_rerender.py:155` 对行模板 `<td style="color:${CH_COLOR[` 的断言与表尾行（class 无内联色）兼容；全测试库无「数据自」文案断言（grep 已核）。
