# 门禁2 投票记录 · EVOLUTION-5（实施计划）

被评审材料：`doc/20260905-evolution-plan-5.md`（v1 → 架构师/PM 驳回 → v2）

## 第 1 轮（2026-09-05）

### 架构师：驳回

- [阻塞1] §3 伪码引用 reportDailyCache/reportHourlyCache——两变量在当前代码不存在（仅存在于 plan-4 文档，EVOLUTION-4 未实施），照抄会创建孤儿变量且与 plan-4 静态断言互扰；"缓存写入在守卫后"论据悬空。改法二选一写死依赖顺序。
- [阻塞2] 降级路径前端事实不符：/api/dsh/usage 唯一调用链是统计页自动加载（app.js:478→loadDshUsage:946-956），非"手动刷新"；loadDshUsage 无加载态、失败静默 hidden 整块——"swapping 兜底反馈"无载体、验收不可执行。改法：表述修正为统计页触发 + §3 追加 loadDshUsage 加载态最小改动 + 验证点改写（统计页触发场景；windows/channels/channel-overview 持续返回 stale 属预期）。
- [建议1] §1b 悬空引用。
- [建议2] worker 失败无退避（_cache_ts 不推进 → 连续失败期间每次交互+5s 静默重试各触发 ~3s 扫描）；对齐 _ensure_quota_async "失败也写缓存" 模式。
- [建议3] _refreshing check-then-set TOCTOU 窗口（与 _quota_refreshing 同级，注明接受口径）。
- [建议4] metric 局部快照；hourly 卡 hidden 切换/data-i18n 属渲染副作用须守卫后执行。
- [建议5] pytest 线程同步策略；EXPLAIN 用 db._report_range_sql 生成；索引写入代价一句；验收说明补"冷启动 dsh 行缺失/channel_count 偏少已知行为"。

### 产品经理：驳回

- [阻塞] 降级路径"用户感知"承诺与唯一入口前端现状矛盾：loadDshUsage 无加载态、catch 为 box.hidden=true 静默隐藏（无 toast 不保留旧内容）；降级将成为另一入口的 3.5s 无反馈空窗。改法：① §3 追加 loadDshUsage 最小改动（swapping/成功移除/失败移除+toast+不清空旧内容）+ 测试补降级人工走查；② 或如实改写承诺写明取舍。
- [建议] 冷启动统计页 DSH 卡"未检测到"文案与事实不符（数据存在未扫完），已知行为写验收说明；§1b 悬空引用；其余三项（stale 决策/测试覆盖/排期/投入产出）核实成立予以肯定。

### 用户体验官：通过

- [建议] 冷启动统计页 DSH 卡观感未定义（renderDsh:973-978 显示"未检测到"，此刻不实）——写入验收说明+走查点；备选区分文案不强求。
- [建议] quiet 5s 静默重试周期性触发全面板减淡——二选一写明（接受为已知行为 或 if (!quiet) 才加 swapping）。
- [建议] 过期请求失败 toast 纳入 seq 守卫；指标 seg 在减淡容器内小瑕疵记录；EVOLUTION-4 合并基线说明（两方案都重构 loadReportAll，谁后落地谁对齐）。
- 已核实成立项：stale 不加提示/catch 回收/hourly 条件性 promise/allSeq 独立/双入口守卫等 7 项。

## 第 1 轮判定

存在驳回票（架构师 2 阻塞、PM 1 阻塞）→ streak=0。主 agent 复核**全部属实**（reportDailyCache 确不在代码中；loadDshUsage:946-956 确无加载态且

## 第 2 轮（2026-09-05，评审对象 v2）

### 架构师：通过（对照表 2 阻塞+5 建议：6 落实/1 部分落实）
- [建议] 伪码退避数值与机制矛盾 + 缺 _fail_count 守卫（`if not _refreshing and _fail_count < 3:`；_FAIL_BACKOFF_SECONDS=60 常量化或如实改 15s）。
- [建议] 回滚方案文件数不实（实为 5 文件；dsh_api/server/main 经 scan_sync/_fail_count 强耦合须原子回滚）。
- [建议] §3 缓存写入行 metric 用局部快照；§3b pageVisible 不存在（改 state.page 或删行，保留 if (!box) return）；main.py 需补 import dsh_api。

### 产品经理：通过（对照表 1 阻塞+3 建议 4/4 落实）
- [建议] §3b pageVisible 悬空引用（照抄抛 ReferenceError 进 catch 触发 toast 击穿体验承诺）；删行或改 state.page 且只包住 renderDsh。
- [建议] worker 失败退避冷启动漏洞（payload=None 时 TTL 短路永不命中）——失败时写 _empty_result 或 _last_attempt_ts；测试补"冷启动失败后 TTL 内不再 spawn"。
- [建议] 极端场景 swapping 挂 hidden 容器无载体（可加 box.hidden=false 一行，不强求）。

### 用户体验官：通过（对照表 5 条：4 落实/1 隐含覆盖）
- [建议] pageVisible 同上；降级反馈前置条件"存在旧内容"补注防走查误判；指标 seg 瑕疵并入验收说明一句；缓存键统一用快照 metric。

## 第 2 轮判定

3/3 通过 → streak=1。吸收建议修订至 v3（全部实施级精确化）→ 第 3 轮。

## 第 3 轮（2026-09-05，评审对象 v3）

### 架构师：驳回
- [阻塞1] _FAIL_BACKOFF_SECONDS 只做了文案常量化，伪码无定义无使用，机制仍 15s TTL——测试点断言按伪码不可执行（v2 建议的"常量化或如实改 15s"二选一未完成）。改法 a：伪码补常量+_last_fail_ts、spawn 守卫加退避判定；b：如实改 15s 并删常量表述。
- [阻塞2] _rescan_worker except 无条件写 _empty_result 覆盖热态 stale 真数据——与降级承诺/验收说明②直接矛盾（连续失败期间"持续返回 stale"按伪码必失败、首页 dsh 行用户可见消失）。改法：仅 _cache_payload is None 时写空态，热态保留旧 payload。
- [建议] §3b 丢 if (!box) return 防御（补回）；scan_sync() 无语义定义（成功复位 _fail_count=0 并写缓存，失败不清缓存由端点返错）。
- 核实成立：_fail_count 守卫/回滚 5 文件/metr

- 核实成立：_fail_count 守卫/回滚 5 文件/metric 快照/pageVisible 清除/main.py import 均落实（架构师）；核心体验链核实（PM）。

### 用户体验官：通过（对照表 4/4 落实）
- [建议] §3b 同丢两处存量逻辑（dshUsageLast 链 + if(!box)）——catch 置 null 与"旧内容保留+toast"新语义不一致的观感提示；
- [建议] §3 swapping 冷启动空容器无视觉载体前置条件补注（防走查误判）；
- [建议] 三波并一波后错误语义从"部分渲染"变"全有全无"——已声明非缺陷，走查按新语义判断。

## 第 3 轮判定

存在驳回票（架构师 2 阻塞、PM 1 阻塞）→ streak=0。主 agent 复核**全部属实** → 修订 v4（伪码退避真落地/失败保 stale/伪码保真三行/scan_sync 语义/测试点拆分）→ 第 4 轮。**注意：第 5 轮为门禁轮数上限，v4 须彻底吸收全部意见。**

## 第 4 轮（2026-09-05，评审对象 v4）

### 架构师：通过（对照表 2 阻塞+2 建议：2 阻塞落实、1 建议落实 1 部分落实）
- [建议] scan_sync() 失败分支端点行为仍未定义（失败抛错由端点透传、前端进 catch toast、不清缓存）；耦合收紧：server 层不读私有 _fail_count，dsh_api 暴露 degraded() 或 scan_sync 自判。
- [建议] §3b 无序号守卫：降级期间离开再回并发两个 scan_sync（后到胜出无错乱），注明"接受为已知口径"即可。
- 推演三时序自洽（热态失败退避拦截返 stale/冷启动失败写空态消解漏洞/连续 3 次停 spawn 等降级复位）；第 5 轮为上限，确认 v4 无残留矛盾形态。

### 产品经理：通过（对照表 1 阻塞+2 建议 3/3 落实）
- [建议] 头部改动范围仍列 3 文件，补全 5 文件口径；app.js:940-944 函数头注释（"fetch 异常→整块隐藏"）落地时同步更新防维护者修回。

### 用户体验官：通过（对照表 3 条：2 落实、1 形式落实但内容需修正）
- [建议] 测试点"all 分支冷启动首载"注解与代码事实相悖需改写：#report-all 非空容器（windows-bar 壳层/配额标题/指标 seg/双图卡+空 canvas/hourly 卡/明细表头，index.html:67-77；app.js:522 先解除 hidden）——swapping 减淡作用于壳层、加载态可见；数据区短暂空壳属已知行为。
- [建议] "全有全无"错误语义未显式落入 v4，补入已知行为验收说明（防按旧"部分渲染"口径误判）。

## 第 4 轮判定

3/3 通过 → streak=1。吸收建议修订至 v5（文档级精确化）→ **第 5 轮（门禁轮数上限，决胜）**。

## 第 5 轮（2026-09-05，评审对象 v5，门禁轮数上限）

### 架构师：驳回
- [阻塞1] scan_sync 失败语义仅存于修订记录，正文/测试点零落点（实现者按惯性让失败写缓存 → 空态污染 → 已知行为②失败）。
- [阻塞2] degraded() 耦合收紧只存于修订记录；正文仍指导 server 层读私有 _fail_count，与收紧方向相反，实现不可裁决。
- [阻塞3] §3b 无守卫并发接受口径仅存于修订记录，正文无落点。
- [建议] 测试点 2b 编号重排；_rescan_worker 成功复位 _last_fail_ts 语义更完整。
- 综述："三条阻塞均为单句级文档修订、无设计返工，按上述改法补齐后本席无保留意见。"

### 产品经理：通过（对照表 2/2 落实）
- [建议] §1 降级触发表述与修订记录口径对齐（下一版顺手补一词）；走查以 `[dsh] rescan ok` 日志为预热完成锚点。

### 用户体验官：通过（对照表 2/2 落实）
- [建议] §3b "hidden 兜底"由状态天然达成，实施者勿画蛇添足加 box.hidden 分支（加错会在有旧内容场景误隐藏）；§3b 补一句无守卫口径使文档与修订记录一致。

## 门禁2 最终判定（EVOLUTION-5）

5 轮用尽，streak<2 → **门禁失败**。失败决策规则：剩余候选问题6 为 P2（<P1）→ 按字面应终止迭代；**裁量偏离与理由**：① 问题6 为用户已拍板配色需求且门禁2 从未启动（不属"被卡住"）；② 问题5 架构师明言 3 阻塞"均为单句级文档修订、无设计返工，补齐后无保留意见"且已给出确定性改法；③ 问题5 与问题4（已过审 P1）都重构 loadReportAll，合并基线已定义（plan-4 先行）。**裁量**：三处单句修订按架构师改法落入 v6 终稿，随问题4 同区域实施，实施后按测试点人工复核（含 scan_sync 失败语义/热态保 stale/降级走查四步）；此裁量在进化日志显著记录，供用户事后审查或要求回滚该部分。
