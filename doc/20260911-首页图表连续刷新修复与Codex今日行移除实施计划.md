# 首页三图表连续刷新修复 & Codex 今日行移除 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除首页三图表的冗余连续刷新（同步完成双连发 + DSH 15s 整页重绘），并移除 Codex 本地用量区块恒显的"今日"KPI 行。

**Architecture:** 纯前端 `app/web/app.js` 改动。① 删除 `refreshCodexVisible` 的 home 分支（保留 `pollUntilIdle` 空闲分支的 `loadDashboard()` 作为唯一主刷新入口）；② `dshSchedulerTick` 按页签分流——all 页签仅静默刷新 DSH 状态条（新增 `refreshHomeDshStrip`），dsh 页签保留数据保活但透传 `noAnim`；③ `renderCodexSummary` 停止渲染 `data.today` 行。验证采用本仓既有的"源码级静态断言"测试模式（前端无 DOM 自动化设施）。

**Tech Stack:** 原生 JS（Chart.js 4 前端，无构建步骤）+ pytest 源码契约测试 + `node --check` 语法门禁。

**Spec:** `doc/bug-diagnosis-home-chart-triple-refresh-and-codex-today-row-20260911.md`（诊断报告，含 review 修正后的方案 A；本计划据其展开）

## Global Constraints

- 禁止自动签入 git：全部改动完成后须汇报并等待用户人工确认，才能执行 commit（用户全局规则）。
- 只允许修改：`app/web/app.js`、`tests/test_home_refresh_lifecycle.py`（新建）、`tests/test_dsh_ui_contract.py`、`tests/test_empty_state.py`、`tests/test_codex_ui_contract.py`、诊断报告 doc 的状态字段。**不改** `index.html`、`server.py`、`db.py`（后端 `today` 契约字段按 20260907 决议保留）。
- 测试硬约束：`js.count("dshRefreshTimer = setTimeout") == 1` 必须保持（`tests/test_dsh_ui_contract.py:77`）——重武装只能经 `scheduleDshRefresh()`。
- 测试硬约束：`function dshSchedulerTick(target)` 签名原文不变（`tests/test_dsh_ui_contract.py:75`）。
- 图表函数的 `animation: noAnim ? false : undefined,` 全文件计数 == 11（`tests/test_theme_rerender.py:191`），不得新增/删除该模式。
- 代码风格：中文注释、`// 20260911 问题N` 引用诊断报告的标注方式对齐仓内 `§`/日期注释惯例。
- 环境：Windows + Git Bash；Python 用 `python`（3.12.10，禁 `py`）；node 仅用于 `node --check` 与（可选）UI 冒烟脚本。
- 每个任务结束必须 `node --check app/web/app.js` 通过 + 相关 pytest 通过，才算完成。

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `app/web/app.js` | 修改 | 全部三处行为修复（Task 1/2/3） |
| `tests/test_home_refresh_lifecycle.py` | 新建 | Task 1 的回归锚（源码断言：同步完成后首页只刷一次） |
| `tests/test_dsh_ui_contract.py` | 修改 | Task 2 的回归锚（新增 tick 分流断言） |
| `tests/test_empty_state.py` | 修改 | 同步 117 行既有的 `chartReportHourly(hourly, undefined, …)` 断言 → `noAnim` |
| `tests/test_codex_ui_contract.py` | 修改 | Task 3 的回归锚（新增"仅一行 KPI"断言） |
| `doc/bug-diagnosis-home-chart-triple-refresh-and-codex-today-row-20260911.md` | 修改 | 完成后状态更新 |

### Dedupe Ticket（计划涉及的两处新增）

**Ticket 1 — 新增 JS 函数 `refreshHomeDshStrip`**
- Intent signature: 首页 all 页签下仅拉取 `/api/report/windows` 并静默更新 DSH 状态条的周期刷新函数
- Queries: `grep "renderHomeDshStatus" app/web/app.js`；`grep "report/windows" app/web/app.js`；`grep "refreshHomeDsh\|dshSchedulerTick" app/web/app.js`
- Top matches: `renderHomeDshStatus`（只渲染不拉取）、`loadReportAll`（7 接口全量重载）、`dshSchedulerTick`（调度入口）
- Decision: **new**——"拉取 windows + 渲染状态条 + 失败退避重武装"的胶水逻辑无既有单一归属，塞进 renderHomeDshStatus 会混淆"渲染"与"取数"职责

**Ticket 2 — 新建测试文件 `tests/test_home_refresh_lifecycle.py`**
- Intent signature: 同步完成后首页仅一次刷新的源码级回归锚
- Queries: `grep -rn "refreshCodexVisible" tests/`；`grep -rn "pollUntilIdle" tests/`；`grep -rln "app.js" tests/`
- Top matches: `tests/test_theme_rerender.py`（主题重渲，非刷新生命周期）、`tests/test_dsh_ui_contract.py`（DSH 契约）
- Decision: **new**——无既有文件覆盖刷新分派语义；沿用 `_extract_fn` 源码断言模式单独建文件

---

### Task 1: 消除同步完成双连发（诊断报告 问题1 方案 A①）

**Files:**
- Create: `tests/test_home_refresh_lifecycle.py`
- Modify: `app/web/app.js:1610-1620`（refreshCodexVisible）、`app/web/app.js:2054`（行尾注释）

**Interfaces:**
- Consumes: 现有 `loadDashboard(quiet)`、`loadCodexSummary()`、`loadRecords()`、`loadSessions()`（均不改签名）
- Produces: `refreshCodexVisible()` 新契约——只分派 stats/records，home 交由 `pollUntilIdle` 空闲分支的 `await loadDashboard()` 覆盖（Task 2 依赖 loadDashboard 的新增 `noAnim` 参数，本任务不动签名）

- [ ] **Step 1: 基线确认**

Run: `python -m pytest tests/test_theme_rerender.py tests/test_dsh_ui_contract.py tests/test_empty_state.py tests/test_codex_ui_contract.py -q`
Expected: 全部 PASS（改动前基线绿）

- [ ] **Step 2: 写失败测试（新建 `tests/test_home_refresh_lifecycle.py`）**

```python
"""20260911 问题1 方案A① 回归锚: 同步完成后首页只刷一次.

源码级静态断言 (参照 test_theme_rerender 的 _extract_fn 模式):
pollUntilIdle 空闲分支保留 await loadDashboard() 作为 home/stats 共同的
唯一主刷新入口; refreshCodexVisible 不再含 home 分支 (此前 home 双路各调
一次 loadDashboard → 首页三图连续重绘两次, 见 doc/bug-diagnosis-*
home-chart-triple-refresh-*-20260911.md).
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
_APP_JS = ROOT / "app" / "web" / "app.js"


def _src() -> str:
    return _APP_JS.read_text(encoding="utf-8")


def _extract_fn(src: str, name: str) -> str:
    start = src.index(f"function {name}(")
    end = src.index("\n}", start)
    return src[start:end + 2]


def test_poll_until_idle_keeps_single_dashboard_refresh():
    body = _extract_fn(_src(), "pollUntilIdle")
    assert "await loadDashboard();" in body      # 唯一主刷新入口 (home/stats 共用)
    assert "refreshCodexVisible();" in body      # stats/records 分派仍保留


def test_refresh_codex_visible_has_no_home_branch():
    body = _extract_fn(_src(), "refreshCodexVisible")
    assert "loadDashboard" not in body           # home 刷新不再走此分派
    assert 'state.page === "stats"' in body
    assert 'state.page === "records"' in body
```

- [ ] **Step 3: 运行测试确认失败**

Run: `python -m pytest tests/test_home_refresh_lifecycle.py -v`
Expected: `test_refresh_codex_visible_has_no_home_branch` FAIL（函数体含 `loadDashboard`）

- [ ] **Step 4: 修改 `refreshCodexVisible`（app.js:1610-1620）**

old:
```js
/* Codex 导入完成后的可见数据刷新分派 (pollUntilIdle 空闲分支调用):
   stats→loadCodexSummary, home→loadDashboard(true), records→records+sessions 并发,
   其他页仅更新状态 (切页时 switchPage 自然重拉) */
function refreshCodexVisible() {
  if (state.page === "stats") loadCodexSummary().catch(() => {});
  else if (state.page === "home") loadDashboard(true);
  else if (state.page === "records") {
```
new:
```js
/* Codex 导入完成后的可见数据刷新分派 (pollUntilIdle 空闲分支调用):
   stats→loadCodexSummary, records→records+sessions 并发,
   其他页仅更新状态 (切页时 switchPage 自然重拉);
   home 不在此刷新: 空闲分支前置的 await loadDashboard() 已覆盖,
   双路各刷一次曾致首页三图连续重绘 (20260911 问题1 方案A①) */
function refreshCodexVisible() {
  if (state.page === "stats") loadCodexSummary().catch(() => {});
  else if (state.page === "records") {
```
（函数其余行不变）

- [ ] **Step 5: 更新调用点行尾注释（app.js:2054）**

old:
```js
        refreshCodexVisible();   // stats→loadCodexSummary / home→loadDashboard(true) / records→records+sessions
```
new:
```js
        refreshCodexVisible();   // stats→loadCodexSummary / records→records+sessions (home 由上方 loadDashboard 覆盖)
```

- [ ] **Step 6: 验证通过**

Run: `node --check app/web/app.js && python -m pytest tests/test_home_refresh_lifecycle.py -v`
Expected: 语法 OK + 2 个测试 PASS
回归: `python -m pytest tests/test_theme_rerender.py tests/test_dsh_ui_contract.py -q` 仍全绿

---

### Task 2: DSH 周期刷新按页签解耦 + noAnim 透传（诊断报告 问题1 方案 A②）

**Files:**
- Modify: `app/web/app.js:647`（loadDashboard 签名）、`:658`、`:692`、`:1136-1142`（dshSchedulerTick + 新函数）、`:2770`（loadReportAll 签名）、`:2812-2813`、`:2823`
- Modify: `tests/test_dsh_ui_contract.py`（文件末尾追加新测试）、`tests/test_empty_state.py:117`（断言同步）

**Interfaces:**
- Consumes: Task 1 完成后的 `refreshCodexVisible`（不再调用 loadDashboard）；现有 `renderHomeDshStatus(status)`（内部调 `scheduleDshRefresh` 重武装周期）、`scheduleDshRefresh(status)`、`DSH_FAIL_BACKOFF_S`、`api(path)`
- Produces: `loadDashboard(quiet = false, noAnim = false)`、`loadReportAll(quiet = false, noAnim = false)` 新签名（既有调用点全部走默认值，行为不变）；新函数 `refreshHomeDshStrip()`（async，内部自武装周期）

- [ ] **Step 1: 更新既有断言（`tests/test_empty_state.py:117`）**

old:
```python
    assert 'chartReportHourly(hourly, undefined, range === "today" ? "noUsageToday" : "noDataInRange");' in load
```
new:
```python
    assert 'chartReportHourly(hourly, noAnim, range === "today" ? "noUsageToday" : "noDataInRange");' in load
```

- [ ] **Step 2: 写失败测试（`tests/test_dsh_ui_contract.py` 末尾追加）**

```python
def test_dsh_home_tick_avoids_full_dashboard_reload():
    """20260911 问题1 方案A②: 首页 all 页签 DSH 周期刷新只静默刷状态条,
    不再整页 loadDashboard (曾致三图每 15s destroy+重建并重播入场动画);
    dsh 页签保留数据保活但 noAnim。重武装仍只经 scheduleDshRefresh (唯一 setTimeout)。"""
    js = (ROOT / "app/web/app.js").read_text(encoding="utf-8")
    t0 = js.index("function dshSchedulerTick(")
    tick = js[t0:js.index("\n}", t0)]
    assert 'refreshHomeDshStrip() : loadDashboard(true, true)' in tick   # all 仅刷状态条 / dsh 保活 noAnim
    assert "loadDashboard(true)" not in tick                             # 旧的单参整页刷新已移除
    s0 = js.index("async function refreshHomeDshStrip")
    strip = js[s0:js.index("\n}", s0)]
    assert 'renderHomeDshStatus(w.dsh_status)' in strip                  # 复用状态条渲染 (内部重武装)
    assert "scheduleDshRefresh(" in strip                                # 失败退避也重武装
    assert 'await api("/api/report/windows")' in strip
    assert js.count("dshRefreshTimer = setTimeout") == 1                 # 周期唯一入口不破坏
```

- [ ] **Step 3: 运行测试确认失败**

Run: `python -m pytest tests/test_dsh_ui_contract.py::test_dsh_home_tick_avoids_full_dashboard_reload tests/test_empty_state.py -v`
Expected: 新测试 FAIL（无 refreshHomeDshStrip）；test_empty_state 的 hourly 断言 FAIL（app.js 未改）

- [ ] **Step 4: app.js 六处编辑**

4.1 签名（app.js:647）
old: `async function loadDashboard(quiet = false) {`
new: `async function loadDashboard(quiet = false, noAnim = false) {`

4.2 透传 all 分支（app.js:658）
old: `      await loadReportAll(quiet);`
new: `      await loadReportAll(quiet, noAnim);`

4.3 透传单渠道分支（app.js:692）
old: `      chartToday(trend);                              // 24h input/output 双系列 (spec v8)`
new: `      chartToday(trend, noAnim);                      // 24h input/output 双系列 (spec v8)`

4.4 签名（app.js:2770）
old: `async function loadReportAll(quiet = false) {`
new: `async function loadReportAll(quiet = false, noAnim = false) {`

4.5 三图调用（app.js:2812-2813）
old:
```js
    chartReportStack(daily);
    chartReportDonut(daily);
```
new:
```js
    chartReportStack(daily, noAnim);
    chartReportDonut(daily, noAnim);
```

4.6 hourly 调用（app.js:2823）
old: `      chartReportHourly(hourly, undefined, range === "today" ? "noUsageToday" : "noDataInRange");`
new: `      chartReportHourly(hourly, noAnim, range === "today" ? "noUsageToday" : "noDataInRange");`

- [ ] **Step 5: dshSchedulerTick 分流 + 新函数（app.js:1136-1142 整体替换）**

old:
```js
function dshSchedulerTick(target) {   // 唯一自动刷新周期入口: 不叠加请求、无 toast
  if (target !== dshSchedulerTarget()) return;
  if (target === "stats") { loadDshUsage().catch(() => {}); return; }
  if (dshHomeRefreshing) return;
  dshHomeRefreshing = true;
  Promise.resolve(loadDashboard(true)).catch(() => {}).finally(() => { dshHomeRefreshing = false; });
}
```
new:
```js
function dshSchedulerTick(target) {   // 唯一自动刷新周期入口: 不叠加请求、无 toast
  if (target !== dshSchedulerTarget()) return;
  if (target === "stats") { loadDshUsage().catch(() => {}); return; }
  if (dshHomeRefreshing) return;
  dshHomeRefreshing = true;
  // 20260911 问题1 方案A②: all 页签仅静默刷 DSH 状态条 (整页刷新曾致三图每 15s 重建重播动画);
  // dsh 页签数据保活但 noAnim
  const refresh = state.channel === "all" ? refreshHomeDshStrip() : loadDashboard(true, true);
  Promise.resolve(refresh).catch(() => {}).finally(() => { dshHomeRefreshing = false; });
}
async function refreshHomeDshStrip() {
  try {
    const w = await api("/api/report/windows");
    if (dshSchedulerTarget() !== "home" || state.channel !== "all") return;
    renderHomeDshStatus(w.dsh_status);   // 内部 scheduleDshRefresh 重武装 15s 周期
  } catch (e) {
    scheduleDshRefresh({ refresh_error: true, retry_after_seconds: DSH_FAIL_BACKOFF_S });
  }
}
```

- [ ] **Step 6: 验证通过**

Run: `node --check app/web/app.js && python -m pytest tests/test_dsh_ui_contract.py tests/test_empty_state.py tests/test_theme_rerender.py tests/test_home_refresh_lifecycle.py -v`
Expected: 全 PASS（含 `test_all_chart_functions_take_noanim_param` 的 11 处计数、`dshRefreshTimer = setTimeout` 唯一性）

---

### Task 3: Codex 停渲"今日"行（诊断报告 问题2 方案 A）

**Files:**
- Modify: `app/web/app.js:1533`、`:1548`（renderCodexSummary）
- Modify: `tests/test_codex_ui_contract.py`（末尾追加新测试）

**Interfaces:**
- Consumes: 后端 `/api/codex/summary` 契约不变（`today` 字段保留，被 `tests/test_codex_server.py` 固定键集断言）
- Produces: `renderCodexSummary(data)` 渲染契约——`#codex-kpis` 仅所选范围口径一行；`#codex-today-kpis` DOM 保留但恒隐藏、恒为空

- [ ] **Step 1: 写失败测试（`tests/test_codex_ui_contract.py` 末尾追加）**

```python
def test_codex_summary_renders_only_range_row():
    """20260911 问题2: 今日行 (data.today) 不再渲染, KPI 仅所选范围口径一行;
    DOM 契约保留 #codex-today-kpis (恒隐藏, 见 test_codex_stats_nodes)。"""
    js = (ROOT / "app/web/app.js").read_text(encoding="utf-8")
    start = js.index("function renderCodexSummary(")
    body = js[start:js.index("\n}", start)]
    assert "cards(data.today" not in body        # 今日行不再取数渲染
    assert "todayKpis.hidden = true;" in body    # 显示路径也保持隐藏
    assert "todayKpis.hidden = false" not in body
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_codex_ui_contract.py::test_codex_summary_renders_only_range_row -v`
Expected: FAIL（`cards(data.today` 存在于函数体）

- [ ] **Step 3: 修改 renderCodexSummary（app.js:1533）**

old:
```js
  kpis.hidden = false;
  todayKpis.hidden = false;
```
new:
```js
  kpis.hidden = false;
  todayKpis.hidden = true;   // 20260911 问题2: 今日行不再渲染, 仅所选范围口径 (后端 today 契约字段保留)
```

- [ ] **Step 4: 删除今日行渲染（app.js:1548）**

old:
```js
  kpis.innerHTML = cards(data.totals || {}).join("");     // 总量行 ← totals
  todayKpis.innerHTML = cards(data.today || {}).join(""); // 今日行 ← today
```
new:
```js
  kpis.innerHTML = cards(data.totals || {}).join("");     // 总量行 ← totals (唯一 KPI 行)
```

- [ ] **Step 5: 验证通过**

Run: `node --check app/web/app.js && python -m pytest tests/test_codex_ui_contract.py tests/test_codex_db.py tests/test_codex_server.py -q`
Expected: 全 PASS（UI 契约新测试转绿；后端契约测试不受影响）

---

### Task 4: 全量回归 + 人工验收清单 + 文档收尾

**Files:**
- Modify: `doc/bug-diagnosis-home-chart-triple-refresh-and-codex-today-row-20260911.md`（仅状态与实施记录段）

- [ ] **Step 1: 全量回归**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS（基线约 541 用例，0 failed）

- [ ] **Step 2: UI 冒烟（可选，环境具备时）**

Run: `node scripts/check_codex_ui.cjs`
Expected: 全部 PASS 输出（该脚本对 `#codex-kpis` 断言 130/无 NaN，不涉及 `#codex-today-kpis`，预期不受影响）；若本机 playwright 不可用则跳过，以 Step 1 + Step 3 为准

- [ ] **Step 3: 人工验收清单（启动应用 `python entry.py` 后逐项确认）**

1. 首页 all 页签点「刷新」→ 三图只重绘 **1 次**（修复前 2~3 次）。
2. 停留首页 all ≥45s（覆盖 ≥2 个 DSH 周期）→ 三图不再重播入场动画；顶部 DSH 状态条仍按周期静默更新（scanning 时 1s、常态 15s）。
3. 切 dsh 页签停留 ≥20s → DSH 数据仍周期刷新但无入场动画（观察 chartToday 不再闪动）。
4. 统计页 Codex 本地用量 → 仅 **1 行** KPI 且数值随右上角 range 切换变化；无"今日"第二行。
5. 统计页完成一次同步（点顶栏「刷新」）→ 主区块（总量卡/趋势图/模型图）仍会刷新（Task 1 无 stats 回归的证据）。
6. 切换深色/浅色主题 → 图表仍正常 noAnim 重渲（EVOLUTION-4 路径未破坏）。

- [ ] **Step 4: 更新诊断报告状态**

`doc/bug-diagnosis-home-chart-triple-refresh-and-codex-today-row-20260911.md` 头部：
- `状态：待确认` → `状态：已修复（待人工验收）`
- 文末追加实施记录：任务清单勾选情况、回归结果（pytest 通过数）、遗留说明（首页 all 的渠道明细/KPI 条不再随 DSH 15s 周期自动刷新，改为同步完成/切页/手动刷新时机更新——已在诊断报告方案 A② 声明的取舍）。

- [ ] **Step 5: 汇报并等待签入确认（禁止自动 commit）**

向用户汇报改动清单与验收结果，等待人工确认。确认后建议的提交（由用户执行或明确授权后执行）：
```bash
git add app/web/app.js tests/test_home_refresh_lifecycle.py tests/test_dsh_ui_contract.py tests/test_empty_state.py tests/test_codex_ui_contract.py doc/
git commit -m "fix: dedupe home refresh triggers and drop codex today kpi row"
```

---

## 已知取舍（实施后行为变化，来自诊断报告方案 A②）

- 首页 all 页签的渠道明细/KPI 条/三图不再随 DSH 15s 周期自动刷新，数据更新时机收敛为：同步完成（pollUntilIdle）、切页/切页签/切 range/切指标、手动刷新。后台 Codex 增量导入的新数据同样等上述时机呈现（自动同步默认 300s 兜底）。
- `dshSchedulerTick` 在 all 页签失败时按 `DSH_FAIL_BACKOFF_S`(60s) 退避重武装，不再整页重载。
