# GoGauge 界面优化（深色主题精修 + 主题持久化）执行计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **本仓附加硬约束（优先级高于技能默认行为）**：每个 Task 末尾的签入步骤必须暂停等待人工确认，不得自动 `git commit`；Python 修改后必须 `py_compile` 通过、JS 修改后必须 `node --check` 通过才算完成。

**Goal:** 按已定稿设计完成深色视觉精修（组件/图表/焦点/浮层）与主题偏好的"保存 → 首页预置 → 首屏脚本 → 原生窗口底色"全链路，跨端口、跨进程重启可恢复。

**Architecture:** 不改框架（Python + pywebview + 原生 JS + Chart.js）。CSS 侧新增 25 个语义变量（亮色值=现状逐像素一致，深色值对比度经程序化校验）；JS 侧新增 `chartColors()`/`chartThemeOptions()` 每次绘制时解析具体颜色（Canvas 不能用 `var(--x)`）；持久化复用现有 settings JSON（加 `theme` 键）与 `PUT /api/settings`，服务端向 index.html 注入 `data-theme-preference` 枚举，head 内联 bootstrap 脚本在样式表前应用主题。

**Tech Stack:** Python 3.12.10（pyenv-win）、pytest、pywebview/WebView2、原生 JavaScript、Chart.js（chart.umd.min.js，禁止编辑）、Node v22.17.0（测试用）、Python Playwright + Chromium（仅开发依赖）。

**Spec:** [20260909-界面优化与渐进重构实施计划.md](./20260909-界面优化与渐进重构实施计划.md) —— 本计划是其实施分解（D1–D4 → Task 1–13），执行时两份文档对照阅读；规格节号（§3.2/§3.3/§3.4/§5）在下文引用。

## Global Constraints

每个 Task 的要求都隐含包含本节：

- 工作目录一律为仓库根 `F:/GitHubs/opencode-go-gauge`；shell 命令按本仓规范加 `rtk proxy` 前缀（RTK 是只读代理，不影响语义）。
- Python 解释器：默认 `python`；不可用则用 `D:\.pyenv\pyenv-win\versions\3.12.10\python.exe`。禁止 `py`、`py -3.12`、裸 `pip`。
- 不引入生产依赖；`requirements-dev.txt` 只允许加 Playwright（Task 8）。不改数据库 schema；不改统计口径、列数量、每页 7 条；不编辑 `app/web/chart.umd.min.js`。
- 浅色主题逐像素保持现状：`:root` 既有变量值一律不改，新增变量在亮色的值必须等于当前生效值。
- 保留七个渠道身份色（`--ch-*`）、图表 callbacks/noAnim/缓存/防乱序、`applyDarkMode → rerenderCharts → refreshIcons` 唯一入口。
- 行号来自基线 `8e9fd63`，仅用于定位；执行时以符号搜索到的当前源码为准。
- 每个 Task 的验证命令必须通过；代码改动后执行编译检查：Python 用 `python -m py_compile`，JS 用 `node --check`。
- **签入门禁**：每个 Task 末尾给出 `git add`/`git commit` 命令，执行者必须停下向人工报告本 Task 结果，得到确认后才可执行签入；不得自行签入、不得推送。
- 建议人工先建分支：`git checkout -b feat/dark-theme-polish`（在 main 上直接工作需人工明确同意）。

## 任务总览

| Task | 内容 | 对应规格 | 主要文件 |
|---|---|---|---|
| 1 | 深色基准图采集（人工门禁） | D1 首项 | artifacts/dark-theme-review/ |
| 2 | CSS 语义令牌 25 个 + 按钮/危险/刷新消费点 | D1、§3.2 | style.css、test_dark_theme.py、app.js(2 行) |
| 3 | 深色层级/焦点可见/浮层变量/减少动画 | D2、§3.1 | style.css、test_dark_theme.py |
| 4 | db.py theme 存储契约 | D3、§3.4 | app/db.py、tests/test_theme_preferences.py |
| 5 | server.py 首页主题预置注入 | D3、§3.4 | app/server.py、tests/test_theme_preferences.py |
| 6 | main.py 主窗口原生底色 | D3、§3.4 | app/main.py、tests/test_theme_preferences.py |
| 7 | index.html 首屏 bootstrap + Node 执行测试 | D3、§3.4 | index.html、tests/test_theme_bootstrap.py |
| 8 | Playwright 设施 + 持久化交互测试（先失败） | §5 | requirements-dev.txt、tests/test_theme_interactions.py |
| 9 | JS 持久化链路实现（队列/入口/init/迁移/I18N） | D3、§3.4 | app/web/app.js |
| 10 | 交互测试扩展：十图/tooltip/图标/减少动画（先失败） | §5 | tests/test_theme_interactions.py |
| 11 | 图表主题实现：chartColors/chartThemeOptions + 十图 | D3、§3.3 | app/web/app.js |
| 12 | 账号色 CSS 变量化（OV_COLORS/sparklineSvg） | D3、§3.3 | app/web/app.js |
| 13 | 全量回归 + 桌面人工验收矩阵 + 证据归档 | D4 | artifacts/dark-theme-review/、验收记录 |

**接口契约（跨 Task 引用，签名以此为准）：**

- `db.get_settings()["theme"]` → `"light" | "dark" | None`（Task 4 产出）
- `server._inject_theme_seed(body: bytes) -> bytes`（Task 5 产出）
- `main._theme_background_color() -> str`（Task 6 产出）
- JS `chartColors() -> {input, output, reasoning, cache, cost, extra}` 均为具体颜色字符串（Task 11 产出）
- JS `chartThemeOptions() -> {legendLabel, tick, gridColor, tooltip: {backgroundColor, titleColor, bodyColor, borderColor, borderWidth}}`（Task 11 产出）
- JS `setThemePreference(on: boolean) -> void`（Task 9 产出；Task 8 的测试先于实现引用它）

---

### Task 1: 深色基准图采集（人工门禁）

**Files:**
- Create: `artifacts/dark-theme-review/baseline-*.png`、`artifacts/dark-theme-review/baseline-notes.md`

**Interfaces:**
- Consumes: 无
- Produces: D1 基准图，供 Task 13 对照验收

本任务是纯人工操作（执行模型无法可靠操作桌面 GUI）。执行者向人工输出下面的操作单，人工完成后继续。

- [ ] **Step 1: 向人工发出基准采集操作单**

```text
请人工执行：
1. 启动应用：rtk proxy python -m app.main
2. 切换到深色主题（顶栏 ◐ 按钮），窗口分别置于 1280×840 与最小 1000×680
3. 逐页截图存入 artifacts/dark-theme-review/，命名 baseline-<页面>-<尺寸>.png：
   首页(全部)、首页(单渠道，如 zcode)、统计页(含各本地区块)、使用记录(两张表)、
   账户总览(设置里先开"账户总览面板")、设置、关于、欢迎页(未登录态)、
   用户菜单(点顶栏账户名)、确认弹框(任一删除/退出确认)、toast(触发一次同步失败或成功)
4. 截图不得含完整 Key/凭据
5. 在 artifacts/dark-theme-review/baseline-notes.md 记录：采集日期、git 提交号、
   窗口尺寸、DPI 缩放(100%/125%/150% 中实际值)、主题=深色、样例账号状态
```

- [ ] **Step 2: 确认产物存在**

Run: `rtk proxy git status --short artifacts/dark-theme-review/`
Expected: 至少 11 张 baseline 截图 + baseline-notes.md 处于未跟踪状态

- [ ] **Step 3: 签入（人工确认后执行）**

```bash
git add artifacts/dark-theme-review/
git commit -m "chore: 深色优化基准截图 (D1 基线)"
```

---

### Task 2: CSS 语义令牌与 D1 消费点

**Files:**
- Modify: `app/web/style.css`（:root 加 25 令牌、dark 块加 25 覆盖、7 条消费规则改引用）
- Modify: `app/web/app.js:1646,1710`（两行内联错误文字 `var(--red)` → `var(--danger-text)`）
- Test: `tests/test_dark_theme.py`（先改测试，失败后实现）

**Interfaces:**
- Consumes: 现有 `:root`/`html[data-theme="dark"]` 变量块（style.css:1-69）
- Produces: 25 个变量名（Task 3/11/12 消费）：`--button-primary-bg/hover/text`、`--refresh-hover-text`、`--danger-text/hover-bg/hover-text`、`--surface-popover`、`--border-popover`、`--focus-ring`、`--chart-input/output/reasoning/cache/cost/extra`、`--account-1..6`、`--chart-tooltip-bg/text/border`

- [ ] **Step 1: 先写失败测试 — test_dark_theme.py 追加与修订**

3 处修订（白名单扩容，否则新增变量会被旧锚测试拒绝）：

a) 在 `_ROOT_ADDED_ALLOWED = {...}`（第 47 行）之前插入令牌集合并改写该行：

```python
# 20260909 界面优化 §3.2: 25 个语义令牌 (:root 值=现状逐像素一致, dark 值对比度见 §6 测试)
_THEME_TOKENS = {
    "--button-primary-bg", "--button-primary-hover", "--button-primary-text",
    "--refresh-hover-text",
    "--danger-text", "--danger-hover-bg", "--danger-hover-text",
    "--surface-popover", "--border-popover", "--focus-ring",
    "--chart-input", "--chart-output", "--chart-reasoning",
    "--chart-cache", "--chart-cost", "--chart-extra",
    "--account-1", "--account-2", "--account-3",
    "--account-4", "--account-5", "--account-6",
    "--chart-tooltip-bg", "--chart-tooltip-text", "--chart-tooltip-border",
}

# :root 不可变锚允许的新增 token 白名单 (--up/--down: EVOLUTION-6; --ch-codex: T5 Codex 渠道色; 25 语义令牌: 20260909)
_ROOT_ADDED_ALLOWED = {"--up", "--down", "--ch-codex"} | _THEME_TOKENS
```

b) `test_dark_untouched_tokens_unchanged_from_head` 中 `assert set(cur) - set(head) <= {"--up", "--down", "--ch-codex"}` 改为：

```python
    assert set(cur) - set(head) <= {"--up", "--down", "--ch-codex"} | _THEME_TOKENS   # 20260909 语义令牌
```

c) 文件末尾追加新测试：

```python
# ---------------------------------------------------------------------------
# 8. 20260909 语义令牌: dark 固定值 + 亮色等于现状值 + WCAG 对比度 + 消费点
# ---------------------------------------------------------------------------

# dark 块 25 令牌固定值 (对比度: 按钮文字 6.00/5.01, 刷新hover 5.92, 危险 5.12/5.02,
# 焦点环 9.41/10.22, tooltip 12.34 — 由下方测试程序化复核, 不凭此注释验收)
_DARK_THEME_EXPECTED = {
    "--button-primary-bg": "#9d7cf8",
    "--button-primary-hover": "#8b6cf6",
    "--button-primary-text": "#111112",
    "--refresh-hover-text": "#111112",
    "--danger-text": "#f87171",
    "--danger-hover-bg": "#ef4444",
    "--danger-hover-text": "#111112",
    "--surface-popover": "#262628",
    "--border-popover": "#3d3d42",
    "--focus-ring": "#c4b5fd",
    "--chart-input": "#6ba3ff",
    "--chart-output": "#4ade80",
    "--chart-reasoning": "#c4b5fd",
    "--chart-cache": "#22d3ee",
    "--chart-cost": "#fbbf24",
    "--chart-extra": "#f472b6",
    "--account-1": "#9d7cf8",
    "--account-2": "#6ba3ff",
    "--account-3": "#4ade80",
    "--account-4": "#fbbf24",
    "--account-5": "#22d3ee",
    "--account-6": "#f472b6",
    "--chart-tooltip-bg": "#262628",
    "--chart-tooltip-text": "#e8e8ea",
    "--chart-tooltip-border": "#3d3d42",
}

# :root 25 令牌亮色锚 (= 改动前生效值, 浅色逐像素不变的程序化保证)
_ROOT_THEME_EXPECTED = {
    "--button-primary-bg": "#7c5cf6",
    "--button-primary-hover": "#6a46ea",
    "--button-primary-text": "#ffffff",
    "--refresh-hover-text": "#ffffff",
    "--danger-text": "#ef4444",
    "--danger-hover-bg": "#ef4444",
    "--danger-hover-text": "#ffffff",
    "--surface-popover": "#ffffff",
    "--border-popover": "#eae7f2",
    "--focus-ring": "#7c5cf6",
    "--chart-input": "#4f8ef7",
    "--chart-output": "#22c55e",
    "--chart-reasoning": "#a78bfa",
    "--chart-cache": "#06b6d4",
    "--chart-cost": "#d97706",
    "--chart-extra": "#ec4899",
    "--account-1": "#7c5cf6",
    "--account-2": "#4f8ef7",
    "--account-3": "#22c55e",
    "--account-4": "#d97706",
    "--account-5": "#06b6d4",
    "--account-6": "#ec4899",
    "--chart-tooltip-bg": "rgba(0, 0, 0, 0.8)",
    "--chart-tooltip-text": "#ffffff",
    "--chart-tooltip-border": "rgba(0, 0, 0, 0)",
}


def test_theme_tokens_dark_fixed_values():
    dark = _dark_vars(_css())
    for token, expected in _DARK_THEME_EXPECTED.items():
        assert dark.get(token) == expected, f"dark {token} 应为 {expected}, 实际 {dark.get(token)!r}"


def test_theme_tokens_root_values_equal_legacy():
    root = _root_vars(_css())
    for token, expected in _ROOT_THEME_EXPECTED.items():
        assert root.get(token) == expected, f":root {token} 应为 {expected} (=现状值), 实际 {root.get(token)!r}"


def test_theme_tokens_dark_text_contrast():
    """§3.1: 文字对实际承载面 >=4.5:1; 焦点环 >=3:1."""
    # 运行时取值 = dark 覆盖 :root (--amber/--danger-soft 仅存在于 :root, 必须合并后取值)
    merged = {**_root_vars(_css()), **_dark_vars(_css())}
    text_cases = [
        ("--button-primary-text", "--button-primary-bg"),
        ("--button-primary-text", "--button-primary-hover"),
        ("--refresh-hover-text", "--amber"),
        ("--danger-text", "--danger-soft"),
        ("--danger-hover-text", "--danger-hover-bg"),
        ("--chart-tooltip-text", "--chart-tooltip-bg"),
    ]
    for fg, bg in text_cases:
        ratio = _contrast(merged[fg], merged[bg])
        assert ratio >= 4.5, f"dark {fg}={merged[fg]} on {bg}={merged[bg]}: {ratio:.2f}:1 < 4.5:1"
    for surface in ("--card", "--bg"):
        ratio = _contrast(merged["--focus-ring"], merged[surface])
        assert ratio >= 3.0, f"dark --focus-ring on {surface}: {ratio:.2f}:1 < 3:1"


def test_theme_tokens_consumers_use_var():
    """§3.2 消费点契约: 按钮/危险/刷新全部改引新变量."""
    css = _css()
    assert ".btn-primary { background: var(--button-primary-bg); border-color: var(--button-primary-bg); color: var(--button-primary-text); font-weight: 600; }" in css
    assert ".btn-primary:hover { background: var(--button-primary-hover); border-color: var(--button-primary-hover); }" in css
    assert ".pill.small.refresh:hover { background: var(--amber); color: var(--refresh-hover-text); }" in css
    assert ".btn-danger { background: var(--danger-soft); border-color: transparent; color: var(--danger-text); }" in css
    assert ".btn-danger:hover { background: var(--danger-hover-bg); color: var(--danger-hover-text); }" in css
    assert ".badge.no { background: var(--danger-soft); color: var(--danger-text); border-color: transparent; }" in css
    assert "#codex-error { margin: 8px 0 10px; padding: 10px 14px; font-size: 12px; color: var(--danger-text); background: var(--danger-soft); border-radius: 10px; }" in css
    js = _js()
    assert js.count("color:var(--danger-text)") == 2, "两张记录表内联错误文字应使用 var(--danger-text)"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `rtk proxy python -m pytest tests/test_dark_theme.py -q`
Expected: FAIL（`test_theme_tokens_dark_fixed_values` 等报变量缺失；`test_root_tokens_unchanged_from_head` 可能先报白名单外新增——若报说明修订 a) 未生效）

- [ ] **Step 3: 实现 style.css 令牌**

在 `:root` 块内 `--down` 行之后（即 `:root` 块收尾 `}` 之前）插入：

```css
  /* ---- 20260909 界面优化 §3.2: 语义令牌 (亮色值 = 现状逐像素一致) ---- */
  --button-primary-bg: #7c5cf6;
  --button-primary-hover: #6a46ea;
  --button-primary-text: #ffffff;
  --refresh-hover-text: #ffffff;
  --danger-text: #ef4444;
  --danger-hover-bg: #ef4444;
  --danger-hover-text: #ffffff;
  --surface-popover: #ffffff;
  --border-popover: #eae7f2;
  --focus-ring: #7c5cf6;
  --chart-input: #4f8ef7;
  --chart-output: #22c55e;
  --chart-reasoning: #a78bfa;
  --chart-cache: #06b6d4;
  --chart-cost: #d97706;
  --chart-extra: #ec4899;
  --account-1: #7c5cf6;
  --account-2: #4f8ef7;
  --account-3: #22c55e;
  --account-4: #d97706;
  --account-5: #06b6d4;
  --account-6: #ec4899;
  --chart-tooltip-bg: rgba(0, 0, 0, 0.8);
  --chart-tooltip-text: #ffffff;
  --chart-tooltip-border: rgba(0, 0, 0, 0);
```

在 `html[data-theme="dark"]` 块内 `--ch-codex` 行之后（块收尾 `}` 之前）插入：

```css
  /* ---- 20260909 界面优化 §3.2: 深色语义令牌 (对比度由 test_dark_theme.py §8 程序化校验) ---- */
  --button-primary-bg: #9d7cf8;
  --button-primary-hover: #8b6cf6;
  --button-primary-text: #111112;
  --refresh-hover-text: #111112;
  --danger-text: #f87171;
  --danger-hover-bg: #ef4444;
  --danger-hover-text: #111112;
  --surface-popover: #262628;
  --border-popover: #3d3d42;
  --focus-ring: #c4b5fd;
  --chart-input: #6ba3ff;
  --chart-output: #4ade80;
  --chart-reasoning: #c4b5fd;
  --chart-cache: #22d3ee;
  --chart-cost: #fbbf24;
  --chart-extra: #f472b6;
  --account-1: #9d7cf8;
  --account-2: #6ba3ff;
  --account-3: #4ade80;
  --account-4: #fbbf24;
  --account-5: #22d3ee;
  --account-6: #f472b6;
  --chart-tooltip-bg: #262628;
  --chart-tooltip-text: #e8e8ea;
  --chart-tooltip-border: #3d3d42;
```

- [ ] **Step 4: 改 7 条消费规则（精确整行替换）**

| 旧（当前源码） | 新 |
|---|---|
| `.btn-primary { background: var(--primary); border-color: var(--primary); color: #fff; font-weight: 600; }` | `.btn-primary { background: var(--button-primary-bg); border-color: var(--button-primary-bg); color: var(--button-primary-text); font-weight: 600; }` |
| `.btn-primary:hover { background: var(--primary-strong); }` | `.btn-primary:hover { background: var(--button-primary-hover); border-color: var(--button-primary-hover); }` |
| `.pill.small.refresh:hover { background: var(--amber); color: #fff; }` | `.pill.small.refresh:hover { background: var(--amber); color: var(--refresh-hover-text); }` |
| `.btn-danger { background: var(--danger-soft); border-color: transparent; color: var(--red); }` | `.btn-danger { background: var(--danger-soft); border-color: transparent; color: var(--danger-text); }` |
| `.btn-danger:hover { background: var(--red); color: #fff; }` | `.btn-danger:hover { background: var(--danger-hover-bg); color: var(--danger-hover-text); }` |
| `.badge.no { background: var(--danger-soft); color: var(--red); border-color: transparent; }` | `.badge.no { background: var(--danger-soft); color: var(--danger-text); border-color: transparent; }` |
| `#codex-error { margin: 8px 0 10px; padding: 10px 14px; font-size: 12px; color: var(--red); background: var(--danger-soft); border-radius: 10px; }` | `#codex-error { margin: 8px 0 10px; padding: 10px 14px; font-size: 12px; color: var(--danger-text); background: var(--danger-soft); border-radius: 10px; }` |

契约外保持 `var(--red)` 不动：`.ub-error`（style.css:243）、`.toast.err` 左边条（:424，装饰）、`tb-login`（app.js:1772）——这些是 §3.2 明确排除的"普通图形红色/契约外消费点"，不得顺手改。

app.js 两处内联错误文字（记录表加载失败行）：

- 第 1646 行：`style="text-align:center;color:var(--red);padding:20px"` → `style="text-align:center;color:var(--danger-text);padding:20px"`
- 第 1710 行：`style="text-align:center;color:var(--red);padding:24px"` → `style="text-align:center;color:var(--danger-text);padding:24px"`

- [ ] **Step 5: 验证（测试 + 编译检查）**

Run: `rtk proxy python -m pytest tests/test_dark_theme.py -q`
Expected: PASS（全部，含旧锚测试）

Run: `rtk proxy node --check app/web/app.js`
Expected: 无输出（退出码 0）

- [ ] **Step 6: 签入（人工确认后执行）**

```bash
git add app/web/style.css app/web/app.js tests/test_dark_theme.py
git commit -m "feat: 深色语义令牌 25 个与按钮/危险/刷新消费点 (D1)"
```

---

### Task 3: 深色层级、焦点可见、浮层变量与减少动画

**Files:**
- Modify: `app/web/style.css`
- Test: `tests/test_dark_theme.py`（先改测试）

**Interfaces:**
- Consumes: Task 2 产出的 `--surface-popover/--border-popover/--focus-ring`
- Produces: focus-visible 规则族、`prefers-reduced-motion` 块、`color-scheme` 声明（Task 13 人工矩阵验收）

- [ ] **Step 1: 先写失败测试 — test_dark_theme.py 追加**

a) `test_dark_untouched_tokens_unchanged_from_head` 的 `changed` 集合加入 `"--shadow"`：

```python
    changed = set(_DARK_EXPECTED) | {"--up", "--down", "--ch-codex", "--ch-claudecode", "--ch-bai", "--shadow"}   # 20260909: --shadow 深色减弱 (D2)
```

b) 文件末尾追加：

```python
# ---------------------------------------------------------------------------
# 9. 20260909 D2: 层级/焦点/浮层/减少动画
# ---------------------------------------------------------------------------


def test_dark_shadow_softened_20260909():
    dark = _dark_vars(_css())
    assert dark["--shadow"] == "0 1px 2px rgba(0,0,0,.2), 0 4px 14px rgba(0,0,0,.24)"


def test_focus_visible_rules_exist():
    """§3.1: select 恢复 focus-visible; switch 焦点画在相邻 slider; 按钮/导航/pill 有焦点环."""
    css = _css()
    assert ".select:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 1px; }" in css
    assert ".sw input:focus-visible + .slider { outline: 2px solid var(--focus-ring); outline-offset: 2px; }" in css
    for sel in (".btn:focus-visible", ".pill:focus-visible", ".tb-btn:focus-visible", ".side-item:focus-visible"):
        assert sel in css, f"缺少 {sel} 焦点规则"


def test_popover_consumers_use_tokens():
    css = _css()
    assert ".user-menu { position: absolute; top: calc(100% + 8px); right: 0; min-width: 230px; background: var(--surface-popover); border: 1px solid var(--border-popover); border-radius: 10px; box-shadow: var(--shadow); padding: 5px; z-index: 90; }" in css
    assert ".modal-card { background: var(--surface-popover); border: 1px solid var(--border-popover);" in css
    assert ".toast { background: var(--surface-popover); border: 1px solid var(--border-popover);" in css


def test_reduced_motion_block_and_dark_th_layer():
    css = _css()
    assert "@media (prefers-reduced-motion: reduce)" in css
    assert 'html[data-theme="dark"] .tbl th { background: var(--muted); }' in css


def test_color_scheme_declared_per_theme():
    css = _css()
    root_block = _extract_block(css, ":root")
    dark_block = _extract_block(css, 'html[data-theme="dark"]')
    assert "color-scheme: light;" in root_block
    assert "color-scheme: dark;" in dark_block
```

- [ ] **Step 2: 运行测试确认失败**

Run: `rtk proxy python -m pytest tests/test_dark_theme.py -q`
Expected: FAIL（新 5 个测试失败；旧测试全过）

- [ ] **Step 3: 实现 style.css 修改（6 处）**

a) dark 块 `--shadow` 行替换：

| 旧 | 新 |
|---|---|
| `--shadow: 0 1px 2px rgba(0,0,0,.3), 0 6px 20px rgba(0,0,0,.35);` | `--shadow: 0 1px 2px rgba(0,0,0,.2), 0 4px 14px rgba(0,0,0,.24);` |

b) `:root` 块开头（`--bg: #f7f6f4;` 之前）加一行：`  color-scheme: light;`
   dark 块开头（`--bg: #111112;` 之前）加一行：`  color-scheme: dark;`
   （非 `--*` 声明，不影响既有锚测试的变量解析）

c) 3 条浮层规则整行替换：

| 旧 | 新 |
|---|---|
| `.modal-card { background: var(--card); border: 1px solid var(--border); border-radius: 16px; box-shadow: 0 20px 60px rgba(16, 24, 40, 0.25); padding: 26px 30px; width: 400px; max-width: 90vw; text-align: center; animation: pop .18s ease; }` | `.modal-card { background: var(--surface-popover); border: 1px solid var(--border-popover); border-radius: 16px; box-shadow: 0 20px 60px rgba(16, 24, 40, 0.25); padding: 26px 30px; width: 400px; max-width: 90vw; text-align: center; animation: pop .18s ease; }` |
| `.toast { background: var(--card); border: 1px solid var(--border); border-radius: 10px; box-shadow: var(--shadow); padding: 10px 16px; font-size: 13px; display: flex; align-items: center; gap: 8px; animation: slide .2s ease; max-width: 340px; }` | `.toast { background: var(--surface-popover); border: 1px solid var(--border-popover); border-radius: 10px; box-shadow: var(--shadow); padding: 10px 16px; font-size: 13px; display: flex; align-items: center; gap: 8px; animation: slide .2s ease; max-width: 340px; }` |
| `.user-menu { position: absolute; top: calc(100% + 8px); right: 0; min-width: 230px; background: var(--card); border: 1px solid var(--border); border-radius: 10px; box-shadow: var(--shadow); padding: 5px; z-index: 90; }` | `.user-menu { position: absolute; top: calc(100% + 8px); right: 0; min-width: 230px; background: var(--surface-popover); border: 1px solid var(--border-popover); border-radius: 10px; box-shadow: var(--shadow); padding: 5px; z-index: 90; }` |

d) 文件末尾追加焦点与表头规则：

```css
/* ---- 20260909 D2: 焦点可见 (select 曾 outline:none 无反馈; switch 真实 input 零尺寸, 焦点画相邻 slider) ---- */
.btn:focus-visible, .pill:focus-visible, .tb-btn:focus-visible, .side-item:focus-visible, .chip:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 1px; }
.select:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 1px; }
.sw input:focus-visible + .slider { outline: 2px solid var(--focus-ring); outline-offset: 2px; }

/* 深色表头轻微提层 (行 hover 已有 var(--muted) 连贯规则, 不新增选择业务) */
html[data-theme="dark"] .tbl th { background: var(--muted); }

/* 减少动画偏好: toast 关闭由 setTimeout 定时驱动, 不依赖动画完成, 节点仍按原时序删除 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation-duration: .01ms !important; animation-iteration-count: 1 !important; transition-duration: .01ms !important; }
}
```

- [ ] **Step 4: 验证**

Run: `rtk proxy python -m pytest tests/test_dark_theme.py tests/test_narrow_layout.py tests/test_window_maximize_ui.py -q`
Expected: PASS（D2 不改布局/字号/窗口规则，相邻测试必须保持绿）

- [ ] **Step 5: 签入（人工确认后执行）**

```bash
git add app/web/style.css tests/test_dark_theme.py
git commit -m "feat: 深色层级/焦点可见/浮层变量/减少动画 (D2)"
```

---

### Task 4: db.py theme 存储契约

**Files:**
- Modify: `app/db.py`（`_DEFAULT_SETTINGS` 1177 行、`get_settings` 1332 行、`save_settings` 1390 行）
- Test: `tests/test_theme_preferences.py`（新建，先写本部分测试）

**Interfaces:**
- Consumes: 现有 `_raw_payload`/`_write_payload`（db.py:476-495）、conftest 的 `tmp_codex_db` fixture
- Produces: `get_settings()["theme"]` → `"light" | "dark" | None`（Task 5/6/9 消费）

- [ ] **Step 1: 先写失败测试 — 新建 tests/test_theme_preferences.py**

```python
"""主题偏好持久化契约测试 (20260909 界面优化计划 §3.4/§5).

覆盖: theme light/dark/非法/null; 读损坏值回退; 保存不覆盖未知键与同步配置;
关闭重开数据库恢复; 首页只注入主题枚举且 Content-Length 正确、无敏感值;
主窗口创建参数匹配已存主题.
"""
from __future__ import annotations

import json

import pytest

from app import db, main, server


# ---------------------------------------------------------------------------
# 1. db 存储契约 (tmp_codex_db: conftest 提供的独立临时数据库 fixture)
# ---------------------------------------------------------------------------


def test_theme_default_is_none(tmp_codex_db):
    assert db.get_settings()["theme"] is None


def test_save_theme_light_dark_roundtrip(tmp_codex_db):
    db.save_settings({"theme": "dark"})
    assert db.get_settings()["theme"] == "dark"
    db.save_settings({"theme": "light"})
    assert db.get_settings()["theme"] == "light"
    # 关闭重开数据库仍恢复
    db.close_db()
    assert db.get_settings()["theme"] == "light"


def test_save_theme_invalid_and_null_ignored(tmp_codex_db):
    db.save_settings({"theme": "dark"})
    db.save_settings({"theme": "neon"})        # 非法枚举: 保留当前值
    assert db.get_settings()["theme"] == "dark"
    db.save_settings({"theme": None})          # null PUT: 忽略
    assert db.get_settings()["theme"] == "dark"
    db.save_settings({"theme": 1})             # 非字符串: 忽略
    assert db.get_settings()["theme"] == "dark"


def test_corrupt_theme_value_read_as_none(tmp_codex_db):
    conn = db.get_db()
    raw = db._raw_payload(conn)
    raw["theme"] = "neon"                      # 模拟历史损坏值直写
    db._write_payload(conn, raw)
    conn.commit()
    assert db.get_settings()["theme"] is None


def test_save_theme_preserves_unknown_keys_and_sync_config(tmp_codex_db):
    db.save_settings({"sync_interval_sec": 900})
    db.save_key_names({"k1": "生产 Key"})       # 非白名单键
    db.save_settings({"theme": "dark"})
    raw = db._raw_payload(db.get_db())
    assert raw.get("key_names") == {"k1": "生产 Key"}, "theme 保存覆盖了未知键"
    assert db.get_settings()["sync_interval_sec"] == 900, "theme 保存覆盖了同步配置"
```

- [ ] **Step 2: 运行确认失败**

Run: `rtk proxy python -m pytest tests/test_theme_preferences.py -q`
Expected: FAIL（`KeyError: 'theme'` 或断言不符——`_DEFAULT_SETTINGS` 尚无 theme）

- [ ] **Step 3: 实现 db.py（3 处）**

a) `_DEFAULT_SETTINGS`（1177 行）追加一键（保持现有注释风格）：

```python
_DEFAULT_SETTINGS = {
    "sync_interval_sec": 300,  # 自动增量同步间隔 (1/5/15/30 分钟)
    "window_days": 60,  # 同步范围: 30/60/90/180, None=所有
    "auto_sync": True,  # 自动增量同步开关
    "show_accounts_panel": False,  # 账户总览面板开关 (侧边栏入口显隐)
    "theme": None,  # 主题偏好 (20260909): "light"/"dark", None=未设置
}
```

b) `get_settings` 改为（损坏值回退）：

```python
def get_settings() -> dict[str, Any]:
    merged = dict(_DEFAULT_SETTINGS)
    merged.update({k: v for k, v in _raw_payload(get_db()).items() if k in _DEFAULT_SETTINGS})
    if merged.get("theme") not in ("light", "dark", None):
        merged["theme"] = None  # 历史损坏值按未设置对待 (20260909)
    return merged
```

c) `save_settings` 的 `for key in _DEFAULT_SETTINGS:` 循环内，在 `elif key in ("auto_sync", "show_accounts_panel"):` 分支之后、`else:` 之前插入：

```python
                elif key == "theme":
                    if payload[key] in ("light", "dark"):   # 非法枚举保留当前值 (null 已被外层 is not None 拦截)
                        current[key] = payload[key]
```

- [ ] **Step 4: 验证（测试 + 编译检查 + 既有设置回归）**

Run: `rtk proxy python -m pytest tests/test_theme_preferences.py tests/test_db_lock.py -q`
Expected: PASS

Run: `rtk proxy python -m py_compile app/db.py`
Expected: 无输出（退出码 0）

- [ ] **Step 5: 签入（人工确认后执行）**

```bash
git add app/db.py tests/test_theme_preferences.py
git commit -m "feat: settings 增加 theme 偏好存储契约 (D3)"
```

---

### Task 5: server.py 首页主题预置注入

**Files:**
- Modify: `app/server.py`（`_static_response` 986-1009 行区域）
- Test: `tests/test_theme_preferences.py`（追加）

**Interfaces:**
- Consumes: Task 4 的 `db.get_settings()["theme"]`
- Produces: `server._inject_theme_seed(body: bytes) -> bytes`；index.html 响应含 `data-theme-preference="light|dark|unset"`（Task 7 的 bootstrap 脚本消费）

- [ ] **Step 1: 先写失败测试 — test_theme_preferences.py 末尾追加**

```python
# ---------------------------------------------------------------------------
# 2. 首页主题预置 (§3.4: 只注入主题枚举, Content-Length 重算, 无敏感值)
# ---------------------------------------------------------------------------


class _FakeHandler:
    """_static_response 最小 handler 替身 (捕获状态/头/体)."""

    def __init__(self):
        self.status = None
        self.headers = {}
        self.body = b""
        self.error = None
        self.wfile = self

    def send_response(self, code):
        self.status = code

    def send_header(self, key, value):
        self.headers[key] = value

    def end_headers(self):
        pass

    def send_error(self, code):
        self.error = code

    def write(self, data):
        self.body += data


def _serve_index() -> _FakeHandler:
    handler = _FakeHandler()
    server._static_response(handler, "index.html")
    return handler


def test_index_seed_dark(tmp_codex_db):
    db.save_settings({"theme": "dark"})
    h = _serve_index()
    assert h.status == 200 and h.error is None
    text = h.body.decode("utf-8")
    assert 'data-theme-preference="dark"' in text
    assert h.headers["Content-Length"] == str(len(h.body)), "注入后 Content-Length 未重算"
    assert h.headers["Cache-Control"] == "no-cache"


def test_index_seed_light_and_unset(tmp_codex_db):
    db.save_settings({"theme": "light"})
    assert 'data-theme-preference="light"' in _serve_index().body.decode("utf-8")
    db.save_settings({"theme": "dark"})
    db._write_payload(db.get_db(), {})   # 模拟无偏好
    db.get_db().commit()
    assert 'data-theme-preference="unset"' in _serve_index().body.decode("utf-8")


def test_index_seed_contains_no_sensitive_values(tmp_codex_db):
    db.save_key_names({"k1": "生产 Key 绝密"})
    db.save_settings({"theme": "dark", "sync_interval_sec": 900})
    text = _serve_index().body.decode("utf-8")
    assert "生产 Key 绝密" not in text and "key_names" not in text
    assert "sync_interval_sec" not in text, "首页注入了 theme 以外的设置"


def test_inject_theme_seed_pure_function(tmp_codex_db):
    raw = '<html lang="zh-CN" data-theme="light">'.encode("utf-8")
    out = server._inject_theme_seed(raw)
    assert b'data-theme-preference=' in out
    # 非 index 内容无标记时原样返回
    assert server._inject_theme_seed(b"plain bytes") == b"plain bytes"
```

- [ ] **Step 2: 运行确认失败**

Run: `rtk proxy python -m pytest tests/test_theme_preferences.py -q`
Expected: FAIL（`AttributeError: ... _inject_theme_seed` 或响应中无注入属性）

- [ ] **Step 3: 实现 server.py（2 处）**

a) 在 `_static_response` 定义之前插入：

```python
def _inject_theme_seed(body: bytes) -> bytes:
    """首页主题预置 (20260909 §3.4): 仅注入 data-theme-preference 枚举 (light/dark/unset),
    无需等待任何 API 请求; 不注入完整 settings/账号/凭据; 读取失败按 unset."""
    try:
        pref = db.get_settings().get("theme")
    except Exception:  # noqa: BLE001 偏好读取失败按未设置处理
        pref = None
    seed = pref if pref in ("light", "dark") else "unset"
    try:
        text = body.decode("utf-8")
    except UnicodeDecodeError:
        return body
    marker = 'data-theme="light"'
    if marker not in text:
        return body
    return text.replace(marker, f'{marker} data-theme-preference="{seed}"', 1).encode("utf-8")
```

b) `_static_response` 函数体内，`with open(path, "rb") as fh: body = fh.read()` 的 `except OSError` 块之后、`handler.send_response(200)` 之前插入：

```python
    if rel == "index.html":
        body = _inject_theme_seed(body)   # Content-Length 在下方按注入后字节重算
```

- [ ] **Step 4: 验证**

Run: `rtk proxy python -m pytest tests/test_theme_preferences.py tests/test_server_overview_cache.py -q`
Expected: PASS

Run: `rtk proxy python -m py_compile app/server.py`
Expected: 无输出（退出码 0）

- [ ] **Step 5: 签入（人工确认后执行）**

```bash
git add app/server.py tests/test_theme_preferences.py
git commit -m "feat: 首页注入 data-theme-preference 主题预置 (D3)"
```

---

### Task 6: main.py 主窗口原生底色

**Files:**
- Modify: `app/main.py`（新增辅助函数 + `create_window` 549-561 行区域）
- Test: `tests/test_theme_preferences.py`（追加）

**Interfaces:**
- Consumes: Task 4 的 `db.get_settings()["theme"]`
- Produces: `main._theme_background_color() -> str`；主窗口 `background_color`（Task 13 原生首帧验收）

- [ ] **Step 1: 先写失败测试 — test_theme_preferences.py 末尾追加**

```python
# ---------------------------------------------------------------------------
# 3. 主窗口原生底色 (§3.4: 与 root/dark --bg 对齐, 避免 HTML 绘制前闪白)
# ---------------------------------------------------------------------------


@pytest.mark.parametrize("theme,expected", [
    ("dark", "#111112"),
    ("light", "#f7f6f4"),
    (None, "#f7f6f4"),
    ("neon", "#f7f6f4"),   # 损坏值: 浅色默认
])
def test_main_window_background_matches_saved_theme(theme, expected, monkeypatch):
    monkeypatch.setattr(db, "get_settings", lambda: {"theme": theme})
    assert main._theme_background_color() == expected


def test_main_window_background_fallback_on_read_error(monkeypatch):
    def _boom():
        raise RuntimeError("db down")
    monkeypatch.setattr(db, "get_settings", _boom)
    assert main._theme_background_color() == "#f7f6f4"


def test_main_window_create_window_uses_theme_background():
    src = (main.__file__ and open(main.__file__, encoding="utf-8").read())
    assert "background_color=_theme_background_color()" in src, "主窗口 create_window 未接主题底色"
```

- [ ] **Step 2: 运行确认失败**

Run: `rtk proxy python -m pytest tests/test_theme_preferences.py -q`
Expected: FAIL（`AttributeError: ... _theme_background_color`）

- [ ] **Step 3: 实现 main.py（2 处）**

a) 在 `main()` 之前（如 `_screen_workarea_logical` 定义之后）插入：

```python
def _theme_background_color() -> str:
    """主窗口原生底色 (20260909 §3.4): 与已保存主题对齐 (dark --bg #111112 / light --bg #f7f6f4),
    避免 HTML 绘制前的原生白底; 未设置或读取失败用浅色默认. 只处理主窗口, 不动第三方登录页."""
    try:
        theme = db.get_settings().get("theme")
    except Exception:  # noqa: BLE001 偏好读取失败按未设置处理
        theme = None
    return "#111112" if theme == "dark" else "#f7f6f4"
```

b) 主窗口 `webview.create_window(` 调用的参数列表中，`js_api=api,` 之前插入一行：

```python
        background_color=_theme_background_color(),
```

（登录子窗口已有的 `background_color="#f7f6f4"` 保持不变。）

- [ ] **Step 4: 验证**

Run: `rtk proxy python -m pytest tests/test_theme_preferences.py tests/test_main_settings.py -q`
Expected: PASS

Run: `rtk proxy python -m py_compile app/main.py`
Expected: 无输出（退出码 0）

- [ ] **Step 5: 签入（人工确认后执行）**

```bash
git add app/main.py tests/test_theme_preferences.py
git commit -m "feat: 主窗口原生底色对齐已存主题 (D3)"
```

---

### Task 7: index.html 首屏 bootstrap + Node 执行测试

**Files:**
- Modify: `app/web/index.html`（head，样式链接之前）
- Test: `tests/test_theme_bootstrap.py`（新建）

**Interfaces:**
- Consumes: Task 5 注入的 `data-theme-preference`；旧键 `localStorage["gousage-dark"]`
- Produces: 首帧前根元素 `data-theme` + `color-scheme`；Task 9 的 init 从该根主题建立 state

- [ ] **Step 1: 先写失败测试 — 新建 tests/test_theme_bootstrap.py**

```python
"""首屏 bootstrap 脚本执行级测试 (20260909 §3.4/§5).

用 Node 执行从 index.html head 提取的内联脚本, 只提供 document.documentElement
与 localStorage 最小替身 — 禁止依赖正文 DOM/Chart/桌面桥接/网络.
覆盖 服务端 light/dark/unset/缺失 × 旧键 1/0/非法/读取抛错.
缺 Node 时 skip (skip 不算通过, D4 必须实跑).
"""
from __future__ import annotations

import json
import re
import shutil
import subprocess
from pathlib import Path

import pytest

_ROOT = Path(__file__).resolve().parents[1]
_HTML = _ROOT / "app" / "web" / "index.html"
_APP_JS = _ROOT / "app" / "web" / "app.js"


def _bootstrap_script() -> str:
    html = _HTML.read_text(encoding="utf-8")
    head = html.split("</head>", 1)[0]
    m = re.search(r"<script>(?P<body>.*?)</script>", head, flags=re.S)
    assert m, "index.html head 缺少内联 bootstrap <script>"
    return m.group("body")


def test_bootstrap_script_sits_before_stylesheet():
    html = _HTML.read_text(encoding="utf-8")
    assert html.index("<script>") < html.index('<link rel="stylesheet" href="style.css">'), \
        "bootstrap 必须位于主样式链接之前 (首帧前应用主题)"


_RUNNER = r"""
const fs = require("fs");
const [scriptPath, casesPath] = process.argv.slice(2);
const script = fs.readFileSync(scriptPath, "utf8");
const cases = JSON.parse(fs.readFileSync(casesPath, "utf8"));
const results = cases.map((c) => {
  const el = { _attrs: {}, dataset: {}, style: {},
    getAttribute(k) { return Object.prototype.hasOwnProperty.call(this._attrs, k) ? this._attrs[k] : null; } };
  if (c.pref !== null) el._attrs["data-theme-preference"] = c.pref;
  const localStorage = { getItem(k) {
    if (c.throwOnRead) throw new Error("denied");
    return Object.prototype.hasOwnProperty.call(c.store, k) ? c.store[k] : null;
  } };
  const document = { documentElement: el };
  try { eval(script); return { theme: el.dataset.theme ?? null, colorScheme: el.style.colorScheme ?? null }; }
  catch (e) { return { error: String(e) }; }
});
console.log(JSON.stringify(results));
"""

# (pref, store, throwOnRead, expected_theme)
_CASES = [
    ("dark", {}, False, "dark"),
    ("light", {"gousage-dark": "1"}, False, "light"),   # 服务端偏好优先于旧键
    ("unset", {"gousage-dark": "1"}, False, "dark"),    # unset 时旧键接管
    ("unset", {"gousage-dark": "0"}, False, "light"),
    ("unset", {"gousage-dark": "bogus"}, False, "light"),  # 非法旧值走默认
    ("unset", {}, True, "light"),                       # 读取异常走默认
    (None, {"gousage-dark": "1"}, False, "dark"),       # 属性缺失按 unset
    (None, {}, False, "light"),
]


def test_bootstrap_matrix(tmp_path):
    node = shutil.which("node")
    if not node:
        pytest.skip("缺 Node: 执行级测试不计通过, 安装 Node 后重跑")
    script = tmp_path / "bootstrap.js"
    cases = tmp_path / "cases.json"
    runner = tmp_path / "runner.js"
    script.write_text(_bootstrap_script(), encoding="utf-8")
    cases.write_text(json.dumps([{"pref": p, "store": s, "throwOnRead": t} for p, s, t, _ in _CASES]), encoding="utf-8")
    runner.write_text(_RUNNER, encoding="utf-8")
    proc = subprocess.run([node, str(runner), str(script), str(cases)], capture_output=True, text=True, timeout=30)
    assert proc.returncode == 0, proc.stderr
    results = json.loads(proc.stdout)
    assert len(results) == len(_CASES)
    for (pref, store, thr, expected), got in zip(_CASES, results):
        assert got == {"theme": expected, "colorScheme": expected}, \
            f"pref={pref} store={store} throw={thr}: 期望 {expected}, 实际 {got}"


def test_init_applies_theme_independent_of_version_request():
    """init 从已应用根主题建立 state, 不在版本响应后重置主题."""
    js = _APP_JS.read_text(encoding="utf-8")
    body = js[js.index("(async function init()"):]
    assert 'document.documentElement.dataset.theme === "dark"' in body, "init 未从根元素主题建立 state"
    assert body.index("applyDarkMode(dark)") < body.index('/api/version'), \
        "init 仍在版本响应后才应用主题 (首屏会被延迟/失败重置)"
```

- [ ] **Step 2: 运行确认失败**

Run: `rtk proxy python -m pytest tests/test_theme_bootstrap.py -q`
Expected: FAIL（head 无内联 script；init 两个源码断言也失败——后者在 Task 9 转绿，本任务先容忍，在 Task 9 的验证里复跑）

- [ ] **Step 3: 实现 index.html**

将第 6-7 行：

```html
<title>GoGauge</title>
<link rel="stylesheet" href="style.css">
```

改为：

```html
<title>GoGauge</title>
<script>
/* 首屏主题 bootstrap (20260909 §3.4): 服务端 seed → 旧 localStorage 键 → 默认 light.
   只设置根元素主题与 color-scheme, 不依赖正文 DOM/Chart/桌面桥接/网络;
   必须在主样式链接之前执行, 非法旧值或读取异常走默认. */
(function () {
  var pref = document.documentElement.getAttribute("data-theme-preference");
  var theme = "light";
  if (pref === "dark" || pref === "light") {
    theme = pref;
  } else {
    try {
      if (localStorage.getItem("gousage-dark") === "1") theme = "dark";
    } catch (e) { /* 读取异常走默认 light */ }
  }
  document.documentElement.dataset.theme = theme;
  document.documentElement.style.colorScheme = theme;
})();
</script>
<link rel="stylesheet" href="style.css">
```

- [ ] **Step 4: 验证**

Run: `rtk proxy python -m pytest tests/test_theme_bootstrap.py -q -k "not init_applies"`
Expected: PASS（init 源码断言属 Task 9 范围，此步豁免）

- [ ] **Step 5: 签入（人工确认后执行）**

```bash
git add app/web/index.html tests/test_theme_bootstrap.py
git commit -m "feat: 首屏主题 bootstrap 脚本与 Node 执行测试 (D3)"
```

---

### Task 8: Playwright 设施与持久化交互测试（先失败）

**Files:**
- Modify: `requirements-dev.txt`
- Test: `tests/test_theme_interactions.py`（新建）

**Interfaces:**
- Consumes: Task 5/6/7 已就位的 seed/bootstrap；浏览器将加载真实 `app/web`
- Produces: 持久化交互测试组（引用 Task 9 才实现的 `setThemePreference`，本任务结束时应失败）

- [ ] **Step 1: 固定开发依赖并安装**

`requirements-dev.txt` 全文改为：

```text
pytest>=8
playwright==1.55.0
```

Run: `rtk proxy python -m pip install -r requirements-dev.txt`
Expected: 安装成功（若 `playwright==1.55.0` 无法解析，按 pip 报错列出的最新 1.x 版本号更新该行后重试，并把实际版本记入本文件注释）

Run: `rtk proxy python -m playwright install chromium`
Expected: Chromium 下载完成

- [ ] **Step 2: 新建 tests/test_theme_interactions.py（持久化组）**

```python
"""主题交互执行级测试 (20260909 §5): Playwright + Chromium 实跑前端.

设施: ThreadingHTTPServer 伺服真实 app/web; /api/* 全部拦截 — 按前缀返回
非敏感 fixture, 未匹配路径 500 明拒; PUT /api/settings 记录请求体并可控失败/延迟.
不启动真实采集, 不用示例固定数据冒充用户真实运行结果.
缺浏览器时 skip (skip 不算通过, D4 必须实跑).
"""
from __future__ import annotations

import functools
import json
import threading
import time
from http.server import SimpleHTTPRequestHandler, ThreadingHTTPServer
from pathlib import Path

import pytest

pytest.importorskip("playwright.sync_api", reason="缺 playwright: pip install -r requirements-dev.txt && python -m playwright install chromium")
from playwright.sync_api import sync_playwright  # noqa: E402

_ROOT = Path(__file__).resolve().parents[1]
_WEB = _ROOT / "app" / "web"


def _fixture(path: str):
    p = path.split("?", 1)[0]
    table = {
        "/api/version": {"version": "0-test"},
        "/api/settings": {"sync_interval_sec": 300, "window_days": 60,
                          "auto_sync": True, "show_accounts_panel": False, "theme": None},
        "/api/state": {"logged_in": True, "progress": {"running": False}, "codex": {"running": False}},
        "/api/report/channels": {"rows": []},
        "/api/report/daily": {"labels": [], "series": {}},
        "/api/report/hourly": {"labels": [], "series": {}},
        "/api/report/windows": {"windows": []},
        "/api/accounts/overview": {"accounts": []},
        "/api/zcode/quota": {},
        "/api/dashboard": {"totals": {}},
        "/api/accounts": {"accounts": [], "active_id": None},
    }
    return table.get(p)


class _Handler(SimpleHTTPRequestHandler):
    api_log: list = []          # [(method, path, body_bytes|None)]
    fail_next_put = False
    version_delay_sec = 0.0

    def log_message(self, *args):
        pass

    @classmethod
    def reset(cls):
        cls.api_log = []
        cls.fail_next_put = False
        cls.version_delay_sec = 0.0

    def _json(self, obj, code=200):
        data = json.dumps(obj).encode("utf-8")
        self.send_response(code)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)

    def do_GET(self):
        if self.path.startswith("/api/"):
            if self.path.startswith("/api/version") and type(self).version_delay_sec:
                time.sleep(type(self).version_delay_sec)
            type(self).api_log.append(("GET", self.path, None))
            fx = _fixture(self.path)
            if fx is None:
                self._json({"ok": False, "error": "rejected: " + self.path}, 500)   # 未覆盖 API 明拒
            else:
                self._json(fx)
            return
        super().do_GET()

    def do_PUT(self):
        length = int(self.headers.get("Content-Length") or 0)
        body = self.rfile.read(length)
        type(self).api_log.append(("PUT", self.path, body))
        if self.path.startswith("/api/settings"):
            if type(self).fail_next_put:
                type(self).fail_next_put = False
                self._json({"ok": False, "error": "boom"}, 500)
                return
            self._json(json.loads(body or b"{}"))
            return
        self._json({"ok": False, "error": "rejected"}, 500)


@pytest.fixture
def web_server():
    _Handler.reset()
    srv = ThreadingHTTPServer(("127.0.0.1", 0), functools.partial(_Handler, directory=str(_WEB)))
    threading.Thread(target=srv.serve_forever, daemon=True).start()
    yield f"http://127.0.0.1:{srv.server_address[1]}"
    srv.shutdown()
    srv.server_close()


@pytest.fixture(scope="session")
def browser():
    with sync_playwright() as pw:
        b = pw.chromium.launch()
        yield b
        b.close()


def _puts():
    return [json.loads(body) for (m, p, body) in _Handler.api_log if m == "PUT" and p.startswith("/api/settings")]


def _gets():
    return [p for (m, p, _) in _Handler.api_log if m == "GET"]


def _wait_put(page, count, timeout=8000):
    deadline = time.time() + timeout / 1000
    while time.time() < deadline:
        if len(_puts()) >= count:
            return
        time.sleep(0.05)
    raise AssertionError(f"等待 {count} 次 settings PUT 超时, 实际 {len(_puts())}: {_puts()}")


# ---------------------------------------------------------------------------
# A. 持久化链路 (setThemePreference, Task 9 实现前本组失败)
# ---------------------------------------------------------------------------


def test_two_entries_share_one_persistence_path(web_server, browser):
    page = browser.new_page()
    page.goto(web_server)
    page.wait_for_selector("#tb-theme")
    page.click("#tb-theme")                                   # 顶栏入口
    page.wait_for_function("document.documentElement.dataset.theme === 'dark'")
    _wait_put(page, 1)
    assert _puts()[-1] == {"theme": "dark"}
    page.click('.side-item[data-page="settings"]')
    page.click('#set-theme-pills .pill[data-v="light"]')      # 设置页入口
    page.wait_for_function("document.documentElement.dataset.theme === 'light'")
    _wait_put(page, 2)
    assert _puts()[-1] == {"theme": "light"}
    page.close()


def test_rapid_toggle_persists_latest_intent(web_server, browser):
    page = browser.new_page()
    page.goto(web_server)
    page.wait_for_selector("#tb-theme")
    for _ in range(3):                                        # dark→light→dark 快速连点
        page.click("#tb-theme")
    page.wait_for_function("document.documentElement.dataset.theme === 'dark'")
    deadline = time.time() + 8
    while time.time() < deadline and (not _puts() or _puts()[-1] != {"theme": "dark"}):
        time.sleep(0.05)
    assert _puts() and _puts()[-1] == {"theme": "dark"}, f"最终持久化不是最新意图 dark: {_puts()}"
    page.close()


def test_failed_save_toasts_and_retries_once(web_server, browser):
    _Handler.fail_next_put = True
    page = browser.new_page()
    page.goto(web_server)
    page.wait_for_selector("#tb-theme")
    page.click("#tb-theme")
    _wait_put(page, 2)                                        # 首次失败 + 重试一次
    toast = page.wait_for_selector(".toast.err", timeout=8000)
    assert "主题未保存" in toast.inner_text()
    page.wait_for_selector(".toast.err", state="detached", timeout=8000)   # toast 仍按原定时删除
    page.close()


def test_theme_toggle_issues_no_business_requests(web_server, browser):
    page = browser.new_page()
    page.goto(web_server)
    page.wait_for_selector("#tb-theme")
    page.wait_for_load_state("networkidle")
    time.sleep(0.5)                                           # 等 init 各请求落定
    _Handler.api_log.clear()
    page.click("#tb-theme")
    _wait_put(page, 1)
    time.sleep(0.5)
    assert _gets() == [], f"切主题产生了额外业务 GET: {_gets()}"
    page.close()


def test_old_key_migrates_once_when_seed_unset(web_server, browser):
    ctx = browser.new_context()
    ctx.add_init_script("try{localStorage.setItem('gousage-dark','1')}catch(e){}")
    page = ctx.new_page()
    page.goto(web_server)                                     # 静态伺服无 seed 属性 → 按 unset
    page.wait_for_function("document.documentElement.dataset.theme === 'dark'")   # bootstrap 旧键即时生效
    _wait_put(page, 1)
    time.sleep(0.5)
    assert _puts() == [{"theme": "dark"}], f"迁移应只保存一次: {_puts()}"
    ctx.close()


def test_version_delay_does_not_reset_theme(web_server, browser):
    _Handler.version_delay_sec = 5.0
    ctx = browser.new_context()
    ctx.add_init_script("try{localStorage.setItem('gousage-dark','1')}catch(e){}")
    page = ctx.new_page()
    page.goto(web_server, wait_until="domcontentloaded")
    page.wait_for_function("document.documentElement.dataset.theme === 'dark'", timeout=2000)
    page.close()
    ctx.close()
```

- [ ] **Step 3: 运行确认持久化组失败**

Run: `rtk proxy python -m pytest tests/test_theme_interactions.py -q`
Expected: FAIL（`_wait_put` 超时——`setThemePreference` 未实现，点击不产生 PUT；`test_old_key_migrates_once_when_seed_unset` 的 PUT 断言失败；版本延迟用例此时能通过属正常）

- [ ] **Step 4: 签入（人工确认后执行）**

```bash
git add requirements-dev.txt tests/test_theme_interactions.py
git commit -m "test: 主题交互 Playwright 设施与持久化用例 (先失败, D3)"
```

---

### Task 9: JS 持久化链路实现

**Files:**
- Modify: `app/web/app.js`（I18N 两处、`applyDarkMode` 后插入队列、464/2408 两入口、init 2890-2910）

**Interfaces:**
- Consumes: Task 7 bootstrap 设置的根主题；`api()`（app.js:342）、`toast()`（:429）、`t()`
- Produces: `setThemePreference(on)`（Task 8 测试转绿的实现）

- [ ] **Step 1: I18N 加文案（中英文各一键）**

zh 字典第 9 行，`themeToggle: "切换主题",` 之后插入：`themeSaveFailed: "主题未保存，重试",`
en 字典第 132 行，`themeToggle: "Toggle theme",` 之后插入：`themeSaveFailed: "Theme not saved, retrying",`

- [ ] **Step 2: `applyDarkMode` 函数（511-517 行）之后插入持久化队列**

```js
/* 主题持久化 (20260909 §3.4): applyDarkMode 保持纯界面应用 + 旧键兼容写入;
   保存走单串行队列 + 最新意图合并 (快速 dark→light→dark 最终存 dark);
   PUT 响应不回写 state (后到响应不能覆盖新选择); 失败 toast 一次并重试一次,
   仍走本队列; 重试失败保留界面选择, 不宣称重启恢复. */
const themeSaveQueue = { pending: null, running: false, retried: false };
function setThemePreference(on) {
  applyDarkMode(on);
  themeSaveQueue.pending = on;
  if (!themeSaveQueue.running) _drainThemeSaveQueue();
}
async function _drainThemeSaveQueue() {
  themeSaveQueue.running = true;
  try {
    while (themeSaveQueue.pending !== null) {
      const target = themeSaveQueue.pending;
      themeSaveQueue.pending = null;
      try {
        await api("/api/settings", { method: "PUT", body: JSON.stringify({ theme: target ? "dark" : "light" }) });
        themeSaveQueue.retried = false;
      } catch (e) {
        if (themeSaveQueue.pending === null && !themeSaveQueue.retried) {
          themeSaveQueue.retried = true;
          themeSaveQueue.pending = target;
          toast(t("themeSaveFailed"), "err");
        }
      }
    }
  } finally {
    themeSaveQueue.running = false;
  }
}
```

> **勘误（20260910 评审）**: 本 Task Step 2 的队列代码有缺陷——`retried` 只在成功路径重置，一轮"首试失败+重试再失败"后残留为 `true`，此后新意图的首试失败会被静默吞掉。修复：把 `themeSaveQueue.retried = false` 移到 `setThemePreference`（新意图入队处），并删除成功路径的重置；回归用例 `test_each_new_intent_gets_fresh_retry_budget` 已固化该行为（见 tests/test_theme_interactions.py）。实施时以仓库当前 app.js 为准。

- [ ] **Step 3: 两个用户入口改调 setThemePreference**

- 第 464 行：`$("tb-theme").addEventListener("click", () => applyDarkMode(document.documentElement.dataset.theme !== "dark"));` → `$("tb-theme").addEventListener("click", () => setThemePreference(document.documentElement.dataset.theme !== "dark"));`
- 第 2408 行：`document.querySelectorAll("#set-theme-pills .pill").forEach((b) => b.addEventListener("click", () => applyDarkMode(b.dataset.v === "dark")));` → 同句末尾改为 `setThemePreference(b.dataset.v === "dark")));`

- [ ] **Step 4: init 改造（2890-2910 行区域）**

旧开头：

```js
(async function init() {
  let dark = false, cur = "CNY", l = "zh";
  try {
    dark = localStorage.getItem("gousage-dark") === "1";
    cur = localStorage.getItem("gousage-currency") || "CNY";
    l = localStorage.getItem("gousage-lang") || "zh";
  } catch (e) { /* ignore */ }
  try { const v = await api("/api/version"); APP_VERSION = v.version || ""; } catch (e) { /* ignore */ }
  applyLang(l);
  applyDarkMode(dark);
  applyCurrency(cur);
```

新：

```js
(async function init() {
  // bootstrap 已在首帧前设定根主题; init 从已应用根主题建立 state,
  // 版本请求延迟或失败不能重置主题 (20260909 §3.4)
  let dark = document.documentElement.dataset.theme === "dark", cur = "CNY", l = "zh";
  try {
    cur = localStorage.getItem("gousage-currency") || "CNY";
    l = localStorage.getItem("gousage-lang") || "zh";
  } catch (e) { /* ignore */ }
  applyLang(l);
  applyDarkMode(dark);
  applyCurrency(cur);
  try { const v = await api("/api/version"); APP_VERSION = v.version || ""; } catch (e) { /* ignore */ }
```

`try { state.settings = await api("/api/settings"); } catch (e) { /* ignore */ }` 行之后插入旧键迁移：

```js
  // 旧偏好迁移: 仅服务端 unset 且当前来源旧键有效时保存一次;
  // 随机旧端口/旧临时 WebView 存储中已不可访问的数据不承诺找回 (20260909 §3.4)
  try {
    if (document.documentElement.getAttribute("data-theme-preference") === "unset") {
      const old = localStorage.getItem("gousage-dark");
      if (old === "1" || old === "0") setThemePreference(old === "1");
    }
  } catch (e) { /* ignore */ }
```

- [ ] **Step 5: 验证（语法 + 交互组 + bootstrap 全量 + I18N 一致性）**

Run: `rtk proxy node --check app/web/app.js`
Expected: 无输出（退出码 0）

Run: `rtk proxy python -m pytest tests/test_theme_interactions.py tests/test_theme_bootstrap.py tests/test_i18n_consistency.py tests/test_theme_rerender.py -q`
Expected: PASS（bootstrap 的 init 源码断言在此转绿）

- [ ] **Step 6: 签入（人工确认后执行）**

```bash
git add app/web/app.js
git commit -m "feat: 主题持久化队列/双入口/init 首屏顺序/旧键迁移 (D3)"
```

---

### Task 10: 交互测试扩展——十图/tooltip/图标/减少动画（先失败）

**Files:**
- Test: `tests/test_theme_interactions.py`（追加 B 组）

**Interfaces:**
- Consumes: Task 8 设施；Task 11 才实现的 `chartColors/chartThemeOptions`
- Produces: 图表主题执行级验收（本任务结束时应失败）

- [ ] **Step 1: 在 test_theme_interactions.py 末尾追加 B 组**

```python
# ---------------------------------------------------------------------------
# B. 图表主题与视觉行为 (Task 11/12 实现前本组大部分失败)
# ---------------------------------------------------------------------------

_CHART_BUILDERS = """
window.__built = [];
function reg(name, inst) { window.__built.push([name, inst]); return inst; }
reg('cToday', (chartToday([{hour:'00',input:5,output:3}], true), cToday));
reg('cModel', (chartModel([{model:'gpt-5',uncached_input_tokens:10,total_output_tokens:5,total_cost_usd:0.01,request_count:3,hit_rate:50}], true), cModel));
reg('cTrend', (chartTrend([{date:'2026-09-01',total_cost_usd:1,request_count:2,total_input_tokens:3,total_output_tokens:4,total_reasoning_tokens:5}], true), cTrend));
reg('cZcodeTrend', (chartZcodeTrend([{date:'2026-09-01',total_input_tokens:1,total_output_tokens:1,total_reasoning_tokens:1,total_cost_usd:0.5}], true), cZcodeTrend));
reg('cClaudecodeTrend', (chartClaudecodeTrend([{date:'2026-09-01',total_tokens:3,total_cost_usd:0.5}], true), cClaudecodeTrend));
reg('cCodexTrend', (chartCodexTrend([{date:'2026-09-01',total_tokens:3,request_count:2}], true), cCodexTrend));
reg('cOvTrendChart', (chartOvTrend([{daily7:[{date:'2026-09-01',total_cost_usd:1,request_count:1,total_input_tokens:1,total_output_tokens:1,total_reasoning_tokens:1}]}], true), cOvTrendChart));
reg('cStack', (chartReportStack({labels:['2026-09-01'],series:{zcode:[5]},metric:'tokens'}, true), cStack));
reg('cDonut', (chartReportDonut({labels:['2026-09-01'],series:{zcode:[5]},metric:'tokens'}, true), cDonut));
reg('cHourly', (chartReportHourly({labels:[0],series:{zcode:[5]}}, true, 'noDataInRange'), cHourly));
window.__built.length;
"""


def _expected_palette(page):
    return page.evaluate("""() => {
      const cs = getComputedStyle(document.body);
      const v = (n) => cs.getPropertyValue(n).trim();
      return { input: v('--chart-input'), output: v('--chart-output'), extra: v('--chart-extra'),
               tooltipBg: v('--chart-tooltip-bg'), tooltipText: v('--chart-tooltip-text'),
               tooltipBorder: v('--chart-tooltip-border') };
    }""")


def test_ten_charts_use_theme_palette_and_tooltip(web_server, browser):
    ctx = browser.new_context()
    ctx.add_init_script("try{localStorage.setItem('gousage-dark','1')}catch(e){}")
    page = ctx.new_page()
    page.goto(web_server)
    page.wait_for_function("document.documentElement.dataset.theme === 'dark'")
    page.wait_for_load_state("networkidle")   # 等 init/loadReportAll 落定, 防迟到空数据重绘销毁已建实例
    exp = _expected_palette(page)
    assert page.evaluate(_CHART_BUILDERS) == 10
    report = page.evaluate("""() => window.__built.map(([name, c]) => ({
      name,
      firstBg: Array.isArray(c.data.datasets[0].backgroundColor) ? c.data.datasets[0].backgroundColor[0] : (c.data.datasets[0].backgroundColor || c.data.datasets[0].borderColor || null),
      tooltipBg: c.options.plugins.tooltip && c.options.plugins.tooltip.backgroundColor || null,
      tooltipText: c.options.plugins.tooltip && c.options.plugins.tooltip.bodyColor || null,
      anim: c.options.animation === undefined ? 'default' : c.options.animation,
      callbacksKept: !!(c.options.plugins.tooltip && c.options.plugins.tooltip.callbacks),
    }))""")
    by_name = {r["name"]: r for r in report}
    # 指标色: chartToday 首系列 = --chart-input
    assert by_name["cToday"]["firstBg"] == exp["input"]
    # 模型环第六色 = --chart-extra ( palette[5] )
    model_palette = page.evaluate("cModel.data.datasets[0].backgroundColor")
    assert model_palette[5] == exp["extra"]
    # 十图 tooltip 底色/文字色全部来自变量 (保留既有 callbacks 的图不得丢)
    with_callbacks = {"cModel", "cTrend", "cZcodeTrend", "cClaudecodeTrend", "cCodexTrend", "cOvTrendChart"}
    for name, r in by_name.items():
        assert r["tooltipBg"] == exp["tooltipBg"], f"{name} tooltip 底色未走主题变量"
        assert r["tooltipText"] == exp["tooltipText"], f"{name} tooltip 文字色未走主题变量"
        assert r["anim"] is False, f"{name} noAnim 被破坏"
        if name in with_callbacks:
            assert r["callbacksKept"], f"{name} 丢失了既有 tooltip callbacks"
    ctx.close()


def test_hidden_page_chart_uses_current_theme(web_server, browser):
    ctx = browser.new_context()
    ctx.add_init_script("try{localStorage.setItem('gousage-dark','1')}catch(e){}")
    page = ctx.new_page()
    page.goto(web_server)
    page.wait_for_function("document.documentElement.dataset.theme === 'dark'")
    page.wait_for_load_state("networkidle")   # 先等首页加载链落定
    page.evaluate("switchPage('stats')")
    exp = _expected_palette(page)
    # 单次 evaluate 原子完成建图+读色, 防 switchPage 触发的异步 loadDashboard 穿插覆盖 cTrend
    first = page.evaluate("""() => {
      chartTrend([{date:'2026-09-01',total_cost_usd:1,request_count:2,total_input_tokens:3,total_output_tokens:4,total_reasoning_tokens:5}], true);
      return cTrend.data.datasets[0].borderColor;
    }""")
    assert first == exp["input"], "进入隐藏页后绘制的图表未按当前主题取色"
    ctx.close()


def test_icons_swap_in_place_on_theme_toggle(web_server, browser):
    page = browser.new_page()
    page.goto(web_server)
    page.wait_for_selector("#tb-theme")
    page.evaluate("""() => {
      const body = document.getElementById('records-body');
      body.innerHTML = '<img alt="gpt" src="icons/gpt.svg">';
    }""")
    expected_dark = page.evaluate("themedName('gpt', true)")
    page.click("#tb-theme")
    page.wait_for_function("document.documentElement.dataset.theme === 'dark'")
    src = page.get_attribute("#records-body img", "src")
    assert expected_dark in src, f"图标未原地换成深色变体: {src}"
    page.close()


def test_reduced_motion_toast_still_removed(web_server, browser):
    ctx = browser.new_context(reduced_motion="reduce")
    page = ctx.new_page()
    page.goto(web_server)
    page.wait_for_selector("#tb-theme")
    page.evaluate("toast('probe', 'err')")
    page.wait_for_selector(".toast.err", state="detached", timeout=6000)   # 定时删除不依赖动画
    ctx.close()


def test_sparkline_uses_css_var_not_snapshot(web_server, browser):
    page = browser.new_page()
    page.goto(web_server)
    page.wait_for_selector("#tb-theme")
    svg = page.evaluate("sparklineSvg([1, 3, 2], 'var(--account-1)')")
    assert 'style="stroke:var(--account-1)"' in svg, "sparkline 未用 CSS var (缓存一次性颜色快照)"
    assert 'style="fill:var(--account-1)"' in svg
    page.close()
```

- [ ] **Step 2: 运行确认 B 组失败**

Run: `rtk proxy python -m pytest tests/test_theme_interactions.py -q`
Expected: A 组全过；B 组失败（`chartColors` 未实现→首系列色仍是旧 hex；tooltip 仍是 Chart.js 默认；sparkline 仍是属性注入）——图标与减少动画两条守护用例此时可能已通过，属正常

- [ ] **Step 3: 签入（人工确认后执行）**

```bash
git add tests/test_theme_interactions.py
git commit -m "test: 十图/tooltip/图标/减少动画交互用例 (先失败, D3)"
```

---

### Task 11: 图表主题实现（chartColors/chartThemeOptions + 十图）

**Files:**
- Modify: `app/web/app.js`（删 COLOR、加两函数、十图改造）

**Interfaces:**
- Consumes: Task 2 的 `--chart-*` 变量、`cssVar()`（app.js:2828，自带 `#8a94a8` 回退）
- Produces: `chartColors()`、`chartThemeOptions()`（契约见文首接口表）

- [ ] **Step 1: 替换 COLOR 定义为两个主题函数（第 278 行）**

旧：`const COLOR = { input: "#4f8ef7", output: "#22c55e", reasoning: "#a78bfa", cache: "#06b6d4", cost: "#d97706" };`

新：

```js
/* 图表主题 (20260909 §3.3): Canvas 不能直接用 var(--x), 每次绘制经 cssVar 解析具体色
   (cssVar 自带 #8a94a8 已知回退); chartColors 读六个指标变量, chartThemeOptions 给
   legend/ticks/grid/tooltip 当前主题值; 各图合并时保留自有 callbacks/轴单位/多轴/事件. */
function chartColors() {
  return {
    input: cssVar("--chart-input"),
    output: cssVar("--chart-output"),
    reasoning: cssVar("--chart-reasoning"),
    cache: cssVar("--chart-cache"),
    cost: cssVar("--chart-cost"),
    extra: cssVar("--chart-extra"),
  };
}
function chartThemeOptions() {
  return {
    legendLabel: { usePointStyle: true, boxWidth: 8, font: { size: 11 }, color: cssVar("--text2") },
    tick: { color: cssVar("--text3"), font: { size: 10 } },
    gridColor: cssVar("--grid"),
    tooltip: {
      backgroundColor: cssVar("--chart-tooltip-bg"),
      titleColor: cssVar("--chart-tooltip-text"),
      bodyColor: cssVar("--chart-tooltip-text"),
      borderColor: cssVar("--chart-tooltip-border"),
      borderWidth: 1,
    },
  };
}
```

- [ ] **Step 2: 十图逐图改造（精确替换；每图 `const cc = chartColors();` 置于 `new Chart` 之前）**

① `chartToday`（约 1430-1447 行）：插入 `const cc = chartColors();`；`backgroundColor: COLOR.input` → `backgroundColor: cc.input`；`backgroundColor: COLOR.output` → `backgroundColor: cc.output`；plugins 行改为：

```js
      plugins: { legend: { labels: { usePointStyle: true, boxWidth: 8, font: { size: 11 }, color: cssVar("--text2") } }, tooltip: chartThemeOptions().tooltip },
```

② `chartModel`（约 1514-1526 行）：`const palette = [COLOR.input, COLOR.output, COLOR.reasoning, COLOR.cache, COLOR.cost, "#ec4899"];` → `const cc = chartColors(); const palette = [cc.input, cc.output, cc.reasoning, cc.cache, cc.cost, cc.extra];`；tooltip 行改为：

```js
        tooltip: { ...chartThemeOptions().tooltip, callbacks: { label: (it) => ` ${it.label}: ${fmt(it.parsed)}${total ? ` (${((it.parsed / total) * 100).toFixed(1)}%)` : ""}` } },
```

③ `chartTrend`（约 1554-1574 行）：插入 `const cc = chartColors();`；三处 `COLOR.input/output/reasoning` → `cc.input/cc.output/cc.reasoning`；tooltip 对象改为：

```js
        tooltip: {
          ...chartThemeOptions().tooltip,
          callbacks: {
            label: (item) => {
              if (item.dataset.label === t("totalTokens")) return ` ${item.dataset.label}: ${fmtTokens(item.parsed.y)}`;
              if (item.dataset.label === t("totalRequests")) return ` ${item.dataset.label}: ${fmtInt(item.parsed.y)}`;
              return ` ${item.dataset.label}: ${fmtMoney(item.parsed.y)}`;
            },
          },
        },
```

④ `chartZcodeTrend`（约 995-1005 行）：插入 `const cc = chartColors();`；`COLOR.reasoning` → `cc.reasoning`、`COLOR.input` → `cc.input`；tooltip 行改为：

```js
        tooltip: { ...chartThemeOptions().tooltip, callbacks: { label: (it) => ` ${it.dataset.label}: ${it.dataset.yAxisID === "y" ? fmtTokens(it.parsed.y) : fmtMoney(it.parsed.y)}` } },
```

⑤ `chartClaudecodeTrend`（约 1206-1216 行）：插入 `const cc = chartColors();`；`COLOR.reasoning` → `cc.reasoning`、`COLOR.input` → `cc.input`；tooltip 行改为：

```js
        tooltip: { ...chartThemeOptions().tooltip, callbacks: { label: (it) => ` ${it.dataset.label}: ${it.dataset.yAxisID === "y" ? fmtTokens(it.parsed.y) : fmtMoney(it.parsed.y)}` } },
```

⑥ `chartCodexTrend`（约 1361-1371 行）：插入 `const cc = chartColors();`；`COLOR.reasoning` → `cc.reasoning`、`COLOR.output` → `cc.output`；tooltip 行改为：

```js
        tooltip: { ...chartThemeOptions().tooltip, callbacks: { label: (it) => ` ${it.dataset.label}: ${it.dataset.yAxisID === "y" ? fmtTokens(it.parsed.y) : fmtInt(it.parsed.y)}` } },
```

⑦ `chartOvTrend`（约 1992-2003 行）：插入 `const cc = chartColors();`；`COLOR.input` → `cc.input`、`COLOR.output` → `cc.output`、`COLOR.reasoning` → `cc.reasoning`；tooltip 行改为：

```js
        tooltip: { ...chartThemeOptions().tooltip, callbacks: { label: (it) => it.dataset.label === t("totalRequests") ? ` ${it.dataset.label}: ${fmtInt(it.parsed.y)}` : it.dataset.label === t("totalTokens") ? ` ${it.dataset.label}: ${fmtTokens(it.parsed.y)}` : ` ${it.dataset.label}: ${fmtMoney(it.parsed.y)}` } },
```

⑧ `chartReportStack`（约 2721 行）：系列色已走 `chColor()`（渠道 CSS var，不动）；plugins 行改为：

```js
      plugins: { legend: { labels: { usePointStyle: true, boxWidth: 8, font: { size: 11 }, color: cssVar("--text2") } }, tooltip: chartThemeOptions().tooltip },
```

⑨ `chartReportDonut`（约 2762 行）：plugins 行改为：

```js
      plugins: { legend: { position: "bottom", labels: { usePointStyle: true, boxWidth: 8, font: { size: 11 }, color: cssVar("--text2") } }, tooltip: chartThemeOptions().tooltip },
```

（`onClick` 扇区切渠道与 `centerText` 插件保持原样。）

⑩ `chartReportHourly`（约 2788 行）：plugins 行改为：

```js
      plugins: { legend: { display: chs.length > 1, labels: { usePointStyle: true, boxWidth: 8, font: { size: 11 }, color: cssVar("--text2") } }, tooltip: chartThemeOptions().tooltip },
```

- [ ] **Step 3: 确认 COLOR 零残留**

Run: `rtk proxy grep -n "COLOR\." app/web/app.js`
Expected: 无 `COLOR.input/output/...` 输出（`CH_COLOR`/`chColor` 是渠道色，名字不同，保留）

- [ ] **Step 4: 验证**

Run: `rtk proxy node --check app/web/app.js`
Expected: 无输出（退出码 0）

Run: `rtk proxy python -m pytest tests/test_theme_interactions.py tests/test_theme_rerender.py -q`
Expected: PASS（B 组图表用例转绿；`test_sparkline_uses_css_var_not_snapshot` 仍红属 Task 12 范围）

- [ ] **Step 5: 签入（人工确认后执行）**

```bash
git add app/web/app.js
git commit -m "feat: 图表主题公共逻辑与十图指标/tooltip 变量化 (D3)"
```

---

### Task 12: 账号色 CSS 变量化

**Files:**
- Modify: `app/web/app.js`（1841、1873、1930、1950-1960 行区域）

**Interfaces:**
- Consumes: Task 2 的 `--account-1..6`、Task 10 的 sparkline 测试
- Produces: 账户卡/sparkline 随主题即时变色（不缓存一次性颜色快照，§3.3）

- [ ] **Step 1: 四处精确替换**

a) 第 1841 行删除整行：

```js
const OV_COLORS = ["#7c5cf6", "#4f8ef7", "#22c55e", "#d97706", "#06b6d4", "#ec4899"];
```

b) 第 1873 行：

旧：`const accounts = (data.accounts || []).map((a, i) => ({ ...a, color: OV_COLORS[i % OV_COLORS.length] }));`
新：`const accounts = (data.accounts || []).map((a, i) => ({ ...a, colorCss: `var(--account-${(i % 6) + 1})` }));   // 20260909 §3.3: 存变量名而非解析色, 主题切换即时生效`

c) 第 1930 行：

旧：`const spark = sparklineSvg((a.today_trend || []).map((d) => d.input + d.output + d.reasoning), a.color);`
新：`const spark = sparklineSvg((a.today_trend || []).map((d) => d.input + d.output + d.reasoning), a.colorCss);`

d) `sparklineSvg`（约 1950-1960 行）改签名与两处注入：

旧：

```js
function sparklineSvg(values, color) {
  const w = 120, h = 30, n = values.length;
  if (!n) return `<svg viewBox="0 0 ${w} ${h}" class="spark"></svg>`;
  const max = Math.max(...values, 1);
  const step = n > 1 ? w / (n - 1) : w;
  const pts = values.map((v, i) => `${(i * step).toFixed(1)},${(h - 2 - (v / max) * (h - 4)).toFixed(1)}`);
  return `<svg viewBox="0 0 ${w} ${h}" class="spark" preserveAspectRatio="none">
    <polyline points="${pts.join(" ")}" fill="none" stroke="${color}" stroke-width="1.6" stroke-linejoin="round" stroke-linecap="round"/>
    <polyline points="0,${h} ${pts.join(" ")} ${w},${h}" fill="${color}" opacity="0.12" stroke="none"/>
  </svg>`;
}
```

新（SVG 属性不支持 var(), 改走 style；fill 透明度用 fill-opacity）：

```js
function sparklineSvg(values, colorCss) {
  const w = 120, h = 30, n = values.length;
  if (!n) return `<svg viewBox="0 0 ${w} ${h}" class="spark"></svg>`;
  const max = Math.max(...values, 1);
  const step = n > 1 ? w / (n - 1) : w;
  const pts = values.map((v, i) => `${(i * step).toFixed(1)},${(h - 2 - (v / max) * (h - 4)).toFixed(1)}`);
  return `<svg viewBox="0 0 ${w} ${h}" class="spark" preserveAspectRatio="none">
    <polyline points="${pts.join(" ")}" fill="none" style="stroke:${colorCss}" stroke-width="1.6" stroke-linejoin="round" stroke-linecap="round"/>
    <polyline points="0,${h} ${pts.join(" ")} ${w},${h}" style="fill:${colorCss}" fill-opacity="0.12" stroke="none"/>
  </svg>`;
}
```

- [ ] **Step 2: 确认 OV_COLORS 零残留**

Run: `rtk proxy grep -n "OV_COLORS" app/web/app.js`
Expected: 无输出

- [ ] **Step 3: 验证**

Run: `rtk proxy node --check app/web/app.js`
Expected: 无输出（退出码 0）

Run: `rtk proxy python -m pytest tests/test_theme_interactions.py tests/test_theme_rerender.py -q`
Expected: PASS（全部转绿）

- [ ] **Step 4: 签入（人工确认后执行）**

```bash
git add app/web/app.js
git commit -m "feat: 账号色 CSS 变量化, sparkline 不缓存颜色快照 (D3)"
```

---

### Task 13: 全量回归、桌面人工验收矩阵与证据归档

**Files:**
- Create: `artifacts/dark-theme-review/验收记录.md`、`artifacts/dark-theme-review/dark-*.png`、`artifacts/dark-theme-review/light-*.png`

**Interfaces:**
- Consumes: 全部前置 Task；Task 1 基准图
- Produces: D4 验收证据（缺一項则对应验收项保持未完成，不得宣称通过）

- [ ] **Step 1: 全量自动回归**

Run: `rtk proxy node --check app/web/app.js`
Expected: 无输出（退出码 0）

Run: `rtk proxy python -m pytest tests -q`
Expected: 全量 PASS。若有失败，先对照基线区分：本轮引入的失败必须修复后才能继续；既有失败（本任务开始前就红）逐项记录在验收记录中，不得隐瞒

- [ ] **Step 2: 桌面人工验收矩阵（向人工发出操作单）**

```text
请人工按规格 §5「实际桌面矩阵」验收 (源码运行一次 + PyInstaller 包一次):
1. 页面: 全部/单渠道首页、统计各源、两张记录表、账户总览、设置、关于、欢迎页、
   用户菜单、弹框、toast — 深色截图 dark-<页面>.png
2. 窗口: 1000×680 / 1280×840 / 最大化; DPI 100%/125%/150% (实际可用工作区)
3. 内容: 中/英文、长模型名、大数字、0、未知值、空态、加载、错误、禁用
4. 图表: 十图主题前后数值/顺序/轴不变; 连续切主题十次图表实例不持续增长
   (DevTools 或观察内存); 进出隐藏页尺寸正常
5. 键盘: Tab 经过按钮/导航/pill/select/switch 有可见焦点环; 回车/空格原功能可用
6. 启动: 先保存深色, 真正退出(托盘退出, 非关窗)再启动 ×3, 允许端口变化;
   用手机录像或连拍区分原生窗口与网页首帧, 无浅色闪帧;
   断网/延迟版本接口仍保持主题
7. 浅色: 保存浅色后同样启动/切换; 浅色画面与 Task 1 前一致
8. 桥接: 拖动、最小化、最大化/还原、关到托盘、再打开、退出、登录成功回调
结果逐项记录在 artifacts/dark-theme-review/验收记录.md (通过/失败/跳过+原因);
缺证据的项目保持「未完成」, 不得改写为通过
```

- [ ] **Step 3: 归档颜色说明**

把 Task 2/3 的最终色值与对比度（以 `tests/test_dark_theme.py` §8/§9 实测输出为准）抄入 `artifacts/dark-theme-review/验收记录.md` 的「最终颜色说明」一节，并注明与 Task 1 基准图的差异点。

- [ ] **Step 4: 签入（人工确认后执行）**

```bash
git add artifacts/dark-theme-review/
git commit -m "docs: 深色主题验收证据与最终颜色说明 (D4)"
```

---

## 规格覆盖核对（Self-Review 留档）

| 规格条目 | 落实 Task |
|---|---|
| §3.1 组件/对比度/焦点/减少动画/color-scheme | 2、3、10、13 |
| §3.2 25 变量与消费点契约 | 2 |
| §3.3 chartColors/chartThemeOptions、十图、账号 SVG、禁改 chart.umd | 11、12 |
| §3.4 存储/首页预置/首屏顺序/迁移/切换队列/原生底色 | 4、5、6、7、9 |
| §5 test_theme_preferences / bootstrap / interactions | 4-7、8、10 |
| §5 验证命令（node --check / 两个 pytest） | 各 Task 验证步、13 |
| §5 桌面矩阵与证据位置 | 1、13 |
| D1–D4 交付顺序与独立验收 | 1+2(D1)、3(D2)、4-12(D3)、13(D4) |
| §6 风险：不回退、不冒称恢复、不删目标 | 全局约束、13 Step 2 |

**已知执行期观察项（review 留痕，非阻塞）**：

1. 旧键迁移"保存一次"的执行级断言放在 test_theme_interactions.py A 组（`test_old_key_migrates_once_when_seed_unset`），bootstrap 测试按规格保持无网络。
2. §3.1 字号为"建议"档：现状 body 14px、.tbl 12.5px、.kpi-v 23px 均在建议区间内，唯一例外 .tbl th 11.5px；为遵守"浅色逐像素不变"的全局约束，本计划不改动任何字号，D2 人工矩阵按可读性验收。
3. 契约外 `var(--red)` 消费点（.ub-error、.toast.err 边条、tb-login 未登录色）按 §3.2 明确排除，本计划保持不动；如后续要纳入需先修订规格契约表。
