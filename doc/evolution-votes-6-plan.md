# 门禁2 投票记录 · EVOLUTION-6（实施计划）

被评审材料：`doc/20260905-evolution-plan-6.md`（v1 → 第 1 轮 3/3 通过 → v2）

## 第 1 轮（2026-09-05）

### 架构师：通过（锚点核实全部属实：dark 块 23 变量=④表 13 换值+10 保持；badge.ok/硬编码/c-slate/PLAN_BADGE 逐字核实；顺序约束真实存在）
- [建议] "亮色 :root 一行不动"措辞精确化（追加 --up/--down 新声明例外），防验收对照争议。
- [建议] 测试分层断言：底色类固定 hex / --up--down 仅断言存在+对比度 / --text3 固定可随实测修订。
- [建议] badge.ok dark 软底对比度纳入程序化断言（≥3.8:1 一行断言）。
- [建议] 死代码实为各 1 行（非 2 行）。

### 产品经理：通过（门禁1 三席全部建议核实已落实进计划文本）
- [建议] 预览不通过处置路径（token 层微调后再确认一轮）。
- [建议] 选中态兜底带具体值 rgba(0,0,0,.4)。
- [建议] 六页截图产出物形式/落点写明（.probe/ui-shots/）。

### 用户体验官：通过（层次递进/EVOLUTION-4 硬约束/--up--down 基准/亮色等值路径/全部行号核实成立）
- [建议] 走查补骨架屏项（sk-* 随新 --muted，shimmer 高光独立覆盖不变，预期零回归但高频可见）。
- [建议] .plan-badge 基类（style.css:298，总览页"活跃"徽标在用）补入小字徽标目视组合（第三处同组合）。
- [建议] 截图计数明确（首页 2 张 + 5 页 = 7 张）。
- [建议] 瞬态组件（用户菜单/确认弹框/toast）补 1-2 张截图或口头确认项。

## 第 1 轮判定

3/3 通过 → streak=1。吸收建议修订至 v2（全部文档级精确化）→ 第 2 轮。

## 第 2 轮（2026-09-05，评审对象 v2）

### 架构师：未返回（子代理并发超限，第 3 轮补跑）

### 产品经理：通过（对照表 3 条：2 落实、1 部分落实）
- [建议] 选中态兜底具体值 rgba(0,0,0,.4) 未落入文本（v2 部分替换未命中），补一词消除验收争议。
- [建议] 总览页截图前置：#side-overview 默认 hidden（app.js applyOverviewPanel 控制设置开关），测试点 3 补一句防 7 张凑不齐误判。
- [建议] 确认体验官席两建议（骨架屏/.plan-badge）是否真写入——提请属实：v2 声明吸收但文本缺失。

### 用户体验官：驳回
- [阻塞1] 骨架屏走查项未落实（v2 声明已吸收但 grep 无"骨架"字样）：sk-* 全族消费 var(--muted)，本次换值直接改其观感，属暗色高频可见面且 7 张静态截图拍不到加载瞬间。改法：走查明细追加一行（含 shimmer 独立覆盖不变的预期零回归说明）。
- [阻塞2] .plan-badge 基类（style.css:298，总览页"活跃"徽标在用）未补入小字徽标目视组合（仍只 est-badge/zcode-badge 两处）。改法：第 57 行括号补入。
- [建议] 选中态兜底补具体值 rgba(0,0,0,.4)；瞬态组件 toast 边条现状注记（var(--green)/var(--red) 亮色继承属现状非回归）。
- 两阻塞均为纯文档级追加（约两行），补齐后预计零争议通过。

## 第 2 轮判定

体验官驳回（2 阻塞，均为主 agent 替换脚本未命中的文档缺失）→ streak=0。主 agent 确认属实并已补齐至 v3（附 PM 两条建议）→ 第 3 轮（架构师席补跑第 2 轮内容）。

## 第 3 轮（2026-09-05，评审对象 v3）

### 架构师：通过（对照表 4/4 落实；对比度数字抽查全部可信）
- [建议] 不做清单 :root 字面冲突补括注（除新增 --up/--down 外）；静态断言补 :root 不可变锚（逐项断言改前值，尤其 --ch-dsh #64748b）与 sk-* var(--muted) 断言；.ub 装饰渐变不在改动面，随首页截图目视即可。

### 产品经理：未返回（子代理并发超限，第 4 轮前补跑完成——见第 4 轮记录）

### 用户体验官：通过（对照表 2 阻塞+2 建议 4/4 落实）
- [建议] 静态断言补 sk-* var(--muted) 一行（骨架屏零回归程序化）；瞬态组件截图/口头确认口径分工明确化。

## 第 3 轮判定

架构师/体验官通过、PM 席故障未返回。为保三席完整，第 4 轮前以同材料补跑 PM 席（其第 2 轮对照在 votes 第 4 轮记录中合并呈现）。材料修订至 v4（吸收架构师/体验官建议）。

## 第 4 轮（2026-09-05，评审对象 v4）

### 架构师：通过（对照表 3/3 落实）
- [建议] 文档措辞残留不一致：头部"死代码 2 行"/第 4 节标题"各约 2 行"与正文"各 1 行"冲突，统一为"合计 2 行（各 1 行）"。
- [建议] .ub 装饰渐变行号区间 205-215 漏 218 行（c-bai::before 渐变），建议写 205-218 或补句。
- 判定注记："3/3 落实，无阻塞项。"

### 产品经理：通过（补跑第 3 轮，对照表 3/3 落实）
- [建议] 死代码行数措辞统一（同上）；7 张截图补文件命名约定（01-home-all…07-about）。
- 附加核实：对比度声明实算成立（--text3 5.06:1 / --down 3.54:1 / badge.no 3.76:1）；验收链条闭环；:root 不可变锚为实质加固。

### 用户体验官：通过（对照表 2/2 落实）
- [建议] sk-box 基类块内无 background（实际在 .sk-box::after, style.css:172），断言按"选择器名含 sk-box 的规则块（含伪元素）"实现防假失败。
- [建议] --text3 对 --muted 复算约 4.66:1 贴线，若实测偶发低于线向提亮一档微调、不动 --muted。

## 门禁2 最终判定（EVOLUTION-6）

第 4 轮 3/3 通过 → **streak=2，门禁2 通过**（计划 v4 + 第 4 轮两条措辞级建议随 SDD 落实）。

---

# 阶段4 门禁2 总判定（2026-09-05）

- EVOLUTION-4：3 轮 3/3 通过（plan-4 v3+实施备注）
- EVOLUTION-5：5 轮上限，最终 2/3 通过（plan-5 v6 终稿；架构师 3 阻塞为单句级文档修订已按其改法落入，实施后人工复核）——裁量记录见 votes-5-plan.md
- EVOLUTION-6：4 轮 3/3 通过（plan-6 v4+两条措辞级建议）
- **进入阶段5 SDD 执行**：实施顺序 plan-4 → plan-5（合并基线：plan-4 先落地）→ plan-6；统一编译 pytest 验证。
