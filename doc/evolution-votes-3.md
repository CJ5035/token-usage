# 投票记录：EVOLUTION-3

# EVOLUTION-3 门禁1（诊断评审）投票记录

## 第 1 轮（2026-09-05）：架构师驳回 / PM 通过 / UX官驳回 → streak=0

### 架构师：驳回（2 阻塞 + 5 建议）
- [阻塞] 窗口②事实性错误：fetch_quota 合同永不抛异常（zcode_api.py:263-299 整体转错误 dict），预热失败写错误占位、后续走后台刷新不阻塞——"每次进页签都同步首采"不成立。
- [阻塞] A.2 自相矛盾："快速返回加载中占位"推翻 §6 声明保留的"首次同步返回数据"语义；定稿等待同一拉取 + 超时兜底。
- [建议] 惰性触发替代常驻定时线程；_quota_refreshing 非严格互斥需取舍；B 细节（TimeoutError name/signal 前置/typeof 守卫/WebView2 103+）；7.2 默认值窗口说明；影响面核实确认（_fetch_usd_cny 仅两处调用点无遗漏）。

### 产品经理：通过（4 建议）
语义变化说明（7.2 先显再准）、窗口②措辞澄清、四类验证场景、20s 阈值余量复核。

### 用户体验官：驳回（2 阻塞 + 4 建议）
- [阻塞] §5"可见错误+可重试"缺落点：loadDashboard catch 只 console.error、骨架屏永久残留——B 须补错误占位 + 重试入口。
- [阻塞] TimeoutError 无识别/无本地化/两问题现场无 toast——须 api() 识别 → i18n 键 → 现场可见化三步。
- [建议] A.2 前端契约二选一、慢加载提示（增强）、WebView2 兼容步骤、与备选池"错误恢复链路"合并考虑。

### 汇总
4 条阻塞全部落实为诊断 v2（对照表见诊断文末）：窗口②修正、A.2 定稿等待式严格单飞、B 扩充失败可见化三步、新增 §7 验证场景。进入第 2 轮。

## 第 2 轮（2026-09-05）：3/3 通过 → streak=1

对照表 16 条全部落实（架构师逐条代码复核：fetch_quota 永不抛异常、A.2 等待式单飞无新挂死点、20s>17s 余量成立）。

- 架构师：通过（3 建议 → §9 #1/#2/#7）：单飞原语与预热线程统一、等待者超时行为定死（不得自行再拉取防撞 20s）、行号微误。
- 产品经理：通过（2 建议 → §9 #3 + toast 覆盖点清单/备选池归属）：zcode 现场可见化不对称记备选池；阈值判定线写明。
- 用户体验官：通过（3 建议 → §9 #4/#5/#6）：错误占位须覆盖 renderSkeletons 全部 5 区域（防半死状态）；zcode toast 复用 zcodeQuotaFail 现成键+零打扰语义；重试 quiet 语义幂等。

### 汇总
8 条建议固化为 §9 计划交接清单。进入第 3 轮确认。

## 第 3 轮（2026-09-05）：3/3 通过 → streak=2 → **门禁1 通过**

§9 七项逐条代码核实（含 #2 错误占位形状与 fetch_quota 合同一致、#4 renderSkeletons 5 区域实数、#5 零打扰链成立）。收尾建议（带入计划）：§1 正文 :65 回改 :64；§8 补"dashboard 有重试、zcode 仅 toast 不对等 → 备选池"显式归属；§9 #2 前端已有现成渲染路径（app.js:736-738）无需新增；#1→#2→#3 实现顺序依赖标注；trend 区错误态需移除遮罩 class。

---

# 门禁2（计划评审）投票记录

## 第 1 轮（2026-09-05）：架构师通过 / PM 驳回 / UX官驳回 → streak=0，已修订计划 v2

### 架构师：通过（7 建议）
toast 函数名笔误；t() 可直接调用（删间接层设计）；错误占位须在 loadSeq 守卫内；错误占位 dict 避开 CREDENTIAL_ERROR_PREFIX；_zcode_quota_refreshing 布尔与新锁分工 + import QUOTA_TIMEOUT；main.py 必改；测试 fixture 隔离与超时可参数化。技术正确性核对：单飞/惰性刷新/影响面/回归风险全部成立。

### 产品经理：驳回（1 阻塞 + 4 建议）
- [阻塞] quiet 三态分流缺失：quiet=true 有两处非用户触发调用（app.js:594 5s 自动重试、:1820 重命名静默刷新），catch 无条件占位会打碎正常数据，且 20s 超时使 catch 从"几乎不触发"变"必达"。
- [建议] toast 函数名/err 样式；t() 最简方案；阈值复核补 /api/update/check 16s；检查单模拟手段 + renderZcodeQuota 分支 729-736。

### 用户体验官：驳回（2 阻塞 + 5 建议）
- [阻塞] 首页现场超时表现未定义：冷启动首页走 loadReportAll（catch :2110）与单渠道（:529-532），现状仅 3.2s toast，容器初始为空——超时=空白页+一闪而过；要求明确定义或维持现状为基线。
- [阻塞] quiet 分流缺失（同 PM）。
- [建议] retry 键新增；"替换占位"断裂重试链（if(!innerHTML) 守卫）→ 清空后渲染；统计页后台 loadZcodeQuota toast 现场；renderZcodeQuota(null) 骨架接线（增强）；TimeoutError 删多余设计。

### 汇总
3 条阻塞（quiet 三态/首页现场口径/同源）+ 全部建议落实为计划 v2（对照表 17 条见计划文末）。进入第 2 轮。

## 门禁2 第 2 轮（2026-09-05）：架构师驳回（1 阻塞）/ PM 通过 / UX官通过 → streak=0，已修订 v3

### 架构师：驳回
- [阻塞] 统一单飞协议误杀过期刷新：worker 兼职首采与过期后台刷新（server.py:871），锁内"data 非空即直返"使过期刷新永不外呼、缓存永久滞留旧值，且 §2.2 无用例可拦。要求方案①（复查=data 非空且未过 TTL）或②（锁仅限首采）+ 补过期刷新用例。
- [建议] 占位只写无守卫区域防重试链断裂；update/check 最坏 ≈51.2s 修正（20s 掐断走 updateFailed 可接受须记录）；quiet 无数据分支措辞。
- 17 条对照表逐条核实无虚报；quiet 三态与首页口径技术自洽。

### 产品经理：通过（3 建议：trend 遮罩显式化、update/check 52s 修正、quiet 无数据防御性定义知悉）
### 用户体验官：通过（2 建议：trend sk-box 移除、占位通栏容器+检查单预期视觉）
两阻塞复核解决；17 条对照全部属实。

### 汇总
1 条阻塞 + 5 条建议落实为计划 v3（对照表见计划 §8）。进入第 3 轮。

## 门禁2 第 3 轮（2026-09-05）：3/3 通过 → streak=2 → **门禁2 通过**

架构师：阻塞项修订正确（方案①与 worker 前置写 at 的既有顺序自洽，过期刷新者锁内读旧 at 判定过期照常拉取；无死锁/无长阻塞/无语义回归），计划 v3 可进入派发。
PM：通过（2 条建议级措辞：括号措辞以表达式为准、updater.py 行号微偏）。
UX官：通过（1 条建议：检查单写明三处伴随状态——通栏占位/两区空白/trend 留白 canvas，防误判渲染缺陷）。
