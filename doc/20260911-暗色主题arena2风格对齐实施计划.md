# 暗色主题 arena2（Linear/Vercel 风）对齐实施计划

- 日期：2026-09-11
- 状态：**已实施**（决策点用户未逐一回复，按 §9 推荐默认项执行；全量 pytest 658 通过）
- 参照物：`doc/20260911-配色方案演示-arena2.html`（方案A）及其渲染截图 `artifacts/arena2-linear.png`
- 关联：`doc/20260911-浅色Slate-Indigo配色实施计划.md`（本次只动暗色，亮色零改动）

## 1. 背景与根因

用户反馈：更新后暗色界面与 arena2 演示稿仍有明显差距。

根因：20260911 的更新只把**亮色** `:root` 重定标为 Slate/Indigo；`html[data-theme="dark"]` 仍保留
EVOLUTION-6 中性纯灰底（无蓝调）+ 20260908 渠道色（bai 金黄 / commandcode 翡翠绿 /
claudecode 陶土橙 / codex 梅子紫），与演示稿的「蓝调灰层次 + Indigo 主色 + 6 渠道色」完全不同源。

## 2. 差距清单（对照演示稿逐项）

| # | 项 | arena2 演示稿 | 当前暗色 | 感知 |
|---|------|----------------|-----------|------|
| 1 | 页面底色 | `#0A0B0F` 蓝调近黑 | `#111112` 中性灰黑 | 高 |
| 2 | 卡片/嵌面 | `#111218` / `#171922` 蓝调 | `#1a1a1c` / `#202022` 中性 | 高 |
| 3 | 边框 | `#1F2230` / `#2A2E3F` | `#2a2a2c` / `#3d3d42` | 中 |
| 4 | 品牌主色 | Indigo `#6366F1` | 亮紫 `#9d7cf8` | 高（激活页签/侧栏/链接/进度条全偏紫粉） |
| 5 | 渠道色相 | bai=紫 / commandcode=青 / claudecode=琥珀 / codex=玫红 | 金黄 / 翡翠绿 / 陶土橙 / 梅子紫 | 高（图例/环形图/堆叠柱第一眼） |
| 6 | 堆叠柱 | 垂直渐变 + 顶圆角 6px + 柱宽≤28 | 平涂直角宽柱 | 中 |
| 7 | 环形图 | 细环 62–82% + 扇区间隙 2° + 圆角 4 | 厚环（cutout 62%）无缝 | 中 |
| 8 | 环心 | 两行：范围标签 + 大数 | 仅大数 | 低 |
| 9 | 刷新按钮 | 靛蓝渐变主按钮 | 琥珀描边 pill | 中 |
| 10 | 「今天」卡 | 靛蓝描边 + 左上光晕高亮 | 平卡 | 中 |
| 11 | 数字字体 | 系统无衬线 + tabular-nums | Fira Code 等宽 | 中（两主题全局） |
| 12 | 文本 | `#E6E8EF` / `#9BA1B0` | `#e8e8ea` / `#a3a3a8` | 低 |

明确定不在本次范围（保持现状）：侧栏激活态（微信式左指示条为既定设计）、KPI 左侧色条、顶栏布局、24h 图配色、`--up/--down` 语义色。

## 3. 暗色令牌映射表（仅 `html[data-theme="dark"]` 块，亮色零改动）

对比度均为对新卡面 `#111218` / 新嵌面 `#171922` 的 WCAG 实算值。

| 令牌 | 现值 | 新值 | 说明 |
|------|------|------|------|
| `--bg` | `#111112` | `#0A0B0F` | 演示稿 bg-0 |
| `--card` | `#1a1a1c` | `#111218` | bg-1；卡片/顶栏/侧栏同层（同演示稿） |
| `--muted` | `#202022` | `#171922` | bg-2 嵌面 |
| `--hover` | `#262628` | `#1B1E29` | 高于 muted 一档 |
| `--sidebar` / `--titlebar` | `#151516` / `#1a1a1c` | `#111218` | 同 bg-1 |
| `--border` | `#2a2a2c` | `#1F2230` | |
| `--border-popover` | `#3d3d42` | `#2A2E3F` | border-strong |
| `--grid` | `#242426` | `#1F2230` | 同演示稿 splitLine |
| `--text` / `--text1` | `#e8e8ea` | `#E6E8EF` | |
| `--text2` | `#a3a3a8` | `#9BA1B0` | 新嵌面上 6.77:1 |
| `--text3` | `#8a8a90` | **保持** | 新卡上 5.45:1 达标，且为冻结测试锚；备选蓝调 `#7C8296`（4.89:1） |
| `--primary` | `#9d7cf8` | `#818CF8` | indigo-400，卡上 6.27:1；色相紫→靛蓝对齐演示稿 |
| `--primary-strong` | `#8b6cf6` | `#6F79F5` | |
| `--primary-soft` | `#2a2440` | `#1C2030` | rgba(99,102,241,.12) 叠加 bg-1 的等效实色 |
| `--focus-ring` | `#c4b5fd` | `#A5B4FC` | indigo-300 |
| `--grad-brand` | 紫→蓝 | `135deg #6366F1→#7C3AED` | 演示稿主按钮渐变（dark 块内覆写） |
| `--button-primary-bg` / hover | `#9d7cf8` / `#8b6cf6` | `#5B5FEF` / `#4A4ED6` | 演示稿 `#6366F1` 白字仅 4.47:1（差 0.03），加深一档保 ≥4.5，实施时程序化复验 |
| `--button-primary-text` / `--refresh-hover-text` | `#111112` | `#FFFFFF` | 同演示稿白字 |
| `--surface-popover` / tooltip 三令牌 | 灰系 | `#171922` / `#E6E8EF` / `#2A2E3F` | 同演示稿 tooltip |
| `--account-1` | `#9d7cf8` | `#818CF8` | 跟随主色 |
| `--ch-bai` | `#facc15` 金黄 | `#A78BFA` | violet-400（演示稿 `#8B5CF6` 提亮档），6.87:1 |
| `--ch-commandcode` | `#34d399` 翠绿 | `#06B6D4` | **演示稿原值** cyan-500，7.70:1 |
| `--ch-zcode` | `#818cf8` | **保持** | 即演示稿 `#6366F1` 的 400 提亮档 |
| `--ch-claudecode` | `#d97757` 陶土橙 | `#F59E0B` | **演示稿原值** amber-500，8.70:1 |
| `--ch-codex` | `#d946ef` 梅子紫 | `#F43F5E` | **演示稿原值** rose-500，5.09:1 |
| `--ch-dsh` | `#94a3b8` | **保持** | 即演示稿 `#64748B` 的 400 提亮档 |

渠道色采用「3 原值 + 3 同色相 400 提亮档」：渠道名在明细表首列、配额卡标题处是**彩色正文**，
`bai/zcode/dsh` 的演示稿原值对卡面只有 4.41 / 4.18 / 3.93:1，不达文字 4.5:1，故提亮一档（色相不变，观感几乎一致）。
图表柱/点等图形用法两种取值都满足非文字 ≥3:1。

`--up/--down/--danger-soft/--chart-input 系列/--account-2..6` 不动（新面上验算仍全部达标：up 10.05 / down 6.33）。

## 4. 图表几何（app.js；Chart.js 4.4.7，两主题共用一份几何配置）

1. `chartReportStack`：
   - 仅**最后一个（最顶）系列** `borderRadius: [6,6,0,0]`，其余系列 0（与演示稿 ECharts 做法一致）；
   - `maxBarThickness: 28`；
   - `backgroundColor` 改为 scriptable 垂直渐变 `色值 → 色值+CC`（`chartArea` 未就绪时回退平色，防初始化抖动）。
2. `chartReportDonut`：
   - `radius: '82%'` + `cutout: '62%'`（细环）+ `padAngle: 2` + 扇区 `borderRadius: 4`；
   - 中心插件改两行：上=范围标签（今天/昨天/近7天/近30天/全部，i18n，11px text3），下=数值（22px 600 text1）。
3. 不动：图例（已是 8px 圆点）、tooltip 令牌、堆叠顺序、24h 图。

## 5. 组件样式（style.css，均限 `html[data-theme="dark"]` 作用域）

1. 刷新按钮：`.pill.small.refresh` 改渐变底（`--grad-brand`）白字，hover 提亮。
2. 「今天」卡高亮：`.windows-bar .wb-cell:first-child` 靛蓝描边 `rgba(99,102,241,.35)` + 左上径向光晕（演示稿 period-card.hi）。

## 6. Task 分解（TDD：先红后绿）

| Task | 内容 | 验证 |
|------|------|------|
| 1 测试锚点重定标（先红） | `tests/test_dark_theme.py`：PLAN_DARK 表、`test_theme_tokens_dark_fixed_values` 表（button/focus/account-1）、`test_codex_channel_color_recolored_*` 渠色断言、`test_dark_untouched_tokens_unchanged_from_head` 名单；`tests/test_theme_preferences.py:141` 窗口底色 `#0A0B0F`；新增图表几何锚（radius/cutout/padAngle/maxBarThickness/中心两行） | pytest 红灯清单与本计划一致 |
| 2 style.css | §3 令牌表替换 dark 块 + §5 两处组件样式（转绿） | Task1 测试转绿；亮色锚点测试不变绿→红 |
| 3 app.js + main.py | §4 图表几何；`app/main.py:509` 窗口底色 dark → `#0A0B0F` | 新几何锚转绿 |
| 4 全量回归 + 目视 | `python -m pytest`；运行应用暗色截图与 `artifacts/arena2-linear.png` 并排对比 | 全绿 + 目视一致 |

## 7. 验收标准

1. 全量 pytest 通过；
2. 暗色首页与演示稿并排：底色层次 / 主色 / 6 渠道色 / 柱渐变顶圆角 / 细环+间隙 / 环心两行 / 刷新按钮 / 今天卡高亮 一致；
3. **亮色逐像素不变**（diff 仅限 dark 块 + 两主题共用图表几何 + main.py dark 分支）；
4. 对比度：正文 ≥4.5:1、图形 ≥3:1 程序化校验通过。

## 8. 风险与说明

1. 暗档渠道色第二次重定标，`test_dark_theme.py` 多处值锚需同步（Task 1 已列）；
2. bai（紫）与 zcode（靛蓝）同处蓝紫区、区分度略降——演示稿本身如此，随演示稿接受；
3. 图表几何两主题共用，亮色图表也会获得柱顶圆角/细环（如需亮色保持原样须加主题分支，默认不做）；
4. `--text3` 保持 `#8a8a90`（中性灰），与蓝调底略有冷暖差但达 5.45:1 且不动冻结锚；如要完全蓝调可换 `#7C8296`（4.89:1，需改冻结测试），默认不动。

## 9. 决策点（实施时按推荐默认执行）

1. **范围**：全面对齐（§3+§4+§5）✔ 已执行；
2. **渠道色**：混合档（3 原值 + 3 提亮档，全部 ≥4.5:1）✔ 已执行；
3. **数字字体**：本次不动，保持 Fira Code 等宽（如需对齐演示稿建议独立立项，影响两主题全部页面）。

## 10. 实施记录与偏差（2026-09-11）

1. `--refresh-hover-text` 未改为 `#FFFFFF`（§3 表原计划）：该 token 语义是「琥珀 hover 底上的深字」，
   dark 刷新按钮整体渐变化后 hover 改用主按钮 hover 底（`html[data-theme="dark"] .pill.small.refresh:hover`），
   token 保持 `#111112` 不动，避免「白字对琥珀底 2.9:1」的假失败锚。
2. Task 1 顺带修复**既有红灯**：`test_root_tokens_unchanged_from_head` 以 `git show HEAD` 为基准，
   浅色 slate/indigo 重定标（本分支未提交改动）本就使其失效；新增 `_ROOT_LIGHT_EXPECTED`（21 项基础令牌新值锚定）
   与 `_ROOT_LIGHT_SEMANTIC_CHANGED`（5 项语义令牌豁免，新值已由 `_ROOT_THEME_EXPECTED` 锚定）。
3. 新增图表几何锚 5 个（test_dark_theme.py §10）：堆叠柱渐变/顶圆角/柱厚、细环四件套、环心两行、
   dark 组件覆写、暗档渠道色；`test_theme_rerender.py` 的 `chartReportDonut` 调用串锚同步加 `range` 参数。
4. 验证：`node --check app.js` 通过；`python -m pytest` 全量 **658 passed**。
   视觉验收：重启应用后暗色首页与 `artifacts/arena2-linear.png` 并排比对（待用户确认）。
5. 明确未动（保持现状）：侧栏激活态（微信式左指示条）、KPI 左侧色条、数字字体（Fira Code）、
   配额进度条 `--grad-brand` 填充（演示稿为渠道色实色，属数字字体级独立项）。
