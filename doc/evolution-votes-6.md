# 门禁1 投票记录 · EVOLUTION-6（暗色主题系统性重构）

被评审材料：`doc/evolution-diagnosis-6.md`（v1 → 架构师驳回 → v2 修订）

## 第 1 轮（2026-09-05，评审对象 v1）

### 架构师：驳回

- [阻塞1] 修复方向4 与"亮色逐像素一致"验证点自相矛盾：直接改 var(--green)/var(--red) 使亮色从 #16a34a/#dc2626 变 #22c55e/#ef4444（亮色回归 + 亮底对比度 3.1→2.1:1 下降）。改法（三选一）：a) **首选**新增 --up/--down token（:root=现硬编码零亮色变化，dark 校准 ≥4.5:1）；b) dark 块内新增覆盖（最小化但延续硬编码）；c) 复用 --green/--red 须同步改 :root 并声明亮色影响面（不推荐）。badge.ok 的两种写法也须明确二选一及亮色等值约束。
- [阻塞2] ③修复清单漏 `.wb-s.spike`（style.css:479 #d97706 硬编码）；验证点"全局 grep 无裸 hex"过宽——按字面必命中 .ub 渐变端点（style.css:206/207/214/215）无法通过，需收窄为"文本涨跌/警示/失败色"+豁免清单（.ub 渐变/--ch-*/app.js 数据系列色/--grad-brand）。
- [建议] --grad-brand 未入④表（dark 下被加载条/进度条三处继承，新体系下显眼），补显式决策；若调整须 dark 单独定义不动 :root。
- [建议] --primary-soft 新组合对比度约 4.68:1（现 4.88:1 略降），est-badge 10px 处 AA 边缘，实施实测校准。
- [建议] src-badge/ub-hint 暗色硬编码蓝青不在 token 体系，纳入协调性目视校验并点名。
- [建议] 实施顺序再明确一格（先 4 后 6，或 6 的图表色验收以 4 落地为前提）。
- [建议] 滚动条 thumb 新明度差下静置可见性纳入走查。
- 证据核实：badge.ok 无 dark 覆盖/badge.no 机制/--text3 3.3:1/硬编码三处/dark 块 23 变量全被④表覆盖/--text3 新值 5.06:1 达标——均属实。

### 产品经理：通过

- [建议] 实施顺序写成硬约束（问题4 先落地或同版本，验收先验证重渲生效）。
- [建议] 定义用户预览确认环节（实施后六页暗色截图请用户确认）。
- [建议] .wb-s.spike 一并改 var(--amber)（零视觉变化），使修复条目与验收自洽。
- [建议] 工作量表述校准（约 20+ 处修改，排期按块级替换估）。
- 三项遗漏事实/④表 15 项现值/关键对比度全部复核属实。

### 用户体验官：通过

- [建议] 补 .wb-s.spike 入修复清单（同 PM）。
- [建议] .kpi.c-slate（style.css:238 #64748b）与暗色 --ch-dsh(#94a3b8) 同渠道双源色不一致，本次一揽子 token 化。
- [建议] 验证补两条目视项：pill/seg 选中态辨识度（紫调投影暗色不可见）；--grad-brand 在纯中性底上更"跳"需确认符合决策意图。
- [建议] transition 现状（仅 body .2s，其余硬切）非本问题引入，无需处理。
- 对比度实测：新值 #8a8a90 在 card/muted/bg 上 5.0/4.7/5.5:1 达标；三层明度差不劣化；--up/--down 暗色值 7.6/4.6:1 可过；PLAN_BADGE 死代码核实成立。

## 第 1 轮判定

存在驳回票（架构师 2 阻塞）→ streak=0。主 agent 复核：**全部属实**（--green/--red :root 值确实会使亮色回归；.wb-s.spike 确漏）→ 修订至 v2 → 第 2 轮。

## 第 2 轮（2026-09-05，评审对象 v2）

### 架构师：通过（对照表：2 阻塞+6 建议 8/8 落实，另核实 --up dark 必须校准：现 #dc2626 在新底仅 4.28:1）
- [建议] 豁免清单补 4 条亮色保留项（badge.ok/c-slate/src-badge/ub-hint 亮色规则），或口径明确为"dark 生效范围内无裸 hex"。
- [建议] plan-badge.lite + PLAN_BADGE 死代码：顺手删除（零风险约 2 行）或明确移交，二选一留痕。
- [建议] --up/--down 补进④表"新增"类别（使④表成为唯一实施清单）；dark 实测对比度记入修订备查。

### 产品经理：通过（对照表：4/4 落实）
- [建议] badge.ok 暗色文字色复用 var(--up) 同源定义，避免暗色绿色双源。
- [建议] 选中态若目视不足优先补中性阴影/微调 muted，不为选中态回退灰阶去紫方向。
- [建议] sync-fail 与涨跌红共用 token 的关系写 CSS 注释防单独改色回归。

### 用户体验官：通过（对照表：4 条中 3 落实、1 低优先级项一致豁免）
- [建议] 豁免清单补三对 soft 徽标亮色底色对；plan-badge.lite 死代码删除或列豁免。
- [建议] --up/--down 暗色校准基准面改为实际承载面 --card（#1a1a1c），bg 基准会滑到 4.4:1；或 bg 基准提到 ≥4.7:1 留余量。
- [建议] c-slate 简化：:root --ch-dsh 恰=#64748b，直接改 var(--ch-dsh) 免 dark 覆盖。
- [建议] spike 暗色 ~5.9:1 已核数据写进验证点免重复测。

## 第 2 轮判定

3/3 通过 → streak=1。吸收建议修订至 v3 → 第 3 轮。

## 第 3 轮（2026-09-05，评审对象 v3）

### 架构师：通过（对照表：2 阻塞+6 建议 8 条中 7 落实、1 部分落实——④表两行未补，本轮已补）
- [建议] ④表补 --up/--down 两行（消除"④表'新增'两行"悬空引用）；
- [建议] 联动校验点5 术语残留（--green/--red）改 --up/--down；
- [建议] 豁免清单扩"亮色规则及其 dark 覆盖（含实施后 badge.ok dark 底色）"；
- [建议] --down 校准基准改按最浅承载面 --muted（sync-fail 真实落点 qb-card，card 基准 4.5:1 时 muted 约 4.2:1）；
- [建议] 数值勘误：#dc2626 新 card 复算约 3.6:1（非 4.28）、spike 约 5.5:1（非 5.9），标注"预估值以实测为准"。
- 判定注记："残留问题全部为文档自洽性与校准精度级别，一行级文字即可修复，不动摇方案结构……同意进入阶段 3。"

### 产品经理：通过（对照表：3/3 落实，1 条部分落实本轮补写）
- [建议] sync-fail 校准基准修正（同架构师：按 --muted 或验证点补 muted 面复核）；
- [建议] #dc2626 数值勘误（复算约 3.6:1；"按 bg 校准滑落 4.4:1"实为约 4.1:1）——不改变决策，实测覆盖记备查即可；
- [建议] badge.ok 暗色 soft 底基准按软徽标族口径（≥.badge.no 现状约 3.8:1），不强求 4.5:1 避免过度设计；
- [建议] 选中态处置优先级半句补写入（不回退去紫方向）。

### 用户体验官：通过（对照表：4/4 落实）
- [建议] spike 5.9:1 为 bg 基准，card 基准约 5.5:1，统一口径为 card 基准；
- [建议] #dc2626 独立复算约 3.6:1（同 PM 勘误），实施以工具实测为准勿沿用纸面值；
- [建议] 选中态兜底动作预写（中性阴影 rgba(0,0,0,.4) 或微调 muted，不回退去紫）；
- [建议] .zcode-badge（10.5px）与 est-badge 同组合纳入校准。

## 第 3 轮判定

3/3 通过 → **streak=2，门禁1 通过**（EVOLUTION-6 诊断 v3.1 成立，第 3 轮建议已吸收进 v3.1）

---

# 门禁1 总判定（2026-09-05）

EVOLUTION-4（2 轮通过）/ EVOLUTION-5（3 轮通过）/ EVOLUTION-6（3 轮通过）三问题诊断全部过审，进入阶段3 实施计划。
