# 门禁1 投票记录 · EVOLUTION-5（all 页签 0~3.5s 无反馈空窗）

被评审材料：`doc/evolution-diagnosis-5.md`（v1 → 门禁驳回 → v2 修订）

## 第 1 轮（2026-09-05，评审对象 v1）

### 架构师：驳回

- [阻塞1] 根因①"4① 表达式索引未实施"与当前 HEAD 不符（证据过期）：提交 8fc12d9（2026-09-05）已实施方案4① v2 路线C——db.py:300-310（迁移2d）建 `idx_usage_acct_utc`/`idx_usage_utc`，db.py:2143-2151 `_report_range_sql` 已改 `datetime(col) >= datetime(?)` 确定性谓词。报告按字面名 grep `idx_usage_day` 得出假阴性。要求基于当前工作区重做事实核查并重写根因①。
- [阻塞2] 修复方向1 的 DDL 在 SQLite 中不可执行：`substr(datetime(col,'localtime'),...)` 含非确定性修饰符，禁止入索引（db.py:300-302 注释已记载否决理由）。建议改法：沿用 v2 模式补 `idx_zcode_utc ON zcode_usage(datetime(started_at))` 与 `idx_cc_utc ON claudecode_usage(datetime(started_at))`，谓词端已逐字匹配、自动命中。
- [阻塞3] 残余瓶颈归因缺失：zcode/cc 段（纯列索引不能服务函数谓词，至今 SCAN）且数据主体恰是 zcode——3.2s 冷算真正来源需分段实验定位（三表 EXPLAIN + dsh 扫描计时）。
- [建议1] ②③④经核实成立保留；Promise.all 实为 4 端点并发，daily/hourly 在其后串行；report_windows 实际约 21 条查询（报告"≈12"低估）。
- [建议2] windows 若加缓存，失效清单须写实（现有 6 调用点 + today/yesterday 档时间推进因素 TTL≤1s 或不缓存 + 缓存键含 channel）。
- [建议3] 建索引存量库一次性开销写入验收说明；all 分支 loading/seq 为纯前端改动可先行，不被后端复测阻塞。

### 产品经理：驳回

- [阻塞1] 根因①与代码事实不符（同架构师阻塞1/2）：真实残留是 4① 只覆盖 usage_records，zcode_usage/claudecode_usage 漏覆盖（db.py:220-221/247-249 仅纯列索引），report_daily（db.py:2194/2201）/report_windows（db.py:2299/2309）对两表谓词已定型仍 SCAN。修复方向1 改为对齐 idx_usage_utc 模式补两表索引；验证点 3 改为先跑现状 EXPLAIN（usage 已 USING INDEX、z/cc 为 SCAN）再修后三表全部 USING INDEX。
- [阻塞2] 计时归因不完整：windows 端点内还含 dsh_api.get_dsh_usage() 磁盘扫描（server.py:1067），3.2s 的构成（z/cc SCAN、dsh IO、页缓存冷各占比）未分段定位，收益预估"降至几十 ms"不可信。要求补"3.2s 构成分解"实验，若 dsh 占比显著应纳入修复方向。
- [建议1] 修复方向2 应点明与 20260904 方案③ stale-while-revalidate 的等价关系（swapping 不清旧 DOM，补齐即隐式 SWR），避免阶段3 重复设计独立 SWR 缓存造成过度设计。
- [建议2] loadReportAll 是三波瀑布，daily/hourly 与第一波无数据依赖可并入同一 Promise.all；验证口径改"全链 <500ms"。
- [建议3] 验证点缺守卫场景（连点 4 档位、all↔单渠道往返）；明确 all 分支守卫与 chSeq 共用/独立（共用更稳）。
- [建议4] P2 可保留，整改后复议（若成本低可向 P1 线靠拢表述）。

### 用户体验官：未参与（子代理并发超限，未返回）

## 第 1 轮判定

存在驳回票 → streak=0。主 agent 复核两席驳回：**全部属实**（自查 git log 见 8fc12d9 已签入；grep 复核 db.py:305/309 确有 v2 索引；分段实验证实 dsh 扫描 3494.6ms 为主因、usage 段 0.4ms 已走索引、z/cc 段 34.8/18.1ms SCAN）→ 按意见修订材料至 v2（修订记录见诊断报告文末）→ 进入第 2 轮。

## 第 2 轮（2026-09-05，评审对象 v2 修订版）

### 架构师：通过（附完整对照表核对：12 条意见 10 已落实/1 部分落实/1 流程性提示）
- [建议] dsh 后台化三设计点阶段3 显式写入：① 防重入守卫（照 server.py:249-254 _quota_refreshing 模式，现状 get_dsh_usage 无锁多线程会双重扫描）；② 冷启动语义二选一写明（首次阻塞扫一次 或 返回 _empty_result()+启动预热）；③ stale 标记只进 debug 字段不改响应 schema。
- [建议] 分段实验补最小环境描述（dsh 会话文件数/数据量）；"受影响端点 5 处"实为 5 调用点/4 端点。
- [建议] 验证点措辞：usage 段已 USING INDEX，验收实为 z/cc 段 SCAN→USING INDEX；连点覆盖 range+metric 双入口（app.js:2105）。

### 产品经理：通过（对照表 6 条全落实/部分落实，三项特别关注均认可）
- [建议] 收益限定写明：后台重扫期间 GIL 竞争理论仍可能短暂拖慢，验证点 2 复测"重扫进行中"窗口；若仍拖慢再议子进程（阶段3 备选不进本方案）。
- [建议] SWR 等价声明从修订记录提升到修复方向 3 正文。
- [建议] "5 处"措辞更正为 5 调用点/4 端点（1069 为 1067 后的缓存命中）。
- [建议] 验证补"档位×指标双维快速切换"。
- [建议] 排期：前端 loading/守卫可先行；dsh 后台化为第三次复用 _ensure_quota_async 惰性模式（server.py:237 汇率同款），按"接近 P1"优先排期。

### 用户体验官：通过（本席首轮缺席，直接评审 v2）
- [建议] stale 提示显式决策为"不加"（最大陈旧 ~18.5s 对分钟粒度面板不可感知，横幅是噪音）；**必须补后台重扫连续失败的行为定义**（连续 N 次失败后用户主动刷新降级为前台同步扫、由 swapping 兜底），否则 dsh 数据可能无限期冻结且零提示——现有验证点未覆盖此失败路径。
- [建议] 对齐单渠道模式须连带移植**错误路径的加载态回收**（单渠道 catch app.js:558-559 有 remove("swapping")，loadReportAll 现 catch 只 toast）——漏掉会让 #report-all 永久停在 opacity .55；验证点追加"模拟失败后恢复正常透明度并保留旧内容"。
- [建议] swapping 对 >1s 极端等待可设阈值递进提示（低优先级可不做），两分支同策略。
- [建议] 三波并一波后验收确认 today 档无两段式弹入（hourly 卡 hidden 在第三波才取消）。

## 第 2 轮判定

3/3 通过 → streak=1（目标 streak=2）。按状态机吸收通过票建议修订材料至 v3 → 第 3 轮复审。

## v3 修订说明（第 2 轮 → 第 3 轮之间）

吸收第 2 轮 3 席全部建议：dsh 后台化五设计点（防重入/冷启动语义/stale 标记/不加提示决策/连续失败降级）、catch 加载态回收、SWR 声明提升正文、验证点补失败路径+降级路径+双维切换+重扫窗口期复测+today 档观感、"5 处"措辞更正、实验环境补 415 文件/145.2MB。详见 diagnosis-5.md 修订记录 v3 条目。

## 第 3 轮（2026-09-05，评审对象 v3）

### 架构师：通过（对照表：本席第2轮 3 建议全部落实）
- [建议] 降级语义限定：`loadReportAll` 另有准自动重试路径 app.js:628（5s quiet 静默重试），"用户主动刷新降级前台扫描"应定义为 quiet=false 人工路径，避免静默重试也占持请求线程 3.5s。
- [建议] daily/hourly 并入第一波时保留 hourly 档位条件（app.js:2137 仅 today/yesterday 请求），用条件性 promise（不需要时 Promise.resolve(null)）。
- [建议] all/单渠道按"各自独立 seq + 切换即重发"最小方案验证，级联失效仅实测乱序覆盖时再加（#report-all/#report-single 为独立容器，hidden 容器滞留过期响应无视觉影响）。
- [建议] 冷启动 dsh 空窗口（数秒）列为已知行为写入验收说明（found 分支已核实天然兼容）。
- 判定注记："v3 无新引入的事实错误……同意通过，streak 可计 2。"

### 产品经理：通过（对照表：本席第2轮 5 建议全部落实）
- [建议] "数秒后自愈"精确化为"服务端缓存数秒内就绪，下一次交互即得真数据"（前端事件驱动无轮询）；验证点补"预热完成后切档位可见 dsh 数据到达"。
- [建议] 降级后仍失败的终态：toast 报错 + 保留旧内容，与既有失败路径一致。
- [建议] 后端两项（dsh 后台化 + zcode/cc 索引）合并一次提交一次复测，接近 P1 优先排期。
- [流程卫生] votes-5.md 重复"第 2 轮"标题残留已顺手清理（主 agent 执行）。

### 用户体验官：通过（对照表：本席第2轮 4 建议中 3 落实、1 低优先级未采纳属允许）
- [建议] "自愈"触发路径需定义：首页无周期自动刷新（app.js:628 为配额骨架重试、2329 为同步定时器），预热完成用户不感知——阶段3 明确刷新路径（预热完成广播/5s 静默刷新/如实改述"下次交互时刷新"）并定义 _empty_result 空态观感（0 值行 vs 暂无数据提示）。
- [建议] 双维切换入口补全：档位实为 app.js:1940（经 loadDashboard:524 转 loadReportAll），指标为 app.js:2105，两条链路都要覆盖。

## 第 3 轮判定

3/3 通过 → **streak=2，门禁1 通过**（EVOLUTION-5 诊断 v3 成立，第 3 轮建议带入阶段3）
