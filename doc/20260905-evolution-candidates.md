# 候选问题清单（2026-09-05）

- **项目类型**：全栈（Python 桌面后端 pywebview + 本地 HTTP 服务 + vanilla JS 前端；依据：CLAUDE.md、app/*.py、app/web/*）
- **已读进化日志**：否（`doc/evolution-log.md` 不存在，首次运行）
- **排除范围**：`doc/20260904-bug-diagnosis-home-ui-7issues.md` 的 7 项首页 UI/性能问题（用户已安排修复，工作区已有未提交改动）。三路探针已按排除清单过滤，未重复报告。
- **扫描方式**：三路并行探针（代码与性能 / UI 一致性 / 操作流程易用性），共 15 项候选，取前 3 名。

## 前 3 名候选

| # | 问题 | 维度 | 文件:行号 | 现象 | 预估等级 | 损伤指数 | 影响面×修复收益 |
|---|---|---|---|---|---|---|---|
| 1 | 全局唯一 SQLite 连接被 HTTP 请求线程 + 5 类后台线程共享，无锁、共享同一隐式事务 | 代码（并发安全）/性能 | `app/db.py:63-75`、`db.py:724-771`、`db.py:672-693`、`app/server.py:1505` | 自动同步写入期间用户任何写操作（删账号/切账号/存设置）与同步线程共享同一事务：他线程的 commit 把未提交语句拦腰提交、异常 rollback 回滚他人写入；偶发 `database is locked` | **P1** | 7/10 | 高×高 |
| 2 | 「退出登录」物理删除该账号全部本地用量历史，不可恢复 | 流程（数据安全/防错） | `app/db.py:696-716`、`app/server.py:1227-1235`、`app/web/app.js:1735-1741` | 修同步误点相邻「退出登录」→ DELETE 全部 usage_records/charts_buckets；CommandCode 服务端仅存 24h 明细，本地库是唯一账单存档，重登也找不回 | **P1** | 7/10 | 中×高 |
| 3 | 请求线程内同步网络调用（汇率 10s / ZCode 配额冷缓存 15s）+ 前端 fetch 无超时 | 性能/代码 | `app/server.py:76-94`、`server.py:1086`、`server.py:858-870`、`app/web/app.js:316-324` | 弱网/被墙时冷启动首页 overview 白屏最长 10s（6h TTL 过期再犯）；前端 fetch 无 AbortController，后端冻结多久 UI 骨架屏多久，无反馈无重试 | P2 [部分推测：触发依赖网络环境，冻结上限为静态可证] | 6.5/10 | 高×高 |

## 排序说明

预估严重等级优先（P1 > P2）；同等级内按影响面 × 修复收益：
- #1 影响面「高」（所有开启自动同步的用户常态并发）> #2 影响面「中」（执行过登出的多账号用户），同为高收益。
- #3 为 P2 中损伤指数最高者（6.5），影响面高（弱网必现+在线偶发）× 收益高（两处局部小改）。

## 备选池（本轮不处理，留给后续迭代）

| 来源 | 问题 | 预估等级 | 指数 |
|---|---|---|---|
| 探针C | 同步长任务不可取消、进度无账号、重复触发被静默吞掉 | P2 | 6 |
| 探针B | 切主题后三张图表（分渠道消耗/渠道占比/总览7日）不重绘残留亮色 | P2 | 6 |
| 探针A | 配额 null 时前端每 5s 无上限重拉全量 dashboard 并全图重绘 | P2 | 5.5 |
| 探针A | 共享缓存（quota/overview/zcode）无锁两步非原子写 | P2 | 4.5 |
| 探针C | 错误恢复链路断裂（配额失败指引无效、统计页骨架屏永久卡死） | P2 | 5 |
| 探针B | 同步失败零反馈、刷新/全量同步按钮卡死禁用态（与探针C同步问题部分重叠） | P2 | 5 |
| 探针B | 暗色主题遗漏：badge.ok 亮底刺眼、涨跌色硬编码绕过 token | P2 | 5 |
| 探针C | 登录窗手动关闭后零反馈无限轮询；登录成功整页重载丢页面状态 | P2 | 4.5 |
| 探针C | 红叉行为不一致不可控（托盘可用=静默驻留无提示，托盘失败=直接退出） | P2 | 4 |
| 探针A | settings 单 JSON blob 读改写无锁并发覆盖（与 #1 同根，修复 #1 时顺带评估） | P2低 | 3 |
| 探针B | 顶栏按钮 tooltip 硬编码中文泄漏英文界面 | P2 | 3 |
| 探针B | 表格空态行三套写法并存（20px/24px/.empty-cell） | P2 | 3 |

> 注：探针A确认无问题的项——异常吞噬、console.log/print 残留、TODO/FIXME、SQL 注入、后端 HTTP 超时、文件/连接泄漏、token 日志泄漏、daemon 线程未捕获异常。探针B确认无问题——i18n 字典键完整、响应式覆盖足够、多数异步按钮有三态、文本溢出已处理。
